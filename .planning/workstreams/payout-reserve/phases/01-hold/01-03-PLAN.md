---
phase: 01-hold
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - .planning/workstreams/payout-reserve/phases/01-hold/01-03-VERIFICATION.md
autonomous: true
requirements: [INV-01]

must_haves:
  truths:
    - "git merge-base(HEAD, main) diff 가 변경된 모든 경로가 settlement-service/(+ .planning/ · docs/)에 국한됨을 증명 — payment/order/product/merchant-limit/risk-management/user/api-gateway 경로 0; reserve hold 는 settlement-only [INV-01]"
    - "settlement Flyway 는 정확히 V1 + V2 + V3(V3__create_reserve.sql) — V4+ 없음, V1/V2 diff 0(무수정), payment(및 타 모듈) 마이그레이션 추가 0 [INV-01]"
    - "4모듈 무회귀 스위트(payment/order/product/settlement) green — reserve 배선이 취소 코어·재고 경로·주문 동기화·기존 정산/payout 원장(409 race·retry/DEAD·convergence)을 건드리지 않음 [INV-01]"
  artifacts:
    - .planning/workstreams/payout-reserve/phases/01-hold/01-03-VERIFICATION.md
  key_links:
    - "payout 02-03 게이트와의 유일한 실질 델타는 Flyway 단언 — payout 게이트는 'settlement V2 위 V-file 없음'을 단언했으나 reserve Phase 1 은 'V3 존재 + V4+ 없음 + V1/V2 무수정'으로 뒤집는다. 나머지(confinement·denylist·no-payment-migration·no-regression)는 verbatim"
    - "중복 .sql cleanup(cloud-sync)을 gradle/Flyway 전에 실행 — 안 하면 중복 V-file checksum 실패; macOS-safe 단일따옴표 glob"
---

<objective>
Phase 1(유보 정책 + 승인 홀드)에 대해 INV-01 을 재검증한다: reserve config·approve 유보 배선·상태 조회가 **settlement-service 코드(+ planning/docs)만** 추가했고, payment 취소 코어와 타 모든 모듈은 diff 0, settlement Flyway 는 정확히 V1+V2+V3(V3 가 정당하게 추가됨), 4모듈 무회귀 스위트가 green 임을.

payout 02-03 게이트(`payout/phases/02-hardening/02-03-PLAN.md`)를 클론하되 **Flyway 단언만 뒤집는다**(RESEARCH §INV-01 Gate 델타): payout 게이트는 "settlement V2 위 V-file 없음"을 단언했다. reserve Phase 1 은 V3 를 정당하게 추가하므로 "V3 존재 + V4+ 없음 + V1/V2 무수정 + payment 마이그레이션 0"으로 단언한다. Confinement 정규식 `^(settlement-service/|\.planning/|docs/)` 와 7모듈 diff-0 denylist 는 동일.

Purpose: "reserve hold 는 순수 settlement-service 변경 + 정당한 V3 한 개"라는 구조적 주장을 스크립트화된 재실행 가능 merge-base 게이트로 전환하고, 취소/재고/주문/정산 스위트 무회귀를 확인.
Output: 통과하는 INV-01 게이트 + confinement 결과·V3-only Flyway 단언·모듈별 스위트 결과를 담은 `01-03-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/payout-reserve/phases/01-hold/RESEARCH.md
@.planning/workstreams/payout-reserve/REQUIREMENTS.md
@.planning/workstreams/payout-reserve/phases/01-hold/01-01-PLAN.md
@.planning/workstreams/payout-reserve/phases/01-hold/01-02-PLAN.md
@.planning/workstreams/payout/phases/02-hardening/02-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: INV-01 merge-base 게이트 — settlement-only confinement, V3-only Flyway(V4+ 없음·payment 마이그레이션 0), 타모듈 diff 0</name>
  <files>.planning/workstreams/payout-reserve/phases/01-hold/01-03-VERIFICATION.md</files>
  <precondition>01-01·01-02 가 feat/settlement-payout-reserve(main 분기)에 커밋되어 있어 `git merge-base HEAD main` 이 올바른 base ref(44b9abb) 이다.</precondition>
  <action>
INV-01 merge-base diff 게이트 실행(payout 02-03 Task 1 클론, Flyway 단언만 뒤집음). BASE = git merge-base HEAD main. (1) Confinement: BASE 대비 변경 경로가 모두 ^(settlement-service/|.planning/|docs/) 매칭 — 그 외(payment/order/product/merchant-limit/risk-management/user/api-gateway 또는 루트 config)는 NON_SETTLEMENT_DIFF 로 실패. (2) 모듈 denylist(belt-and-suspenders): 7개 비-settlement 모듈 디렉터리가 BASE 대비 커밋 diff 0. (3) Flyway(payout 게이트에서 뒤집힘): settlement 마이그레이션 디렉터리에 V3__create_reserve.sql **존재**, V4+ **없음**, V1/V2 는 diff 0(무수정), payment(및 타 모듈) 마이그레이션 추가 0. gradle/Flyway 전에 build/ 하위 cloud-sync 중복 마이그레이션 복제본 삭제, 단일따옴표 glob pathspec(BSD/macOS). confinement 결과·변경파일 목록·V3-only 단언·게이트 PASS/FAIL 을 01-03-VERIFICATION.md 에 기록.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /Users/juho/Documents/docswithmulti-reserve; find . -path "*/build/*" -name "* [0-9].sql" -delete; BASE=$(git merge-base HEAD main); BAD=$(git diff $BASE...HEAD --name-only | grep -vE "^(settlement-service/|\.planning/|docs/)" || true); test -z "$BAD" || { echo "NON_SETTLEMENT_DIFF: $BAD"; exit 1; }; DENY=$(git diff $BASE...HEAD --name-only -- payment-service order-service product-service merchant-limit-service risk-management-service user-service api-gateway); test -z "$DENY" || { echo "MODULE_DIFF: $DENY"; exit 1; }; test -f settlement-service/src/main/resources/db/migration/V3__create_reserve.sql || { echo "MISSING_V3"; exit 1; }; SV=$(ls settlement-service/src/main/resources/db/migration/ | grep -E "^V([4-9]|[1-9][0-9])__" || true); test -z "$SV" || { echo "UNEXPECTED_SETTLEMENT_MIGRATION: $SV"; exit 1; }; V12=$(git diff $BASE...HEAD --name-only -- "settlement-service/src/main/resources/db/migration/V1__*" "settlement-service/src/main/resources/db/migration/V2__*" || true); test -z "$V12" || { echo "V1_OR_V2_MODIFIED: $V12"; exit 1; }; PM=$(git diff $BASE...HEAD --name-only -- "payment-service/src/main/resources/db/migration/*" || true); test -z "$PM" || { echo "PAYMENT_MIGRATION: $PM"; exit 1; }; echo INV01_PASS'</automated>
  </verify>
  <done>게이트가 INV01_PASS 출력: 모든 변경 경로가 settlement-service/(+ .planning/ · docs/) 하위, 7개 타모듈 디렉터리 diff 0, settlement 은 V3 추가(V4+ 없음, V1/V2 무수정), payment 마이그레이션 0. confinement + V3-only 범위를 01-03-VERIFICATION.md 에 기록.</done>
</task>

<task type="auto">
  <name>Task 2: 4모듈 무회귀 스위트 — payment/order/product/settlement green</name>
  <files>.planning/workstreams/payout-reserve/phases/01-hold/01-03-VERIFICATION.md</files>
  <action>
중복 .sql cleanup 후 전체 무회귀 스위트 실행: payment(취소 + 재고 reserve/release + convergence/idempotency ITs 무회귀 — reserve 배선이 아무것도 교란 안 함 증명), order, product, settlement(정산 config·sale/cancel 원장·reconcile·query + payout ITs: tracer/poll/query/convergence/PayoutApproveHardening/PayoutRetry — 하위호환으로 payout=net 유지 + 신규 reserve ITs: ReserveHold/ReserveConfig/ReserveQuery). 모듈별 pass/fail 요약을 01-03-VERIFICATION.md 에 append.
  </action>
  <verify>
    <automated>bash -c "cd /Users/juho/Documents/docswithmulti-reserve && find . -path '*/build/*' -name '* [0-9].sql' -delete; ./gradlew :payment-service:test :order-service:test :product-service:test :settlement-service:test"</automated>
  </verify>
  <done>4모듈 스위트 전부 통과. 기존 취소/재고/주문·정산 원장/payout ITs 무회귀(payout=net 유지), 신규 reserve ITs green. 요약을 01-03-VERIFICATION.md 에 기록.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reserve hold → 취소 코어 / 타 모듈 | reserve config·approve 배선이 payment 취소 TX·스케줄러·재고 경로·타 모듈을 교란할 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | 비-settlement 모듈로 새는 미신고 변경 | high | mitigate | merge-base confinement 게이트가 모든 변경 경로 settlement-only + 7모듈 diff 0 단언; verify 에서 실행 |
| T-01-10 | Availability | 부적절한 Flyway 버전(V4+) 또는 payment 마이그레이션 유입 | medium | mitigate | 게이트가 V3 존재·V4+ 없음·V1/V2 무수정·payment 마이그레이션 0 단언 |
| T-01-11 | Denial of Service | cloud-sync 중복 V .sql → Flyway checksum 실패 | medium | mitigate | 모든 build/gate 전 `find . -path '*/build/*' -name '* [0-9].sql' -delete` |
| T-01-SC | Tampering | supply chain(신규 deps) | low | accept | 신규 패키지 0 — 기존 Redisson/web/JPA/Flyway/Mockito 재사용 |
</threat_model>

<verification>
- INV-01 게이트 INV01_PASS(Task 1): settlement-only confinement + 7모듈 diff 0 + V3-only Flyway(V4+ 없음, V1/V2 무수정) + payment 마이그레이션 0.
- `./gradlew :payment-service:test :order-service:test :product-service:test :settlement-service:test` 전부 green(Task 2).
- 01-03-VERIFICATION.md 가 confinement diff 범위·V3-only 단언·모듈별 스위트 결과 기록.
</verification>

<success_criteria>
- INV-01: Phase 1 reserve diff 가 settlement-service/(+ .planning/ · docs/) 국한; payment/order/product/merchant + risk/user/gateway diff 0; Flyway V3 추가(V1/V2 무수정, V4+ 없음, payment 마이그레이션 0); 4모듈 스위트 green.
</success_criteria>

<output>
Create `.planning/workstreams/payout-reserve/phases/01-hold/01-03-SUMMARY.md` when done.
</output>
