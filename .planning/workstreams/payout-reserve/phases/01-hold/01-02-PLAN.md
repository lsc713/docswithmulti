---
phase: 01-hold
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - settlement-service/src/main/java/com/example/settlement/common/exception/ErrorCode.java
  - settlement-service/src/main/java/com/example/settlement/application/exception/InvalidReserveConfigException.java
  - settlement-service/src/main/java/com/example/settlement/application/exception/ReserveConfigNotFoundException.java
  - settlement-service/src/main/java/com/example/settlement/application/exception/ReserveNotFoundException.java
  - settlement-service/src/main/java/com/example/settlement/application/service/ReserveConfigService.java
  - settlement-service/src/main/java/com/example/settlement/application/service/ReserveQueryService.java
  - settlement-service/src/main/java/com/example/settlement/presentation/controller/ReserveConfigController.java
  - settlement-service/src/main/java/com/example/settlement/presentation/controller/ReserveQueryController.java
  - settlement-service/src/main/java/com/example/settlement/presentation/dto/ReserveConfigRequest.java
  - settlement-service/src/main/java/com/example/settlement/presentation/dto/ReserveConfigResponse.java
  - settlement-service/src/main/java/com/example/settlement/presentation/dto/ReserveResponse.java
  - docs/error-catalog.md
  - settlement-service/src/test/java/com/example/settlement/integration/ReserveConfigIntegrationTest.java
  - settlement-service/src/test/java/com/example/settlement/integration/ReserveQueryIntegrationTest.java
autonomous: true
requirements: [RCFG-01, RCFG-02, HOLD-04]

must_haves:
  truths:
    - "PUT /v1/settlements/reserve-config/{merchantId} {reserveRate,reserveCap,holdDays} 가 정책을 멱등 upsert 하고, rate 0≤r<1·scale≤4·cap≥0·holdDays≥0 위반 시 InvalidReserveConfigException → 400 [RCFG-01]"
    - "GET /v1/settlements/reserve-config/{merchantId} 가 저장된 정책을 반환하고 미설정이면 ReserveConfigNotFoundException → 404 [RCFG-02]"
    - "GET /v1/settlements/{id}/reserve 가 유보 상태(amount/status/hold_until/transfer_ref)를 반환하고 미존재면 ReserveNotFoundException → 404 [HOLD-04]"
  artifacts:
    - settlement-service/src/main/java/com/example/settlement/application/service/ReserveConfigService.java
    - settlement-service/src/main/java/com/example/settlement/presentation/controller/ReserveConfigController.java
    - settlement-service/src/main/java/com/example/settlement/presentation/controller/ReserveQueryController.java
    - settlement-service/src/test/java/com/example/settlement/integration/ReserveConfigIntegrationTest.java
    - settlement-service/src/test/java/com/example/settlement/integration/ReserveQueryIntegrationTest.java
  key_links:
    - "신규 예외 3종은 BusinessException 상속 → GlobalExceptionHandler generic 핸들러(:32-38)가 자동 매핑, 전용 @ExceptionHandler 불필요 (GlobalExceptionHandler 무수정)"
    - "config GET 은 신규(settlement config 는 PUT-only) — PayoutAccountController.get(:38-43) shape 클론; 미설정 orElseThrow(RESERVE_CONFIG_NOT_FOUND)"
    - "01-01 이 만든 포트 재사용: ReserveConfigService→MerchantReserveConfigRepository(findConfig/upsert), ReserveQueryService→ReserveRepository.findBySettlementId — 신규 영속/빈 없음"
---

<objective>
RCFG-01/02(유보 정책 PUT/GET)와 HOLD-04(유보 상태 GET)의 REST 표면을 얹는다. 01-01 이 만든 `MerchantReserveConfigRepository`(findConfig/upsert)·`ReserveRepository`(findBySettlementId) 포트를 소비하는 서비스+컨트롤러+DTO+예외만 추가 — 신규 영속/빈/마이그레이션 없음.

검증 실패는 신규 커스텀 예외(InvalidPayoutAccountException 컨벤션 미러): `InvalidReserveConfigException`+`INVALID_RESERVE_CONFIG`(400), `ReserveConfigNotFoundException`+`RESERVE_CONFIG_NOT_FOUND`(404), `ReserveNotFoundException`+`RESERVE_NOT_FOUND`(404). 모두 BusinessException 상속 → generic 핸들러 자동 매핑(전용 핸들러 없음).

Purpose: 정책 설정/조회·유보 상태 조회의 사용자 표면을 완성해 hold 슬라이스를 운영 가능하게 만든다.
Output: 3개 라우트 + 3개 예외/ErrorCode + error-catalog 갱신 + 2개 통합테스트.

Note: 선택 엔드포인트 `GET /v1/merchants/{id}/reserve-balance`(current_held)는 Phase 1 소비자가 없어 descope — Phase 2 릴리스 화면이 필요로 할 때 추가(currentHeld 는 이미 ReserveRepository 에 존재, 얇은 컨트롤러 한 개면 됨).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/payout-reserve/phases/01-hold/RESEARCH.md
@.planning/workstreams/payout-reserve/phases/01-hold/01-01-PLAN.md
@docs/superpowers/specs/2026-08-05-settlement-payout-reserve-design.md
@settlement-service/src/main/java/com/example/settlement/application/service/SettlementConfigService.java
@settlement-service/src/main/java/com/example/settlement/presentation/controller/SettlementConfigController.java
@settlement-service/src/main/java/com/example/settlement/presentation/controller/PayoutAccountController.java
@settlement-service/src/main/java/com/example/settlement/presentation/controller/PayoutController.java
@settlement-service/src/main/java/com/example/settlement/common/exception/ErrorCode.java
@settlement-service/src/main/java/com/example/settlement/presentation/GlobalExceptionHandler.java
@settlement-service/src/main/java/com/example/settlement/application/exception/PayoutNotFoundException.java
@settlement-service/src/test/java/com/example/settlement/integration/MerchantSettlementConfigIntegrationTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: ErrorCode + 예외 3종 + 유보 정책 서비스/컨트롤러 (RCFG-01/02)</name>
  <files>settlement-service/src/main/java/com/example/settlement/common/exception/ErrorCode.java, settlement-service/src/main/java/com/example/settlement/application/exception/InvalidReserveConfigException.java, settlement-service/src/main/java/com/example/settlement/application/exception/ReserveConfigNotFoundException.java, settlement-service/src/main/java/com/example/settlement/application/exception/ReserveNotFoundException.java, settlement-service/src/main/java/com/example/settlement/application/service/ReserveConfigService.java, settlement-service/src/main/java/com/example/settlement/presentation/controller/ReserveConfigController.java, settlement-service/src/main/java/com/example/settlement/presentation/dto/ReserveConfigRequest.java, settlement-service/src/main/java/com/example/settlement/presentation/dto/ReserveConfigResponse.java, docs/error-catalog.md</files>
  <action>
`ErrorCode` 3개 항목 추가(기존 enum 스타일): INVALID_RESERVE_CONFIG(400), RESERVE_CONFIG_NOT_FOUND(404), RESERVE_NOT_FOUND(404). 메시지는 RESEARCH Code Examples 참조.
예외 3종(PayoutNotFoundException.java 미러, BusinessException 상속): `InvalidReserveConfigException(String reason)`→INVALID_RESERVE_CONFIG, `ReserveConfigNotFoundException(long merchantId)`→RESERVE_CONFIG_NOT_FOUND, `ReserveNotFoundException(long settlementId)`→RESERVE_NOT_FOUND. 전용 @ExceptionHandler 추가 금지 — generic BusinessException 핸들러(:32-38)가 httpStatus 로 자동 매핑.
`ReserveConfigService`(SettlementConfigService.java:28-40 클론, MerchantReserveConfigRepository 주입): `setConfig(long merchantId, BigDecimal rate, BigDecimal cap, int holdDays)` @Transactional — 검증(rate null 금지·rate.signum()<0 또는 rate.compareTo(ONE)>=0·rate.scale()>4·cap null 금지 또는 cap.signum()<0·holdDays<0) 위반 시 InvalidReserveConfigException(사유), 통과 시 repo.upsert(merchantId, rate, cap, holdDays). `getConfig(long merchantId)` @Transactional(readOnly=true) → repo.findConfig(merchantId).orElseThrow(() → new ReserveConfigNotFoundException(merchantId)) 반환(도메인 MerchantReserveConfig).
`ReserveConfigController`(SettlementConfigController.java 미러, @RequestMapping("/v1/settlements/reserve-config")): PUT /{merchantId} {reserveRate,reserveCap,holdDays} → service.setConfig → ReserveConfigResponse; GET /{merchantId}(PayoutAccountController.get:38-43 shape 클론) → service.getConfig → ReserveConfigResponse. 라우트는 spec §5.
DTO: `ReserveConfigRequest(BigDecimal reserveRate, BigDecimal reserveCap, Integer holdDays)`, `ReserveConfigResponse(long merchantId, BigDecimal reserveRate, BigDecimal reserveCap, int holdDays)`.
`docs/error-catalog.md`: settlement 섹션에 3개 코드(INVALID_RESERVE_CONFIG 400, RESERVE_CONFIG_NOT_FOUND 404, RESERVE_NOT_FOUND 404) 추가.
  </action>
  <verify>
    <automated>bash -lc 'cd /Users/juho/Documents/docswithmulti-reserve && find . -path "*/build/*" -name "* [0-9].sql" -delete; ./gradlew :settlement-service:test --tests "*ReserveConfigIntegrationTest"'</automated>
  </verify>
  <done>PUT reserve-config → 200 + 저장, 재 PUT overwrite, GET → 200/미설정 404, 잘못된 rate/cap/holdDays → 400. ReserveConfigIntegrationTest green.</done>
</task>

<task type="auto">
  <name>Task 2: 유보 상태 조회 (HOLD-04) + 통합테스트 2종</name>
  <files>settlement-service/src/main/java/com/example/settlement/application/service/ReserveQueryService.java, settlement-service/src/main/java/com/example/settlement/presentation/controller/ReserveQueryController.java, settlement-service/src/main/java/com/example/settlement/presentation/dto/ReserveResponse.java, settlement-service/src/test/java/com/example/settlement/integration/ReserveConfigIntegrationTest.java, settlement-service/src/test/java/com/example/settlement/integration/ReserveQueryIntegrationTest.java</files>
  <action>
`ReserveQueryService`(ReserveRepository 주입): `getReserve(long settlementId)` → reserveRepo.findBySettlementId(settlementId).orElseThrow(() → new ReserveNotFoundException(settlementId)) 반환(도메인 Reserve).
`ReserveQueryController`(PayoutController.payout:31-35 shape 클론, @RequestMapping("/v1/settlements")): GET /{id}/reserve → service.getReserve → ReserveResponse.
DTO `ReserveResponse(Long id, String status, BigDecimal amount, LocalDate holdUntil, String transferRef)`(PayoutResponse 미러 + hold_until/transfer_ref).
`ReserveConfigIntegrationTest`(MerchantSettlementConfigIntegrationTest.java 하네스 클론, @BeforeEach DELETE merchant_reserve_config): PUT round-trip(값 반환·DB 왕복), 재 PUT overwrite, 미설정 GET→404, 검증 400 3종(rate 음수·rate>=1·scale>4) + cap<0·holdDays<0 → 400.
`ReserveQueryIntegrationTest`(동일 하네스 + @MockitoBean BankTransferPort, @BeforeEach DELETE reserve/merchant_reserve_config/payout/merchant_payout_account/settlement): reserve config 시드 + account PUT + FINALIZED settlement approve(POST /{id}/payout) → GET /{id}/reserve 200 + status HELD·amount·transfer_ref 'RSV-'+id; 유보 없는 settlement GET → 404 RESERVE_NOT_FOUND.
  </action>
  <verify>
    <automated>bash -lc 'cd /Users/juho/Documents/docswithmulti-reserve && find . -path "*/build/*" -name "* [0-9].sql" -delete; ./gradlew :settlement-service:test --tests "*ReserveConfigIntegrationTest" --tests "*ReserveQueryIntegrationTest"'</automated>
  </verify>
  <done>GET /{id}/reserve → 200(HELD 상태 바디) / 404(미존재). ReserveConfigIntegrationTest·ReserveQueryIntegrationTest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → reserve-config PUT | 위조/크로스-가맹점 정책 쓰기 — 런타임 인증 없음, 배포 시 NetworkPolicy 게이트웨이 ingress 제한(기존 SettlementConfigController 클래스) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | 잘못된 유보 정책 값(음수 rate/cap, rate≥1, scale 초과) | high | mitigate | ReserveConfigService 검증 → InvalidReserveConfigException 400 (ASVS V5); DB DECIMAL(5,4)/(19,2) 정밀도 |
| T-01-07 | Elevation/Tampering | 크로스-가맹점 정책 쓰기 | medium | transfer | 배포 시 NetworkPolicy 게이트웨이-only ingress(objective 결정, payout/product 동일 클래스) |
| T-01-08 | Information Disclosure | 타 가맹점 유보 상태 조회 | low | transfer | 동일 NetworkPolicy 경계; settlementId 로만 조회 |
| T-01-SC | Tampering | supply chain | low | accept | 신규 패키지 0 |
</threat_model>

<verification>
- ReserveConfigIntegrationTest: PUT/GET round-trip·overwrite·404·400(rate/cap/holdDays 위반).
- ReserveQueryIntegrationTest: GET reserve 200(HELD)·404.
- 신규 예외 3종 generic 핸들러로 400/404 매핑(GlobalExceptionHandler 무수정).
</verification>

<success_criteria>
- RCFG-01: PUT reserve-config upsert + 검증 400.
- RCFG-02: GET reserve-config 200/404.
- HOLD-04: GET /{id}/reserve 200/404.
</success_criteria>

<output>
Create `.planning/workstreams/payout-reserve/phases/01-hold/01-02-SUMMARY.md` when done.
</output>
