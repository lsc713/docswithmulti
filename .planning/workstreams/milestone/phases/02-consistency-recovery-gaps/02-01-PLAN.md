---
phase: 02-consistency-recovery-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - payment-service/src/main/java/com/example/payment/infrastructure/http/PgCancelHttpClient.java
  - payment-service/src/main/java/com/example/payment/infrastructure/http/RiskManagementHttpClient.java
  - payment-service/src/main/java/com/example/payment/application/dto/CheckChargeResponseDto.java
  - payment-service/src/main/java/com/example/payment/application/service/ProcessingRecoveryService.java
  - payment-service/src/test/java/com/example/payment/infrastructure/http/PgCancelHttpClientTest.java
  - payment-service/src/test/java/com/example/payment/infrastructure/http/RiskManagementHttpClientTest.java
  - payment-service/src/test/java/com/example/payment/application/service/ProcessingRecoveryServiceTest.java
autonomous: false
requirements: [RESIL-01]
user_setup:
  - service: payment-gateway (PG)
    why: "getStatus() 는 운영 PG 취소 상태조회 엔드포인트 계약(D-01)에 결합 — 실 PG 문서/스테이징 확인 없이는 ASSUMED 계약으로만 검증 가능"
    dashboard_config:
      - task: "PG 취소 상태조회 REST 계약 확인 (경로/응답 스키마/인증) 후 checkpoint 승인"
        location: "PG 벤더 문서 또는 스테이징 샌드박스"

must_haves:
  truths:
    - "PROCESSING 5분 초과 건이 getStatus 조회 결과에 따라 수동 개입 없이 수렴한다 (APPROVED→TX3 재실행 / FAILED retryable→재호출 / FAILED non-retryable→보상 / PENDING→유지)"
    - "getStatus()/isCharged() 가 호출 시 스텁 예외가 아니라 실제 PG/risk 응답을 PgCancelResult/boolean 으로 반환한다 (D-01 getStatus, D-05 isCharged)"
    - "getStatus 조회 실패(네트워크/비-2xx)는 PgServiceException 으로 전파되어 recoverOne 이 PROCESSING 을 유지하고 다음 주기에 재시도한다 (조회 실패 ≠ 무동작 회귀)"
  artifacts:
    - "PgCancelHttpClient.getStatus() 실제 구현 (기존 cancel() POST 미러링한 GET 어댑터)"
    - "RiskManagementHttpClient.isCharged() 배선 (기존 GET /internal/cancel-limit/check)"
    - "CheckChargeResponseDto record (payment-service application/dto)"
    - "PgCancelHttpClientTest / RiskManagementHttpClientTest (getStatus·isCharged 매핑 단위 테스트)"
  key_links:
    - "getStatus 응답 status/retryable 필드 → PgCancelResult.isApproved()/isFailed()/isRetryable() 매핑 → ProcessingRecoveryService.recoverOne 분기"
    - "isCharged(cancelRequestId) → pending-recovery 의 보상 여부 결정 (charged=true→compensate→FAILED / false→FAILED)"
  prohibitions:
    - "getStatus 예외를 삼키던 catch 가 진짜 조회 실패까지 조용히 삼켜 복구 무동작으로 회귀하지 않는다"
---

<objective>
RESIL-01: `PgCancelHttpClient.getStatus()` 와 `RiskManagementHttpClient.isCharged()` 의
스텁(`UnsupportedOperationException`)을 실제 구현으로 채워, PROCESSING 5분 초과 건이 PG
상태조회 → TX3 재실행/보상으로 **수동 개입 없이 수렴**하게 한다.

Purpose: 멀티파드/스케일아웃에서만 드러난 복구 스텁 결함 마감. stale PROCESSING 이 스케줄러로 자연 수렴해야 한다.
Output: getStatus/isCharged 실 구현 + CheckChargeResponseDto + 매핑 단위 테스트 + recoverOne 자동복구 회귀 테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-consistency-recovery-gaps/02-CONTEXT.md
@.planning/phases/02-consistency-recovery-gaps/02-RESEARCH.md
@.planning/phases/02-consistency-recovery-gaps/02-PATTERNS.md
@CLAUDE.md
@sysdesign/cancel-design.md
@docs/superpowers/specs/2026-04-28-scheduler-enhancement-design.md
</context>

## Artifacts this phase produces (Plan 01)

- `PgCancelHttpClient.getStatus(String paymentKey)` — 실제 HTTP GET 구현 (스텁 제거)
- `RiskManagementHttpClient.isCharged(long cancelRequestId)` — 실 엔드포인트 배선 (스텁 제거)
- `CheckChargeResponseDto(String cancelRequestId, boolean charged, Long merchantId, BigDecimal cancelAmount)` — 신규 record
- `PgCancelHttpClientTest` (신규), `RiskManagementHttpClientTest` (신규 또는 기존에 케이스 추가 — 존재 여부 실행 시 확인)
- `ProcessingRecoveryServiceTest` 신규 케이스 (APPROVED→TX3 재실행 / 조회 실패→PROCESSING 유지)
- 신규 Flyway 마이그레이션 없음 (스키마 변경 없음)

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: PG 취소 상태조회 REST 계약 승인 (D-01, ASSUMED)</name>
  <what-built>
    아직 구현 전. D-01(costly reversibility)는 운영 PG 가 취소 상태조회 엔드포인트를
    제공한다는 **전제**로 계약을 ASSUMED 설계한다. 근거 문서가 없어 자동 진행 금지 —
    실 PG 계약을 확정하거나 ASSUMED 계약 진행을 명시 승인해야 Task 2 로 넘어간다.
  </what-built>
  <how-to-verify>
    1. RESEARCH.md §"D-01: getStatus() 관례적 REST 계약" 과 §"Assumptions Log A1/A2", COVERAGE.md 를 검토.
    2. ASSUMED 계약: `GET {external.pg.url}/v1/payments/{paymentKey}/cancel/status`,
       응답 바디 `{ pgTransactionId, status(APPROVED|FAILED|PENDING), retryable }` → PgCancelResult 1:1 매핑.
    3. 실 PG 문서/스테이징 계약이 있으면 경로·스키마·인증을 이 계약과 대조.
    4. 계약이 맞으면 "approved (ASSUMED 진행)" 또는 정정된 경로/스키마를 회신.
  </how-to-verify>
  <resume-signal>"approved" (ASSUMED 계약 그대로 진행) 또는 정정된 엔드포인트/응답 스키마를 제시</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: getStatus() 실 구현 + stale PROCESSING → TX3 재실행 end-to-end 배선</name>
  <files>payment-service/src/main/java/com/example/payment/infrastructure/http/PgCancelHttpClient.java, payment-service/src/main/java/com/example/payment/application/service/ProcessingRecoveryService.java, payment-service/src/test/java/com/example/payment/infrastructure/http/PgCancelHttpClientTest.java, payment-service/src/test/java/com/example/payment/application/service/ProcessingRecoveryServiceTest.java</files>
  <reversibility rating="costly">getStatus 조회 계약에 recoverOne 이 결합 — PG 조회 미지원 판명 시 "멱등 재취소로 복구"로 재설계 필요 (Task 1 게이트로 방어).</reversibility>
  <read_first>
    - payment-service/.../infrastructure/http/PgCancelHttpClient.java (getStatus 스텁 + 같은 파일 cancel() lines 36-57 analog: circuitBreaker/restTemplate/baseUrl 필드 재사용)
    - payment-service/.../infrastructure/http/MockPgCancelClient.java (@Profile("local") getStatus 응답 형태 참조점)
    - payment-service/.../application/dto/PgCancelResult.java (status/retryable + approved/failed/retryableFailed/pending 팩토리 — 매핑 타깃)
    - payment-service/.../application/service/ProcessingRecoveryService.java (recoverOne lines 56-73 현재 catch 의미, retryPgCancel lines 94-96)
    - 02-RESEARCH.md §"D-01 getStatus" Code Examples + §"Common Pitfalls Pitfall 3" + §Architecture Patterns 복구 상태머신 다이어그램
  </read_first>
  <behavior>
    - getStatus(paymentKey): status=APPROVED 응답 → PgCancelResult.isApproved()==true
    - getStatus(paymentKey): status=FAILED & retryable=true 응답 → isFailed()==true, isRetryable()==true
    - getStatus(paymentKey): status=PENDING 응답 → isPending()==true
    - getStatus(paymentKey): 비-2xx 또는 body=null → PgServiceException
    - getStatus(paymentKey): RestTemplate 예외(네트워크) → PgServiceException (삼키지 않음)
    - recoverOne: PROCESSING 5분 초과 + getStatus=APPROVED → cancelTxWriter.saveTx3() 재실행 후 COMPLETED
    - recoverOne: getStatus 가 PgServiceException → PROCESSING 유지, 다음 주기 재시도 (무동작 회귀 아님)
  </behavior>
  <action>
    D-01 승인된 계약으로 `getStatus(String paymentKey)` 스텁을 실 구현으로 교체한다. 같은 파일 `cancel()`
    구조(생성자 주입된 `circuitBreaker`/`restTemplate`/`baseUrl` 필드 재사용, `circuitBreaker.executeCheckedSupplier`
    래핑, 2xx/null 체크 후 `PgServiceException`, `catch (PgServiceException) rethrow` + `catch (Throwable) → log.error(paymentKey) + PgServiceException`)를
    그대로 미러링하되 `restTemplate.getForEntity(url, PgCancelResult.class, paymentKey)` 와 URL
    `/v1/payments/{paymentKey}/cancel/status` 로 바꾼다. 신규 CircuitBreaker/RestTemplate 빈 생성 금지 — 기존
    `pgCancelCircuitBreaker` 공유. 그다음 `ProcessingRecoveryService.recoverOne` 의 catch 를 재검토: 스텁 시절
    getStatus 예외를 삼켜 무동작이던 방어를, 이제 진짜 조회 실패(PgServiceException)만 PROCESSING 유지로 걸러야
    한다 — recoverOne 이 getStatus 반환값으로 APPROVED→saveTx3 재실행(D-001 인라인 payment.cancelled 발행 경로 준수),
    FAILED retryable→retryPgCancel, FAILED non-retryable→compensate→FAILED, PENDING→유지 로 분기하는지 확인/보강한다.
    PgCancelHttpClientTest 는 Mockito 로 RestTemplate/CircuitBreaker 목업(프로젝트 JUnit5+Mockito 컨벤션),
    ProcessingRecoveryServiceTest 는 PgCancelPort 목으로 APPROVED→TX3 재실행 및 조회실패→PROCESSING 유지 케이스를 추가한다.
  </action>
  <verify>
    <automated>./gradlew :payment-service:test --tests "*PgCancelHttpClientTest" --tests "*ProcessingRecoveryServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - PgCancelHttpClientTest: getStatus 가 status=APPROVED 응답에 PgCancelResult.isApproved()==true, 비-2xx 응답에 PgServiceException 을 던진다 (behavior assertion)
    - ProcessingRecoveryServiceTest: PROCESSING 5분 초과 + getStatus=APPROVED 인 케이스가 cancelTxWriter.saveTx3() 를 호출하고 CancelRequest 가 COMPLETED 로 전이 (mock verify)
    - ProcessingRecoveryServiceTest: getStatus 가 PgServiceException 을 던지는 케이스에서 CancelRequest 가 PROCESSING 을 유지하고 saveTx3 미호출 (behavior assertion)
    - getStatus() 호출이 스텁 시절의 UnsupportedOperationException 대신 정상 PgCancelResult 를 반환 (behavior assertion)
    - `./gradlew :payment-service:test --tests "*PgCancelHttpClientTest" --tests "*ProcessingRecoveryServiceTest"` green
  </acceptance_criteria>
  <done>getStatus 실 구현 + recoverOne 자동복구 배선 완료, 위 test 커맨드 green, 커밋됨.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: isCharged() 배선 (D-05) + CheckChargeResponseDto + pending-recovery 보상 결정</name>
  <files>payment-service/src/main/java/com/example/payment/infrastructure/http/RiskManagementHttpClient.java, payment-service/src/main/java/com/example/payment/application/dto/CheckChargeResponseDto.java, payment-service/src/main/java/com/example/payment/application/service/ProcessingRecoveryService.java, payment-service/src/test/java/com/example/payment/infrastructure/http/RiskManagementHttpClientTest.java</files>
  <read_first>
    - payment-service/.../infrastructure/http/RiskManagementHttpClient.java (isCharged 스텁 + validateAndReserve() lines 36-61 analog, compensate())
    - risk-management-service/.../presentation/controller/InternalCancelLimitController.java + CheckChargeService.java + CheckChargeResponse.java (배선 대상 엔드포인트 계약: GET /internal/cancel-limit/check?cancelRequestId= → {cancelRequestId,charged,merchantId,cancelAmount})
    - payment-service/.../application/dto/RiskReserveResult.java (신규 DTO 를 옆에 둘 위치·record 스타일 참조)
    - 02-RESEARCH.md §"D-05 isCharged() Code Examples" + §Architecture Patterns pending-recovery 흐름
  </read_first>
  <behavior>
    - isCharged(cancelRequestId): risk 응답 charged=true → true 반환
    - isCharged(cancelRequestId): charged=false → false 반환
    - isCharged(cancelRequestId): 비-2xx/null → RiskServiceException
    - pending-recovery: PENDING 5분 초과 + isCharged=true → compensate → FAILED
    - pending-recovery: isCharged=false → FAILED (보상 불필요)
  </behavior>
  <action>
    `CheckChargeResponseDto(String cancelRequestId, boolean charged, Long merchantId, java.math.BigDecimal cancelAmount)`
    record 를 application/dto 에 신규 추가 (risk 의 CheckChargeResponse 필드와 동일 매핑). `isCharged(long cancelRequestId)`
    스텁을 `validateAndReserve()` 구조 미러링으로 교체: 기존 `riskManagementCircuitBreaker`/`restTemplate`/`baseUrl` 재사용,
    `GET /internal/cancel-limit/check?cancelRequestId={cancelRequestId}` 를 `restTemplate.getForEntity(...)` 로 호출,
    2xx/null 체크 후 `RiskServiceException`, body.charged() 반환. RiskManagementPort 인터페이스가 long 이므로 URL 변수는
    long 그대로 전달(risk 측 String 파라미터는 스프링이 변환). ProcessingRecoveryService 의 pending-recovery 경로가 isCharged
    반환으로 compensate 여부를 결정하는지 확인/보강. RiskManagementHttpClientTest 존재 여부를 먼저 확인 — 없으면 신규 생성,
    있으면 isCharged 케이스 추가. 예외 메시지에 식별자는 log.error 에만 남기고 클라이언트 응답 노출 금지 (기존 원칙).
  </action>
  <verify>
    <automated>./gradlew :payment-service:test --tests "*RiskManagementHttpClientTest" --tests "*ProcessingRecoveryServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - RiskManagementHttpClientTest: isCharged 가 charged=true 응답에 true, charged=false 응답에 false, 비-2xx 에 RiskServiceException 반환 (behavior assertion)
    - CheckChargeResponseDto record 존재하고 charged() 접근자로 risk 응답을 매핑
    - isCharged() 호출이 스텁 시절의 UnsupportedOperationException 대신 정상 boolean 을 반환 (behavior assertion)
    - `./gradlew :payment-service:test --tests "*RiskManagementHttpClientTest" --tests "*ProcessingRecoveryServiceTest"` green
  </acceptance_criteria>
  <done>isCharged 배선 + DTO + pending-recovery 보상 결정 완료, 위 test 커맨드 green, 커밋됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payment-service → 외부 PG (getStatus) | 신뢰 불가한 외부 HTTP 응답이 PgCancelResult 로 매핑되는 경계 |
| payment-service → risk-management (isCharged) | 내부 서비스 간 HTTP (`/internal/*`, 내부망 전용) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | PgCancelHttpClient.getStatus (외부 PG 응답→PgCancelResult) | high | mitigate | status 필드가 예상 enum(APPROVED/FAILED/PENDING) 밖이면 PgCancelResult 의 `"APPROVED".equals(status)` 패턴이 전부 false 반환(안전 기본값); 2xx/null 검증 후 PgServiceException |
| T-02-02 | Denial of Service | getStatus 무한 재시도 폭주 | high | mitigate | 기존 pgCancelCircuitBreaker(50%/10s OPEN) 공유 + MAX_PG_RETRIES=5 상한 유지 (신규 로직이 우회 금지) |
| T-02-03 | Information Disclosure | 예외 메시지에 paymentKey/HTTP 상태 노출 | medium | mitigate | 식별자는 log.error 에만, 클라이언트 응답은 GlobalExceptionHandler 가 INTERNAL_ERROR 로 일반화 |
| T-02-SC | Tampering | npm/pip/cargo/gradle installs | low | accept | 이 페이즈 신규 패키지 설치 없음 (RESEARCH Package Legitimacy Audit — 스킵) |
</threat_model>

<verification>
- `./gradlew :payment-service:test --tests "*PgCancelHttpClientTest" --tests "*RiskManagementHttpClientTest" --tests "*ProcessingRecoveryServiceTest"` green
- getStatus/isCharged 가 스텁 예외 없이 실 응답 매핑
- recoverOne 이 APPROVED→TX3 재실행, 조회실패→PROCESSING 유지로 분기
</verification>

<success_criteria>
PROCESSING 5분 초과 건이 getStatus 조회 결과에 따라 수동 개입 없이 수렴하고,
getStatus()/isCharged() 가 스텁 예외를 던지지 않는다 (ROADMAP Phase 2 Success Criteria 1).
</success_criteria>

<output>
Create `.planning/phases/02-consistency-recovery-gaps/02-01-SUMMARY.md` when done
</output>
