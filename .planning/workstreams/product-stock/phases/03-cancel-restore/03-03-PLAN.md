---
phase: 03-cancel-restore
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - product-service/src/main/resources/application.yml
  - product-service/src/main/java/com/example/product/infrastructure/messaging/RetryRouter.java
  - product-service/src/main/java/com/example/product/infrastructure/messaging/DlqMessage.java
  - product-service/src/main/java/com/example/product/application/exception/NonRetryableException.java
  - product-service/src/main/java/com/example/product/infrastructure/config/KafkaConsumerConfig.java
  - product-service/src/main/java/com/example/product/infrastructure/config/KafkaProducerConfig.java
  - product-service/src/main/java/com/example/product/infrastructure/messaging/PaymentCancelledStockConsumer.java
  - product-service/src/main/java/com/example/product/infrastructure/messaging/PaymentCancelledStockRetryConsumer.java
  - product-service/src/test/java/com/example/product/messaging/RetryRouterTest.java
autonomous: true
requirements: [RST-02]

must_haves:
  truths:
    - "일시적 오류(DB 타임아웃 등)는 retry-count < 3이면 payment.cancelled.retry 토픽으로 라우팅되고, 3회 이상 또는 데이터 오류(NonRetryableException)면 payment.cancelled.DLQ로 격리된다 (RST-02, D-P3-2)"
    - "consumer는 성공·retry·DLQ 이동 후에만 offset을 ack한다(수동 커밋, 메시지 유실 없음)"
  artifacts:
    - "product-service/src/main/java/com/example/product/infrastructure/messaging/RetryRouter.java (order 복제)"
    - "product-service/src/main/java/com/example/product/infrastructure/messaging/PaymentCancelledStockRetryConsumer.java"
    - "product-service/src/test/java/com/example/product/messaging/RetryRouterTest.java"
  key_links:
    - "PaymentCancelledStockConsumer catch(Exception) → RetryRouter.route → retry/DLQ 토픽 (KafkaTemplate producer)"
    - "retryKafkaListenerContainerFactory(retry-group) → PaymentCancelledStockRetryConsumer → 동일 처리 useCase"
---

<objective>
tracer(03-01) consumer에 order-service의 retry/DLQ 라우팅을 복제해 일시적 오류는 재시도 토픽으로, 데이터 오류·3회 초과는 DLQ로 격리한다. product에 Kafka producer(재시도/DLQ 발행) 인프라를 추가한다.

Purpose: consumer 실패가 재고 미복원으로 유실되지 않도록 at-least-once 재시도/격리 보장(RST-02 운영 견고성).
Output: RetryRouter·DlqMessage·NonRetryableException, retry consumer, producer/consumer config, 라우팅 단위테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/kafka-design.md

# 레퍼런스 (retry/DLQ 복제원)
@order-service/src/main/java/com/example/order/infrastructure/messaging/RetryRouter.java
@order-service/src/main/java/com/example/order/infrastructure/messaging/PaymentCancelledConsumer.java
@order-service/src/main/java/com/example/order/infrastructure/messaging/PaymentCancelledRetryConsumer.java
@order-service/src/main/java/com/example/order/infrastructure/config/KafkaConsumerConfig.java
@payment-service/build.gradle
</context>

<tasks>

<task type="auto">
  <name>Task 1: RetryRouter + producer/retry consumer config (RST-02)</name>
  <files>product-service/src/main/resources/application.yml, product-service/src/main/java/com/example/product/infrastructure/messaging/RetryRouter.java, product-service/src/main/java/com/example/product/infrastructure/messaging/DlqMessage.java, product-service/src/main/java/com/example/product/application/exception/NonRetryableException.java, product-service/src/main/java/com/example/product/infrastructure/config/KafkaConsumerConfig.java, product-service/src/main/java/com/example/product/infrastructure/config/KafkaProducerConfig.java</files>
  <read_first>order RetryRouter(MAX_RETRY_COUNT=3, retryDelay 1/5/10분, isDataError=NonRetryableException, retry 헤더 세팅, DlqMessage.of), order KafkaConsumerConfig(retryConsumerFactory: retry-group-id, MAX_POLL_RECORDS=50, read_uncommitted; retryKafkaListenerContainerFactory), order DlqMessage</read_first>
  <action>D-P3-2: order retry/DLQ 인프라를 product로 복제.
1) 의존성: spring-kafka는 03-01에서 이미 추가됨(producer는 spring-kafka에 포함). build.gradle 변경 없음.
2) application.yml: kafka.topic.payment-cancelled-retry=payment.cancelled.retry, kafka.topic.payment-cancelled-dlq=payment.cancelled.DLQ, spring.kafka.consumer.retry-group-id=product-service-retry, spring.kafka.producer(acks/StringSerializer) 추가(order/docs kafka-design 정렬).
3) KafkaProducerConfig: KafkaTemplate&lt;String,String&gt; 프로듀서 팩토리(order producer 설정 또는 payment 프로듀서 설정 참조 — StringSerializer, acks=all, enable.idempotence).
4) NonRetryableException(application/exception): 데이터 오류 표식(order 복제).
5) DlqMessage: order 복제(of(record, retryCount, e), truncate).
6) RetryRouter: order 로직 복제(route, publishToRetry/Dlq)하되 `@Component` + 생성자 @Value("${kafka.topic.payment-cancelled-retry}")·@Value("${kafka.topic.payment-cancelled-dlq}")로 토픽 주입(자기배선 — 별도 bean config 없음).
7) KafkaConsumerConfig: retryConsumerFactory(retry-group-id, MAX_POLL_RECORDS=50, read_uncommitted) + retryKafkaListenerContainerFactory(MANUAL_IMMEDIATE) 추가(order 복제).</action>
  <verify>
    <automated>./gradlew :product-service:compileJava :product-service:compileTestJava</automated>
  </verify>
  <done>RetryRouter/DlqMessage/NonRetryableException/producer/retry config 컴파일 그린. application.yml에 retry/dlq 토픽·retry-group 등록.</done>
</task>

<task type="auto">
  <name>Task 2: consumer 오류 라우팅 + retry consumer + 라우팅 테스트 (RST-02)</name>
  <files>product-service/src/main/java/com/example/product/infrastructure/messaging/PaymentCancelledStockConsumer.java, product-service/src/main/java/com/example/product/infrastructure/messaging/PaymentCancelledStockRetryConsumer.java, product-service/src/test/java/com/example/product/messaging/RetryRouterTest.java</files>
  <read_first>order PaymentCancelledConsumer(catch DataIntegrityViolationException → 멱등 ack; catch Exception → retryRouter.route(record, e) → ack), order PaymentCancelledRetryConsumer(retryKafkaListenerContainerFactory, 동일 useCase)</read_first>
  <action>D-P3-2: 03-01의 PaymentCancelledStockConsumer에 order 오류 라우팅을 추가.
1) PaymentCancelledStockConsumer: RetryRouter 주입. try{처리→ack} catch(DataIntegrityViolationException){멱등 ack}(이미 03-01에 있으면 유지) catch(Exception e){retryRouter.route(record, e); ack;}. offset ack는 항상 마지막(성공/retry/DLQ 이동 후).
2) PaymentCancelledStockRetryConsumer: `@Component` + @KafkaListener(topics=${kafka.topic.payment-cancelled-retry}, groupId=${spring.kafka.consumer.retry-group-id}, containerFactory=retryKafkaListenerContainerFactory) — 동일 useCase 실행, 재실패 시 RetryRouter.route(order 패턴). RetryRouter는 @Component(Task1)이므로 생성자 주입.
3) RetryRouterTest(단위, 브로커 불필요 — KafkaTemplate mock): (a) 일시적 오류 + retry-count 없음 → retry 토픽 발행(retry-count=1 헤더). (b) NonRetryableException → DLQ 발행. (c) retry-count>=3 → DLQ 발행. order RetryRouter 테스트 관행 정렬.</action>
  <verify>
    <automated>./gradlew :product-service:test --tests "com.example.product.messaging.RetryRouterTest"</automated>
  </verify>
  <done>일시적 오류 → retry 토픽, 데이터오류/3회초과 → DLQ 라우팅이 단위테스트로 그린. consumer가 실패 시 route 후 ack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka consumer 처리 실패 → retry/DLQ | 실패 메시지가 유실되거나 무한 재처리되지 않아야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Denial of Service (무한 재처리) | PaymentCancelledStockConsumer/RetryRouter | medium | mitigate | retry-count 3회 상한 → DLQ 격리. 데이터오류(NonRetryableException) 즉시 DLQ. |
| T-03-06 | Repudiation (유실) | 수동 offset 커밋 | medium | mitigate | 성공/retry/DLQ 이동 후에만 ack(MANUAL_IMMEDIATE) — order 패턴 동형. |
</threat_model>

<verification>
- `./gradlew :product-service:test` — RetryRouterTest + 기존 테스트 무회귀 그린.
</verification>

<success_criteria>
- 일시적 오류는 재시도, 데이터오류·3회 초과는 DLQ 격리(RST-02).
- consumer 수동 커밋으로 메시지 유실 없음.
</success_criteria>

<output>
Create `.planning/workstreams/product-stock/phases/03-cancel-restore/03-03-SUMMARY.md` when done
</output>
