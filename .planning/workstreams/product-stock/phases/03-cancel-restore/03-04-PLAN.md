---
phase: 03-cancel-restore
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - payment-service/src/main/java/com/example/payment/presentation/controller/PaymentController.java
  - payment-service/src/main/java/com/example/payment/presentation/dto/PaymentExistsResponse.java
  - payment-service/src/main/java/com/example/payment/application/usecase/PaymentExistsQuery.java
  - payment-service/src/main/java/com/example/payment/application/service/PaymentExistsQueryService.java
  - payment-service/src/main/java/com/example/payment/application/interfaces/PaymentRepository.java
  - payment-service/src/main/java/com/example/payment/infrastructure/persistence/PaymentRepositoryImpl.java
  - payment-service/src/test/java/com/example/payment/integration/PaymentExistsEndpointIntegrationTest.java
autonomous: true
requirements: [RST-03]

must_haves:
  truths:
    - "GET /v1/payments/{paymentKey}/exists 는 커밋된 payment가 있으면 {exists:true}, 없으면 {exists:false}를 반환한다 (RST-03, D-P3-3)"
    - "신규 조회 엔드포인트 추가가 취소 코어 로직을 변경하지 않는다 — 기존 취소/결제 테스트 무회귀 그린 (D-P3-5)"
  artifacts:
    - "payment-service/src/main/java/com/example/payment/presentation/controller/PaymentController.java (GET exists 엔드포인트 추가)"
    - "payment-service/src/test/java/com/example/payment/integration/PaymentExistsEndpointIntegrationTest.java"
  key_links:
    - "PaymentController GET exists → PaymentExistsQueryService → PaymentRepository.existsByPaymentKey"
    - "이 엔드포인트 ↔ product orphan 스케줄러(03-05)의 'paymentKey 커밋됨?' 조회 대상"
---

<objective>
orphan 예약 복구(03-05)가 조회할 payment-service 경량 존재확인 read 엔드포인트를 신설한다: GET /v1/payments/{paymentKey}/exists. 취소 코어(멱등·TX·스케줄러·outbox)는 무변경 — 순수 read 경로 추가.

Purpose: reserve 성공 후 결제 미생성된 orphan 예약을 product가 판별할 수 있는 권위 조회 API 제공(RST-03 전제).
Output: exists 엔드포인트 + 쿼리 서비스 + PaymentRepository.existsByPaymentKey + 통합테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/api-spec.md
@docs/superpowers/specs/2026-07-30-sku-stock-lifecycle-design.md

# 레퍼런스
@payment-service/src/main/java/com/example/payment/presentation/controller/PaymentController.java
@payment-service/src/main/java/com/example/payment/application/interfaces/PaymentRepository.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: existsByPaymentKey 조회 + exists 엔드포인트 (RST-03)</name>
  <files>payment-service/src/main/java/com/example/payment/application/interfaces/PaymentRepository.java, payment-service/src/main/java/com/example/payment/infrastructure/persistence/PaymentRepositoryImpl.java, payment-service/src/main/java/com/example/payment/application/usecase/PaymentExistsQuery.java, payment-service/src/main/java/com/example/payment/application/service/PaymentExistsQueryService.java, payment-service/src/main/java/com/example/payment/presentation/dto/PaymentExistsResponse.java, payment-service/src/main/java/com/example/payment/presentation/controller/PaymentController.java</files>
  <read_first>PaymentController(현재 POST create만; @RequestMapping("/v1/payments")); PaymentRepository(findByPaymentKey 존재 — existsByPaymentKey는 신규); PaymentRepositoryImpl의 JPA 조회 패턴</read_first>
  <reversibility rating="costly">exists 엔드포인트는 product orphan 스케줄러가 의존하는 조회 계약. read-only·하위호환이나 경로/응답 형태는 계약.</reversibility>
  <action>D-P3-3/D-P3-5: 순수 read 조회 경로만 추가(취소 코어·결제 생성 로직 무변경).
1) PaymentRepository: boolean existsByPaymentKey(String paymentKey) 추가. PaymentRepositoryImpl: JpaRepository의 existsBy 파생쿼리 또는 findByPaymentKey().isPresent()로 구현(가벼운 조회, count/exists 선호).
2) PaymentExistsQuery(usecase 인터페이스): boolean exists(String paymentKey). PaymentExistsQueryService(@Service): repository.existsByPaymentKey 위임.
3) PaymentExistsResponse(record): boolean exists.
4) PaymentController: @GetMapping("/{paymentKey}/exists") → PaymentExistsResponse(exists). 항상 200(존재=true/false 바디). 결제 생성·취소 메서드는 손대지 않음.</action>
  <verify>
    <automated>./gradlew :payment-service:test --tests "com.example.payment.integration.PaymentExistsEndpointIntegrationTest"</automated>
  </verify>
  <done>존재하는 paymentKey→{exists:true}, 미존재→{exists:false} 200 반환. 통합테스트 그린.</done>
</task>

<task type="auto">
  <name>Task 2: exists 엔드포인트 통합테스트 + 취소 코어 무회귀 (RST-03)</name>
  <files>payment-service/src/test/java/com/example/payment/integration/PaymentExistsEndpointIntegrationTest.java</files>
  <read_first>CreatePaymentReserveIntegrationTest 또는 기존 payment 통합테스트의 Testcontainers MySQL + MockMvc 조립 패턴</read_first>
  <action>PaymentExistsEndpointIntegrationTest(Testcontainers MySQL): payment 1건 저장(또는 create 경로) 후 GET /v1/payments/{paymentKey}/exists → exists:true; 임의의 미존재 paymentKey → exists:false. 결제/취소 코어를 새로 만들지 말고 기존 조립 재사용.</action>
  <verify>
    <automated>./gradlew :payment-service:test --tests "com.example.payment.integration.PaymentExistsEndpointIntegrationTest" --tests "com.example.payment.integration.CancelFlowIntegrationTest"</automated>
  </verify>
  <done>exists 엔드포인트 통합테스트 그린 + 기존 취소 통합테스트 무회귀(취소 코어 불변).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| product orphan 스케줄러 → GET /v1/payments/{paymentKey}/exists | 내부 서비스 간 조회. paymentKey 존재 여부 노출. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Information Disclosure | GET exists 엔드포인트 | low | accept | boolean만 반환(금액·PII 없음). 내부망 서비스 간 호출 전용(product↔payment). |
| T-03-08 | Tampering (취소 코어) | PaymentController 변경 | high | mitigate | 신규 GET 메서드만 추가 — create/취소 메서드 무변경. 기존 취소 통합테스트 무회귀로 확인. |
</threat_model>

<verification>
- `./gradlew :payment-service:test` — exists 통합테스트 + 취소 코어 무회귀 그린.
- git diff가 신규 read 경로에만 국한(취소/생성 로직 무변경).
</verification>

<success_criteria>
- GET /v1/payments/{paymentKey}/exists 가 커밋 payment 존재 여부를 반환(RST-03).
- 취소 코어 로직 불변(기존 테스트 무회귀).
</success_criteria>

<output>
Create `.planning/workstreams/product-stock/phases/03-cancel-restore/03-04-SUMMARY.md` when done
</output>
