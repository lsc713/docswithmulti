---
phase: 03-cancel-restore
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-03", "03-04"]  # 03-04: exists 엔드포인트(RST-03). 03-01/03-03: product build.gradle·application.yml 쓰기순서(파일공유 직렬화 — wave 계산과 무관하게 견고)
files_modified:
  - product-service/build.gradle
  - product-service/src/main/resources/application.yml
  - product-service/src/main/java/com/example/product/infrastructure/config/RedissonConfig.java
  - product-service/src/main/java/com/example/product/application/interfaces/PaymentQueryPort.java
  - product-service/src/main/java/com/example/product/infrastructure/http/PaymentQueryHttpClient.java
  - product-service/src/main/java/com/example/product/infrastructure/config/HttpClientConfig.java
  - product-service/src/main/java/com/example/product/application/interfaces/StockReservationRepository.java
  - product-service/src/main/java/com/example/product/infrastructure/persistence/StockReservationRepositoryImpl.java
  - product-service/src/main/java/com/example/product/infrastructure/persistence/StockReservationJpaRepository.java
  - product-service/src/main/java/com/example/product/application/service/OrphanReservationRecoveryService.java
  - product-service/src/main/java/com/example/product/infrastructure/scheduler/OrphanReservationRecoveryScheduler.java
  - product-service/src/test/java/com/example/product/integration/OrphanReservationRecoveryIntegrationTest.java
autonomous: true
requirements: [RST-03]

must_haves:
  truths:
    - "오래된(5분+) RESERVED 예약을 payment-service에 조회해 커밋된 payment가 없으면 release되고, 있으면 RESERVED로 유지된다 (RST-03, D-P3-3)"
    - "orphan 복구는 Redis 분산락으로 단일 인스턴스만 실행하며 release는 멱등(Phase 1 원자 전이)이라 재실행이 재고를 오염시키지 않는다 (D-P3-3, D-P3-4)"
  artifacts:
    - "product-service/src/main/java/com/example/product/application/service/OrphanReservationRecoveryService.java"
    - "product-service/src/main/java/com/example/product/infrastructure/scheduler/OrphanReservationRecoveryScheduler.java (Redisson 분산락, ProcessingRecoveryScheduler 패턴)"
    - "product-service/src/main/java/com/example/product/infrastructure/http/PaymentQueryHttpClient.java"
    - "product-service/src/test/java/com/example/product/integration/OrphanReservationRecoveryIntegrationTest.java"
  key_links:
    - "OrphanReservationRecoveryService → PaymentQueryPort.exists(paymentKey) → payment GET /v1/payments/{paymentKey}/exists (03-04)"
    - "StockReservationRepository.findStaleReserved(threshold) → idx_reservation_status_created 활용 → StockService.release"
---

<objective>
reserve 성공 후 결제가 커밋되지 않아 유실된 orphan 예약을 복구하는 스케줄러를 product-service에 추가한다. 오래된 RESERVED를 스캔해 payment-service exists 조회(03-04)로 커밋 여부를 확인하고, 없으면 release한다. payment의 processing-recovery(PG 조회) 패턴과 동형, Redis 분산락.

Purpose: reserve 후 생성 실패 보상(Phase 2)마저 유실된 경우의 backstop — 재고가 영구 예약에 묶이지 않도록 정리(RST-03).
Output: Redisson + payment 조회 클라이언트 + orphan 스캔 리포지토리 + 복구 서비스/스케줄러 + 통합테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/superpowers/specs/2026-07-30-sku-stock-lifecycle-design.md
@CLAUDE.md

# 레퍼런스 (스케줄러/조회 패턴)
@payment-service/src/main/java/com/example/payment/infrastructure/scheduler/ProcessingRecoveryScheduler.java
@payment-service/src/main/java/com/example/payment/application/service/ProcessingRecoveryService.java
@payment-service/src/main/java/com/example/payment/infrastructure/http/ProductStockHttpClient.java
@product-service/src/main/java/com/example/product/application/service/StockService.java
@product-service/src/main/java/com/example/product/application/interfaces/StockReservationRepository.java
@product-service/src/main/resources/db/migration/V1__create_product_core.sql
@payment-service/build.gradle
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redisson + payment 조회 클라이언트 + orphan 스캔 쿼리 (RST-03)</name>
  <files>product-service/build.gradle, product-service/src/main/resources/application.yml, product-service/src/main/java/com/example/product/infrastructure/config/RedissonConfig.java, product-service/src/main/java/com/example/product/application/interfaces/PaymentQueryPort.java, product-service/src/main/java/com/example/product/infrastructure/http/PaymentQueryHttpClient.java, product-service/src/main/java/com/example/product/infrastructure/config/HttpClientConfig.java, product-service/src/main/java/com/example/product/application/interfaces/StockReservationRepository.java, product-service/src/main/java/com/example/product/infrastructure/persistence/StockReservationJpaRepository.java, product-service/src/main/java/com/example/product/infrastructure/persistence/StockReservationRepositoryImpl.java</files>
  <read_first>payment build.gradle(redisson-spring-boot-starter:4.3.1 좌표); ProductStockHttpClient(RestTemplate 조립 참조 — 단, orphan 조회는 CircuitBreaker 불요); V1 stock_reservation(idx_reservation_status_created, payment_key, sku_id, qty, status); StockReservationRepository 기존 시그니처</read_first>
  <reversibility rating="reversible">스캔 쿼리·조회 클라이언트는 내부 구현. 계약 노출 없음.</reversibility>
  <action>D-P3-3: orphan 복구의 의존 인프라 구축.
1) build.gradle: `implementation 'org.redisson:redisson-spring-boot-starter:4.3.1'`(payment 좌표 정렬) 추가. RestTemplate은 spring-boot-starter-web(base)로 충분 — 신규 없음. (ponytail: product 단일 인스턴스면 분산락 불요하나 D-P3-3이 processing-recovery 패턴/Redis락을 명시 — 검증된 패턴 미러 + 수평확장 대비로 채택. 상한: Redis 의존 추가.)
2) application.yml: spring.data.redis(host/port, payment 설정 정렬) + external.payment-service.url + scheduler.lock.orphan-recovery=lock:orphan-reservation-recovery + orphan.threshold-minutes=5 추가.
3) RedissonConfig: redisson-spring-boot-starter 자동설정으로 충분하면 별도 빈 불요 — payment RedissonConfig 없이 starter만으로 동작하면 이 파일 생략 가능(파일 생성 대신 application.yml redisson 설정만). 자동설정으로 RedissonClient 빈이 뜨는지 확인.
4) PaymentQueryPort(application/interfaces): boolean exists(String paymentKey).
5) HttpClientConfig: RestTemplate 빈(product에 없으면 신설).
6) PaymentQueryHttpClient(infrastructure/http): RestTemplate로 GET {external.payment-service.url}/v1/payments/{paymentKey}/exists 호출 → 응답 exists 반환. CircuitBreaker 없음(best-effort 배경 스케줄러 — 조회 실패는 예외 던져 그 건 skip, 다음 주기 재시도. fail-safe: 조회 실패 시 release 금지). 예외 시 조회 불가로 취급(false 반환 금지 — release 오작동 방지).
7) StockReservationRepository: List&lt;StockReservation&gt; findStaleReserved(Instant threshold) 추가. JpaRepository/Impl: status='RESERVED' AND created_at &lt; threshold 조회(idx_reservation_status_created 활용, LIMIT 배치).</action>
  <verify>
    <automated>./gradlew :product-service:compileJava :product-service:compileTestJava</automated>
  </verify>
  <done>Redisson·PaymentQueryHttpClient·findStaleReserved 컴파일 그린. application.yml에 redis·payment url·락키·threshold 등록.</done>
</task>

<task type="auto">
  <name>Task 2: orphan 복구 서비스 + 스케줄러 (RST-03)</name>
  <files>product-service/src/main/java/com/example/product/application/service/OrphanReservationRecoveryService.java, product-service/src/main/java/com/example/product/infrastructure/scheduler/OrphanReservationRecoveryScheduler.java</files>
  <read_first>ProcessingRecoveryScheduler(@Scheduled(fixedDelay=60_000), Redisson tryLock(0,55,SECONDS), isHeldByCurrentThread finally unlock); ProcessingRecoveryService.recoverAll/recoverOne 구조; StockService.release</read_first>
  <action>D-P3-3/D-P3-4: payment processing-recovery 패턴 동형 복구.
1) OrphanReservationRecoveryService.recoverAll(): findStaleReserved(now - threshold-minutes) 스캔 → paymentKey별 그룹 → 각 paymentKey마다 paymentQueryPort.exists 조회. exists=false(커밋 payment 없음)면 stockService.release(paymentKey, 해당 예약 items)로 정리. exists=true면 유지(skip). 조회 예외는 그 paymentKey skip + warn 로그(다음 주기 재시도) — release 오작동 방지(fail-safe). release는 멱등(Phase 1 W2)이라 재실행 안전.
2) OrphanReservationRecoveryScheduler: @Scheduled(fixedDelay=60_000) + Redisson 분산락(ProcessingRecoveryScheduler 복제: tryLock(0,55,SECONDS), 실패 시 skip, finally isHeldByCurrentThread unlock) → recoverAll 호출. @Value로 scheduler.lock.orphan-recovery 주입. product ProductServiceApplication에 @EnableScheduling 없으면 추가.</action>
  <verify>
    <automated>./gradlew :product-service:test --tests "com.example.product.integration.OrphanReservationRecoveryIntegrationTest"</automated>
  </verify>
  <done>스케줄러가 락 하에 recoverAll 실행. orphan(exists=false)만 release, 커밋된(exists=true) 예약은 유지.</done>
</task>

<task type="auto">
  <name>Task 3: orphan 복구 통합테스트 (RST-03)</name>
  <files>product-service/src/test/java/com/example/product/integration/OrphanReservationRecoveryIntegrationTest.java</files>
  <read_first>StockTracerIntegrationTest(Testcontainers MySQL 조립); StockService.reserve로 RESERVED 예약 생성</read_first>
  <action>OrphanReservationRecoveryIntegrationTest(Testcontainers MySQL, PaymentQueryPort는 mock/stub): (a) orphan — 오래된 RESERVED 예약(created_at 과거로 설정) + PaymentQueryPort.exists→false → recoverAll 후 available_qty 복원 + status=RELEASED. (b) 정상 — exists→true → RESERVED 유지, 재고 불변. (c) 최근(threshold 미만) 예약은 exists=false여도 스캔 대상 아님(미복원). (d) 조회 예외 → 해당 건 skip(release 안 함). Redisson·실 payment HTTP는 불필요 — 서비스 레벨로 PaymentQueryPort를 스텁하고 recoverAll 직접 호출.</action>
  <verify>
    <automated>./gradlew :product-service:test --tests "com.example.product.integration.OrphanReservationRecoveryIntegrationTest"</automated>
  </verify>
  <done>orphan release·정상 유지·threshold 경계·조회실패 skip 4케이스 그린.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orphan 스케줄러 → payment exists 조회 | 조회 결과에 따라 재고 release — 오판 시 premature release(재고 조기 복원) 위험 |
| 다중 product 인스턴스 → 스케줄러 | 동시 실행 시 이중 release 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering (premature release) | OrphanReservationRecoveryService | high | mitigate | threshold(5분) + payment 권위 조회 exists=false일 때만 release. 조회 실패는 skip(release 금지, fail-safe). |
| T-03-10 | Elevation (이중 release) | OrphanReservationRecoveryScheduler | medium | mitigate | Redisson 분산락(단일 실행) + StockService.release 멱등 원자 전이(over-release 불가, 이중 안전). |
| T-03-SC | Tampering | build.gradle redisson install | low | accept | 신규 패키지 아님 — payment-service가 이미 사용 중인 redisson-spring-boot-starter:4.3.1. 버전 정렬. |
</threat_model>

<verification>
- `./gradlew :product-service:test` — orphan 통합테스트 + 기존 테스트 무회귀 그린.
- 조회 실패 시 release 안 함(fail-safe) 검증 포함.
</verification>

<success_criteria>
- orphan 예약(exists=false)이 복구 스케줄러로 release, 커밋된 예약은 유지(RST-03).
- Redis 분산락 + 멱등 release로 이중 실행/over-release 없음.
</success_criteria>

<output>
Create `.planning/workstreams/product-stock/phases/03-cancel-restore/03-05-SUMMARY.md` when done
</output>
