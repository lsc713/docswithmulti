---
phase: 03-cancel-restore
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - product-service/src/main/resources/db/migration/V2__create_processed_cancel_event.sql
  - product-service/src/main/java/com/example/product/application/interfaces/ProcessedCancelEventRepository.java
  - product-service/src/main/java/com/example/product/infrastructure/persistence/ProcessedCancelEventJpaEntity.java
  - product-service/src/main/java/com/example/product/infrastructure/persistence/ProcessedCancelEventJpaRepository.java
  - product-service/src/main/java/com/example/product/infrastructure/persistence/ProcessedCancelEventRepositoryImpl.java
  - product-service/src/main/java/com/example/product/application/service/ProcessCancelledStockService.java
  - product-service/src/test/java/com/example/product/integration/CancelRestoreIdempotencyIntegrationTest.java
autonomous: true
requirements: [RST-02]

must_haves:
  truths:
    - "중복 payment.cancelled 이벤트는 cancelRequestId 멱등으로 no-op 처리된다 — 두 번째 동일 이벤트가 available_qty를 추가 복원하지 않는다 (RST-02, D-P3-2, D-P3-4)"
    - "부분취소 시 취소 이벤트의 cancelledItems에 실린 SKU만 복원되고 나머지 예약은 RESERVED로 유지된다 (RST-02, D-P3-2)"
  artifacts:
    - "product-service/src/main/resources/db/migration/V2__create_processed_cancel_event.sql (order 스키마 복제, cancel_request_id UK)"
    - "product-service/src/main/java/com/example/product/application/service/ProcessCancelledStockService.java (멱등 게이트 추가)"
    - "product-service/src/test/java/com/example/product/integration/CancelRestoreIdempotencyIntegrationTest.java"
  key_links:
    - "ProcessCancelledStockService.execute → TransactionTemplate: existsByCancelRequestId 게이트 → release → processed_cancel_event save (order ProcessCancelledItemsService 패턴)"
    - "uk_processed_cancel_event_cancel_request_id ↔ 중복 이벤트 no-op"
---

<objective>
tracer(03-01)의 release-on-cancel happy path를 멱등·부분취소 하드닝으로 확장한다. product에 processed_cancel_event 테이블(Flyway V2)을 신설하고 ProcessCancelledStockService에 cancelRequestId 멱등 게이트를 넣어 중복 이벤트를 no-op 처리한다. 부분취소(취소된 items만 복원)를 검증한다.

Purpose: at-least-once Kafka에서 중복 수신·부분취소가 재고를 오염시키지 않도록 보장(RST-02 완성).
Output: V2 마이그레이션 + processed_cancel_event 리포지토리 + 멱등 게이트 + 멱등/부분취소 통합테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/kafka-design.md
@docs/superpowers/specs/2026-07-30-sku-stock-lifecycle-design.md

# 레퍼런스 (멱등 패턴 복제원)
@order-service/src/main/resources/db/migration/V1__create_order_core.sql
@order-service/src/main/java/com/example/order/application/service/ProcessCancelledItemsService.java
@order-service/src/main/java/com/example/order/infrastructure/persistence/ProcessedCancelEventJpaEntity.java
@order-service/src/main/java/com/example/order/application/interfaces/ProcessedCancelEventRepository.java
@product-service/src/main/java/com/example/product/application/service/StockService.java
@product-service/src/main/java/com/example/product/application/service/ProcessCancelledStockService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: processed_cancel_event V2 마이그레이션 + 리포지토리 (RST-02 멱등)</name>
  <files>product-service/src/main/resources/db/migration/V2__create_processed_cancel_event.sql, product-service/src/main/java/com/example/product/application/interfaces/ProcessedCancelEventRepository.java, product-service/src/main/java/com/example/product/infrastructure/persistence/ProcessedCancelEventJpaEntity.java, product-service/src/main/java/com/example/product/infrastructure/persistence/ProcessedCancelEventJpaRepository.java, product-service/src/main/java/com/example/product/infrastructure/persistence/ProcessedCancelEventRepositoryImpl.java</files>
  <read_first>order V1__create_order_core.sql의 processed_cancel_event DDL(id BIGINT AI PK, cancel_request_id VARCHAR(64) NOT NULL, processed_at DATETIME(3), uk_processed_cancel_event_cancel_request_id); order ProcessedCancelEventJpaEntity/Repository/Impl</read_first>
  <reversibility rating="costly">processed_cancel_event 스키마는 멱등 계약. 적용 후 불변(Flyway 규약) — 변경은 새 버전으로만.</reversibility>
  <action>D-P3-2/D-P3-4: order-service processed_cancel_event 스키마·리포지토리를 product로 복제.
1) V2__create_processed_cancel_event.sql: order V1의 processed_cancel_event 테이블 DDL 그대로(id/cancel_request_id VARCHAR(64)/processed_at DATETIME(3)/uk_processed_cancel_event_cancel_request_id). product는 spring.jpa.hibernate.ddl-auto=validate이므로 컬럼/제약 정확히 일치 필요.
2) ProcessedCancelEventRepository(application/interfaces): boolean existsByCancelRequestId(String) + void save(String cancelRequestId) (order 인터페이스 시그니처 정렬).
3) JpaEntity/JpaRepository/RepositoryImpl: order 복제(@Table processed_cancel_event, UK, of(cancelRequestId) 팩토리).</action>
  <verify>
    <automated>./gradlew :product-service:flywayInfo && ./gradlew :product-service:compileJava</automated>
  </verify>
  <done>V2 마이그레이션이 flywayInfo에 나타나고 컴파일 그린. processed_cancel_event 스키마가 order와 동형(cancel_request_id UK).</done>
</task>

<task type="auto">
  <name>Task 2: 멱등 게이트 + 부분취소 통합테스트 (RST-02)</name>
  <files>product-service/src/main/java/com/example/product/application/service/ProcessCancelledStockService.java, product-service/src/test/java/com/example/product/integration/CancelRestoreIdempotencyIntegrationTest.java</files>
  <read_first>order ProcessCancelledItemsService.execute(TransactionTemplate: existsByCancelRequestId 게이트 → 처리 → processedCancelEventRepository.save); StockService.release(멱등 원자 전이 W2, 이미 RELEASED는 no-op)</read_first>
  <action>D-P3-2/D-P3-4: ProcessCancelledStockService에 order와 동형의 멱등 게이트를 넣는다.
1) execute를 TransactionTemplate.execute로 감싸: existsByCancelRequestId(cancelRequestId)면 즉시 return null(no-op) → StockService.release(paymentKey, items) → processedCancelEventRepository.save(cancelRequestId). ProcessedCancelEventRepository·TransactionTemplate을 @Service 생성자에 추가 주입(자기배선 — 별도 bean config 없음). 부분취소는 이미 items가 cancelledItems만 담고 있으므로 release가 그 SKU만 처리 — 별도 로직 불필요(검증만).
2) 참고: StockService.release 자체도 멱등(W2)이라 이중 안전. processed_cancel_event 멱등은 "release 재호출 자체를 건너뛰는" 상위 게이트(order 패턴 동형).
3) CancelRestoreIdempotencyIntegrationTest(Testcontainers MySQL): (a) 멱등 — 같은 cancelRequestId 이벤트를 서비스에 2회 실행 시 available_qty가 1회분만 복원(2회차 no-op, processed_cancel_event 1행). (b) 부분취소 — 2개 SKU 예약 후 1개만 cancelledItems에 담아 실행 시 해당 SKU만 available_qty 복원, 나머지 stock_reservation은 RESERVED 유지. Consumer/Kafka 없이 서비스 레벨로 직접 검증(브로커 불필요, 03-01 tracer가 배선 증명).</action>
  <verify>
    <automated>./gradlew :product-service:test --tests "com.example.product.integration.CancelRestoreIdempotencyIntegrationTest"</automated>
  </verify>
  <done>중복 이벤트 no-op(추가 복원 없음, processed_cancel_event 1행) + 부분취소(취소된 SKU만 복원, 나머지 RESERVED) 그린.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka payment.cancelled (at-least-once) → product | 중복/재전송 이벤트가 재고를 과다 복원할 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Elevation of Privilege (over-release) | ProcessCancelledStockService | high | mitigate | cancelRequestId processed_cancel_event UK 멱등 게이트(중복 no-op) + StockService.release 원자 조건부 전이(over-release 불가, 이중 안전). |
| T-03-04 | Tampering (부분취소 오복원) | release 대상 산정 | medium | mitigate | cancelledItems에 실린 SKU만 release — 부분취소 통합테스트로 나머지 RESERVED 유지 검증. |
</threat_model>

<verification>
- `./gradlew :product-service:test` — 멱등/부분취소 통합테스트 + 기존 재고 테스트 무회귀 그린.
- `./gradlew :product-service:flywayInfo` — V2 마이그레이션 등록.
</verification>

<success_criteria>
- 중복 payment.cancelled가 cancelRequestId 멱등으로 no-op(RST-02).
- 부분취소 시 취소된 SKU만 복원(RST-02).
</success_criteria>

<output>
Create `.planning/workstreams/product-stock/phases/03-cancel-restore/03-02-SUMMARY.md` when done
</output>
