---
phase: 03-cancel-restore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - payment-service/src/main/java/com/example/payment/application/service/CancelTxWriter.java
  - product-service/build.gradle
  - product-service/src/main/resources/application.yml
  - product-service/src/main/java/com/example/product/infrastructure/config/KafkaConsumerConfig.java
  - product-service/src/main/java/com/example/product/infrastructure/messaging/PaymentCancelledStockConsumer.java
  - product-service/src/main/java/com/example/product/infrastructure/messaging/PaymentCancelledPayload.java
  - product-service/src/main/java/com/example/product/application/usecase/ProcessCancelledStockUseCase.java
  - product-service/src/main/java/com/example/product/application/service/ProcessCancelledStockService.java
  - product-service/src/test/java/com/example/product/integration/CancelRestoreTracerIntegrationTest.java
  - payment-service/src/test/java/com/example/payment/application/service/CancelTxWriterPayloadTest.java
autonomous: true
requirements: [RST-01, RST-02]

must_haves:
  truths:
    - "payment.cancelled payload의 cancelledItems[]에 취소된 아이템의 skuId와 quantity가 실린다 (RST-01, D-P3-1)"
    - "기존 취소 통합테스트(CancelFlowIntegrationTest·CancelRaceIdempotencyIT·ProcessingRecoveryConcurrencyIT·CancelEventOutboxPublisherIT)가 무회귀 그린 — 취소 코어 TX/멱등/스케줄러/outbox 로직 불변의 행동 증거 (D-P3-1, D-P3-5)"
    - "product-service가 payment.cancelled를 신규 consumer(groupId=product-service)로 구독해 취소된 SKU 재고를 release로 복원한다 (RST-02, D-P3-2)"
    - "생성(reserve 차감)→취소(release 복원) 왕복이 available_qty를 원복한다 (Testcontainers MySQL+Kafka e2e)"
  artifacts:
    - "product-service/src/main/java/com/example/product/infrastructure/config/KafkaConsumerConfig.java (order-service 복제, groupId=product-service)"
    - "product-service/src/main/java/com/example/product/infrastructure/messaging/PaymentCancelledStockConsumer.java"
    - "product-service/src/main/java/com/example/product/application/service/ProcessCancelledStockService.java"
    - "product-service/src/test/java/com/example/product/integration/CancelRestoreTracerIntegrationTest.java"
  key_links:
    - "CancelTxWriter.buildPayload → cancelledItems JSON (skuId/quantity 추가, PaymentItem.getSkuId()/getQuantity() from Phase 2 V16)"
    - "Kafka topic payment.cancelled → PaymentCancelledStockConsumer.consume → ProcessCancelledStockService → StockService.release (Phase 1 원자 상태전이)"
---

<objective>
취소 복원의 tracer(수직 슬라이스): payment.cancelled payload에 skuId/quantity를 실어(RST-01, 취소 코어 로직 불변) product-service 최초 Kafka consumer가 이를 구독해 해당 SKU 재고를 복원하는(RST-02) 최소 e2e를 Testcontainers(MySQL+Kafka)로 그린으로 만든다.

Purpose: 아키텍처(payload 필드추가 + Kafka consumer 배선 + release 재사용)를 한 슬라이스로 관통 증명. 취소 코어 무변경을 기존 통합테스트 무회귀로 확정.
Output: buildPayload 필드추가, product Kafka consumer 인프라(Wave 0), release-on-cancel happy path, e2e 통합테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/kafka-design.md
@docs/superpowers/specs/2026-07-30-sku-stock-lifecycle-design.md

# 레퍼런스 (반드시 읽고 패턴 복제)
@payment-service/src/main/java/com/example/payment/application/service/CancelTxWriter.java
@order-service/src/main/java/com/example/order/infrastructure/config/KafkaConsumerConfig.java
@order-service/src/main/java/com/example/order/infrastructure/messaging/PaymentCancelledConsumer.java
@order-service/src/main/java/com/example/order/infrastructure/messaging/PaymentCancelledPayload.java
@order-service/src/main/java/com/example/order/application/service/ProcessCancelledItemsService.java
@order-service/src/main/java/com/example/order/application/usecase/ProcessCancelledItemsUseCase.java
@product-service/src/main/java/com/example/product/application/service/StockService.java
@product-service/build.gradle
@payment-service/build.gradle
@product-service/src/test/java/com/example/product/integration/StockTracerIntegrationTest.java
</context>

<tasks>

<task type="tracer">
  <name>Task 1: payment.cancelled payload에 skuId/quantity 추가 (RST-01, 취소 코어 불변)</name>
  <files>payment-service/src/main/java/com/example/payment/application/service/CancelTxWriter.java</files>
  <read_first>CancelTxWriter.java (line 76-101 buildPayload만 변경 대상); PaymentItem.getSkuId()/getQuantity() (Phase 2 V16, 이미 존재)</read_first>
  <reversibility rating="costly">payload 필드 계약(skuId/quantity)은 order/product consumer가 의존하는 이벤트 계약. kafka-design 규약상 필드추가는 하위호환(즉시 배포 가능)이나 한번 배포 후 제거는 하위호환 불가 — 되돌리기 비용 있음.</reversibility>
  <action>D-P3-1: buildPayload(line 76-101)의 cancelledItems 항목 JSON 포맷 문자열에 skuId·quantity 두 필드만 추가한다. i.getSkuId()(Long, nullable)·i.getQuantity()(int)를 읽어 기존 format 문자열 뒤에 붙인다. skuId가 null인 하위호환 데이터는 JSON null로 직렬화(String.format의 %d 대신 null 안전 처리 — 예: skuId 값이 null이면 "null" 리터럴, 아니면 숫자). saveTx1/saveTx2/saveTx3의 TX 경계·findAllByPaymentIdForUpdate 재조회·cancelEventPublisher.publish 호출·CancelRequest 상태전이는 한 줄도 변경 금지(D-P3-1, D-P3-5). 파티션 키·토픽·outbox 스키마 무변경.</action>
  <verify>
    <automated>./gradlew :payment-service:test --tests "com.example.payment.integration.CancelFlowIntegrationTest" --tests "com.example.payment.integration.CancelRaceIdempotencyIT" --tests "com.example.payment.integration.ProcessingRecoveryConcurrencyIT" --tests "com.example.payment.infrastructure.scheduler.CancelEventOutboxPublisherIT" --tests "com.example.payment.application.service.CancelTxWriterPayloadTest" && ./gradlew :order-service:test</automated>
  </verify>
  <done>기존 취소 통합/멱등/복구/outbox 테스트가 무회귀 그린(취소 코어 로직 불변 증거). CancelTxWriterPayloadTest가 cancelledItems JSON에 skuId·quantity 존재를 단언. **공유 payment.cancelled 계약 무회귀**: order-service test가 증강 payload(skuId/quantity 추가)에도 그린 — order consumer가 신규 필드를 무시하고 정상 동작(tools.jackson FAIL_ON_UNKNOWN_PROPERTIES 기본 false). git diff가 buildPayload 내부에만 국한됨.</done>
</task>

<task type="auto">
  <name>Task 2: CancelTxWriter payload 단언 테스트 (RST-01 무회귀 게이트)</name>
  <files>payment-service/src/test/java/com/example/payment/application/service/CancelTxWriterPayloadTest.java</files>
  <read_first>CancelFlowIntegrationTest.java (Testcontainers MySQL + MockitoBean KafkaTemplate 패턴, INLINE 모드로 payload 캡처)</read_first>
  <action>skuId/quantity를 가진 PaymentItem으로 취소를 실행하고 buildPayload가 만든 payload 문자열(publish로 넘어가는 값 또는 kafkaTemplate.send 캡처)을 검증한다. cancelledItems[].skuId·quantity가 PaymentItem 값과 일치하고, skuId가 null인 아이템은 payload에 null로 실림을 단언. 취소 코어를 새로 만들지 말고 CancelFlowIntegrationTest의 조립(INLINE 모드 + KafkaTemplate 스텁 캡처)을 재사용/복제.</action>
  <verify>
    <automated>./gradlew :payment-service:test --tests "com.example.payment.application.service.CancelTxWriterPayloadTest"</automated>
  </verify>
  <done>payload에 skuId/quantity가 정확히 실림 + null skuId 하위호환이 그린으로 검증됨.</done>
</task>

<task type="auto">
  <name>Task 3: product 최초 Kafka consumer 인프라 + release-on-cancel 최소 e2e (RST-02, Wave 0)</name>
  <files>product-service/build.gradle, product-service/src/main/resources/application.yml, product-service/src/main/java/com/example/product/infrastructure/config/KafkaConsumerConfig.java, product-service/src/main/java/com/example/product/infrastructure/messaging/PaymentCancelledStockConsumer.java, product-service/src/main/java/com/example/product/infrastructure/messaging/PaymentCancelledPayload.java, product-service/src/main/java/com/example/product/application/usecase/ProcessCancelledStockUseCase.java, product-service/src/main/java/com/example/product/application/service/ProcessCancelledStockService.java, product-service/src/test/java/com/example/product/integration/CancelRestoreTracerIntegrationTest.java</files>
  <read_first>order-service KafkaConsumerConfig(consumerFactory: group-id, enable.auto.commit=false, read_committed, MANUAL_IMMEDIATE ackMode), PaymentCancelledConsumer(ObjectMapper readValue → useCase.execute → ack; DataIntegrityViolationException catch로 UK충돌 멱등 ack), PaymentCancelledPayload(record), StockService.release(String paymentKey, List&lt;ReserveItem&gt;), StockTracerIntegrationTest(Testcontainers MySQL + MockMvc 조립 패턴)</read_first>
  <action>D-P3-2: product-service 최초 Kafka consumer를 order-service 스택 복제로 구축.
1) build.gradle: dependencies 블록 신설(현재 flyway 플러그인만 존재) — `implementation 'org.springframework.kafka:spring-kafka'` + `testImplementation 'org.testcontainers:kafka:1.19.7'` 추가(payment-service/build.gradle 버전 정렬). Resilience4j·producer는 이 plan 범위 아님(consume-only).
2) application.yml: spring.kafka.bootstrap-servers + spring.kafka.consumer.group-id=product-service + kafka.topic.payment-cancelled=payment.cancelled 추가(order-service application.yml 키 구조 정렬).
3) KafkaConsumerConfig: order 복제 — consumerFactory(bootstrap, groupId=product-service, enable.auto.commit=false, isolation.level=read_committed, MAX_POLL_RECORDS=100, StringDeserializer) + kafkaListenerContainerFactory(MANUAL_IMMEDIATE). MeterRegistry 주입은 product에 이미 있는 경우만; 없으면 생략.
4) PaymentCancelledPayload: order record 복제하되 CancelledItem에 `Long skuId, int quantity` 추가(paymentItemId, orderItemId, itemAmount, skuId, quantity). cancelRequestId는 String(order와 동일, JSON 숫자→String 코어스).
5) ProcessCancelledStockUseCase: 인터페이스 + record Command(String cancelRequestId, String paymentKey, List&lt;Item&gt; items) where Item(long skuId, int qty). ProcessCancelledStockService: `@Service`(product 컨벤션 — StockService가 @Service) + 생성자 주입으로 StockService를 받아 Command의 items를 StockService.release(paymentKey, items)로 위임(Phase 1 원자 상태전이 재사용). 이 plan에서는 멱등 테이블·retry 없이 happy path만(멱등/부분취소 하드닝은 03-02, retry/DLQ는 03-03). skuId null인 아이템은 release 대상에서 제외(하위호환). 별도 bean config 파일 없음(@Service 자기배선).
6) PaymentCancelledStockConsumer: `@Component` + @KafkaListener(topics=${kafka.topic.payment-cancelled}, groupId=${spring.kafka.consumer.group-id}, containerFactory=kafkaListenerContainerFactory) — payload 파싱 → cancelledItems를 skuId/qty로 매핑(skuId!=null만) → useCase.execute → ack. DataIntegrityViolationException catch로 멱등 ack(order 패턴, 03-02 UK 대비 미리 포함). ObjectMapper는 Boot 4 tools.jackson(order와 동일).
7) CancelRestoreTracerIntegrationTest: Testcontainers MySQL(StockTracerIntegrationTest 패턴) + KafkaContainer(org.testcontainers.kafka). @DynamicPropertySource로 datasource + spring.kafka.bootstrap-servers 주입. seed(product+sku+stock, available_qty=N) → reserve로 차감(available=N-q) → 테스트 producer로 payment.cancelled JSON(cancelledItems에 skuId/quantity 포함) 발행 → Awaitility로 consumer 처리 대기 → available_qty가 N으로 원복 단언.</action>
  <verify>
    <automated>./gradlew :product-service:test --tests "com.example.product.integration.CancelRestoreTracerIntegrationTest"</automated>
  </verify>
  <done>payment.cancelled(skuId/quantity 포함) 발행 → product consumer가 StockService.release로 available_qty를 정확히 원복. 전체 슬라이스가 실 MySQL+Kafka로 그린이고 커밋됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka payment.cancelled → product consumer | 신뢰 경계: 이벤트가 신뢰되지 않는 재전송/중복으로 들어올 수 있음(at-least-once) |
| payment TX3 → outbox payload | payload 필드추가가 취소 코어 TX에 영향 없어야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | CancelTxWriter.buildPayload | high | mitigate | 변경을 buildPayload 필드추가로 한정 + 기존 취소 통합테스트 무회귀 그린(TX/멱등/스케줄러/outbox 불변 증거). git diff 코어 게이트. |
| T-03-02 | Denial of Service | PaymentCancelledStockConsumer 재처리 | medium | mitigate(부분, 03-02) | happy path에 DataIntegrityViolationException 멱등 ack 포함. cancelRequestId 멱등 테이블은 03-02에서 완성. |
| T-03-SC | Tampering | product build.gradle 신규 install(spring-kafka, testcontainers:kafka) | low | accept | 신규 외부 패키지 아님 — 모노레포 order/payment가 이미 사용 중인 검증된 좌표. 버전 정렬만. |
</threat_model>

<verification>
- `./gradlew :payment-service:test` — 취소 코어 전체 무회귀 그린.
- `./gradlew :product-service:test` — tracer 통합테스트 그린.
- `git diff payment-service` 가 CancelTxWriter.buildPayload 내부 + 신규 테스트에만 국한(취소 코어 로직 불변).
</verification>

<success_criteria>
- payment.cancelled payload에 skuId/quantity가 실린다(RST-01).
- 기존 취소 통합/멱등/복구/outbox 테스트 무회귀 그린(취소 코어 불변).
- product consumer가 취소 이벤트로 SKU 재고를 복원(RST-02 happy path), Testcontainers MySQL+Kafka e2e 그린.
</success_criteria>

<output>
Create `.planning/workstreams/product-stock/phases/03-cancel-restore/03-01-SUMMARY.md` when done
</output>
