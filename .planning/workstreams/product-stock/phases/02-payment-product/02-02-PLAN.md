---
phase: 02-payment-product
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - payment-service/src/main/resources/db/migration/V17__create_stock_release_retry.sql
  - payment-service/src/main/java/com/example/payment/application/service/CreatePaymentService.java
  - payment-service/src/main/java/com/example/payment/application/interfaces/StockReleaseRetryRepository.java
  - payment-service/src/main/java/com/example/payment/infrastructure/persistence/StockReleaseRetryJpaEntity.java
  - payment-service/src/main/java/com/example/payment/infrastructure/persistence/StockReleaseRetryJpaRepository.java
  - payment-service/src/main/java/com/example/payment/infrastructure/persistence/StockReleaseRetryRepositoryImpl.java
  - payment-service/src/main/java/com/example/payment/application/service/StockReleaseRetryService.java
  - payment-service/src/main/java/com/example/payment/infrastructure/scheduler/StockReleaseRetryScheduler.java
  - payment-service/src/main/java/com/example/payment/infrastructure/config/PersistenceConfig.java
  - payment-service/src/main/resources/application.yml
  - payment-service/src/test/java/com/example/payment/application/service/CreatePaymentCompensationTest.java
  - payment-service/src/test/java/com/example/payment/application/service/StockReleaseRetryServiceTest.java
autonomous: true
requirements: [RSV-03]

must_haves:
  truths:
    - "예약 성공 후 payment 생성 TX가 실패하면 ProductStockPort.release(paymentKey, items)가 best-effort로 호출된다 (RSV-03, D-P2-6)"
    - "release 보상이 실패하면 stock_release_retry에 적재되고, 재시도 스케줄러(30s, Redis 분산락)가 release를 재호출해 정리한다 (RSV-03, D-P2-6)"
    - "취소 코어(CancelPaymentService/CancelTxWriter/CancelDomainService/기존 scheduler/messaging)는 무변경이다 (D-P2-7)"
  artifacts:
    - "payment-service/src/main/resources/db/migration/V17__create_stock_release_retry.sql (payment_key UK, items_json, attempt_count, next_retry_at, status)"
    - "com/example/payment/application/service/StockReleaseRetryService.java + infrastructure/scheduler/StockReleaseRetryScheduler.java (compensation-retry 동형)"
    - "재구조화된 CreatePaymentService의 persist 실패 catch → release 보상 → 실패 시 enqueue"
  key_links:
    - "CreatePaymentService: paymentCreateTxWriter.persist 실패 catch → release best-effort → release 실패 시 stockReleaseRetryRepository.enqueue 후 원예외 재던짐 (D-P2-6)"
    - "StockReleaseRetryScheduler → StockReleaseRetryService.retryAll → ProductStockPort.release (Redis 분산락, 30s)"
    - "release는 paymentKey+sku 멱등(product 측 UK)이라 재시도 안전 — 중복 release는 no-op"
---

<objective>
예약 성공 후 payment 생성 TX가 실패하는 경로에 보상을 배선한다: CreatePaymentService가 persist 실패를
catch해 ProductStockPort.release를 best-effort 호출하고, release도 실패하면 stock_release_retry에 적재한 뒤
compensation-retry 동형 스케줄러(30s, Redis 분산락)가 release를 재호출해 예약 누수를 정리한다(RSV-03). 취소 코어 불변.

Purpose: reserve→persist 실패 사이의 예약 고아(orphan)를 payment 측에서 최선-노력 + 재시도로 회수(오버셀 반대편의 재고 누수 방지).
Output: V17 재시도 테이블 + 보상 catch + StockReleaseRetryService/Scheduler.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/product-stock/ROADMAP.md
@.planning/workstreams/product-stock/REQUIREMENTS.md
@docs/superpowers/specs/2026-07-30-sku-stock-lifecycle-design.md
@CLAUDE.md
@.planning/workstreams/product-stock/phases/02-payment-product/02-01-PLAN.md

# 레퍼런스 — compensation-retry(risk 보상 재시도)를 그대로 미러링(D-P2-6)
@payment-service/src/main/java/com/example/payment/application/service/CompensationRetryService.java
@payment-service/src/main/java/com/example/payment/infrastructure/scheduler/CompensationRetryScheduler.java
@payment-service/src/main/resources/db/migration/V5__create_compensation.sql
# 재시도 repo/entity 패턴은 기존 CompensationRetry* 영속 어댑터 미러
@payment-service/src/main/java/com/example/payment/infrastructure/config/PersistenceConfig.java
# 이 plan이 확장하는 대상(02-01 산출): reserve/release 포트 + 오케스트레이터
@payment-service/src/main/java/com/example/payment/application/interfaces/ProductStockPort.java
@payment-service/src/main/java/com/example/payment/application/service/CreatePaymentService.java
@payment-service/src/main/java/com/example/payment/application/service/PaymentCreateTxWriter.java

# scheduler.lock.* 키는 application.yml에 이미 compensation-retry 등 존재 — stock-release-retry 신규 키 추가.
# CompensationRetryRepository 인터페이스(findDueForRetry/markDone/markRetryLater/exhaust)를 재시도 계약 템플릿으로 복제.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: persist 실패 보상(release) + V17 재시도 적재</name>
  <files>
    payment-service/src/main/resources/db/migration/V17__create_stock_release_retry.sql
    payment-service/src/main/java/com/example/payment/application/interfaces/StockReleaseRetryRepository.java
    payment-service/src/main/java/com/example/payment/infrastructure/persistence/StockReleaseRetryJpaEntity.java
    payment-service/src/main/java/com/example/payment/infrastructure/persistence/StockReleaseRetryJpaRepository.java
    payment-service/src/main/java/com/example/payment/infrastructure/persistence/StockReleaseRetryRepositoryImpl.java
    payment-service/src/main/java/com/example/payment/infrastructure/config/PersistenceConfig.java
    payment-service/src/main/java/com/example/payment/application/service/CreatePaymentService.java
    payment-service/src/test/java/com/example/payment/application/service/CreatePaymentCompensationTest.java
  </files>
  <behavior>
    - reserve 성공 + paymentCreateTxWriter.persist 성공 → release/enqueue 없음, 정상 반환
    - reserve 성공 + persist 예외 → ProductStockPort.release(paymentKey, items) 1회 호출 + 원예외 재던짐(payment 미생성)
    - reserve 성공 + persist 예외 + release도 예외 → stock_release_retry 1행 적재(payment_key, items) + 원예외 재던짐
  </behavior>
  <action>
    reserve 성공 후 persist 실패 경로에 보상을 배선한다(RSV-03, D-P2-6).

    V17__create_stock_release_retry.sql (compensation_retry 미러, V5 참고):
    - stock_release_retry(id PK AI, payment_key VARCHAR(64) NOT NULL, items_json VARCHAR(1000) NOT NULL,
      attempt_count INT NOT NULL DEFAULT 0, next_retry_at DATETIME(3) NOT NULL,
      status VARCHAR(20) NOT NULL DEFAULT 'PENDING', last_error VARCHAR(500) NULL, created_at, updated_at,
      UNIQUE uk_stock_release_payment_key(payment_key), INDEX idx_stock_release_status_next(status, next_retry_at)).
    - items_json = release 대상 items([{skuId,qty}]) 직렬화. persist가 롤백돼 payment_item이 없으므로 items를 여기 보존.

    StockReleaseRetryRepository (application/interfaces — CompensationRetryRepository 계약 미러):
    - enqueue(paymentKey, itemsJson): PENDING 1행 INSERT(next_retry_at=now, 멱등 UK 충돌 시 무시).
    - findDueForRetry(now) → List<PendingRelease{id, paymentKey, itemsJson, attemptCount}>.
    - markDone(id) / markRetryLater(id, attempt, nextRetryAt, error) / exhaust(id, attempt, error).

    infrastructure/persistence: StockReleaseRetryJpaEntity + JpaRepository + RepositoryImpl(기존 CompensationRetry* 어댑터 패턴).
    PersistenceConfig: StockReleaseRetryRepository 빈 수동 배선(기존 repository 빈 패턴 동일).

    CreatePaymentService 보상 배선(02-01의 오케스트레이터 확장):
    - reserve 성공 후 persist를 try/catch로 감싼다:
        try { return paymentCreateTxWriter.persist(...); }
        catch (RuntimeException e) {
          String itemsJson = 직렬화(items);
          try { productStockPort.release(paymentKey, items); }   // best-effort
          catch (RuntimeException re) { stockReleaseRetryRepository.enqueue(paymentKey, itemsJson); }
          throw e;   // 원예외 재던짐 → 결제 실패(payment 미생성)
        }
    - release가 성공하면 enqueue 안 함. release/enqueue는 결제 실패 응답을 바꾸지 않음(원예외 전파).
    - 직렬화는 Jackson ObjectMapper(기존 빈) 또는 간단 문자열 — items가 소수이므로 ObjectMapper 권장.

    CreatePaymentCompensationTest(@ExtendWith Mockito): ProductStockPort·PaymentCreateTxWriter·StockReleaseRetryRepository mock.
    <behavior> 3케이스 검증 — persist 성공 시 release/enqueue 없음(verify never), persist 실패 시 release 1회(verify),
    release도 실패 시 enqueue(paymentKey, itemsJson) 1회(verify) + 원예외 전파(assertThrows).
  </action>
  <verify>
    <automated>./gradlew :payment-service:test --tests "com.example.payment.application.service.CreatePaymentCompensationTest"</automated>
  </verify>
  <done>persist 실패 시 release가 best-effort 호출되고, release 실패 시 stock_release_retry에 적재되며, 두 경우 모두 결제는 실패(payment 미생성)한다.</done>
</task>

<task type="auto">
  <name>Task 2: StockReleaseRetryService + 재시도 스케줄러 (30s, Redis 분산락)</name>
  <files>
    payment-service/src/main/java/com/example/payment/application/service/StockReleaseRetryService.java
    payment-service/src/main/java/com/example/payment/infrastructure/scheduler/StockReleaseRetryScheduler.java
    payment-service/src/main/resources/application.yml
    payment-service/src/test/java/com/example/payment/application/service/StockReleaseRetryServiceTest.java
  </files>
  <action>
    stock_release_retry의 PENDING 건을 재시도해 예약 누수를 정리한다(RSV-03, D-P2-6 — CompensationRetryService/Scheduler 동형).

    StockReleaseRetryService(@Service — CompensationRetryService 미러):
    - MAX_ATTEMPTS=5, 백오프 attempt*60초(기존 정책 동일).
    - retryAll(): findDueForRetry(now) 순회 → retryOne.
    - retryOne(pending): items = itemsJson 역직렬화; productStockPort.release(pending.paymentKey, items);
      성공 → markDone. 실패 → nextAttempt>=MAX면 exhaust(수동 처리 알림), 아니면 markRetryLater(now+attempt*60s).
      (release는 paymentKey+sku 멱등이라 재시도 안전 — 이미 RELEASED면 product가 no-op 200.)

    StockReleaseRetryScheduler(@Component — CompensationRetryScheduler 미러):
    - RedissonClient 분산락 tryLock(0,25,SECONDS), @Scheduled(fixedDelay=30_000), @Value("${scheduler.lock.stock-release-retry}").
    - 락 획득 시 stockReleaseRetryService.retryAll(), finally unlock(isHeldByCurrentThread 가드).
    - 신규 스케줄러 — 취소 코어 scheduler 3종(pending/processing/compensation-recovery) 무변경(D-P2-7).

    application.yml: scheduler.lock.stock-release-retry 키 추가(기존 scheduler.lock.compensation-retry 옆).

    StockReleaseRetryServiceTest(@ExtendWith Mockito): StockReleaseRetryRepository·ProductStockPort mock.
    - due 1건 → release 성공 → markDone 검증.
    - due 1건 → release 예외 & attempt<5 → markRetryLater 검증(next_retry_at 미래).
    - attempt==4(다음 5) → release 예외 → exhaust 검증(최대초과 고정).
  </action>
  <verify>
    <automated>./gradlew :payment-service:test --tests "com.example.payment.application.service.StockReleaseRetryServiceTest"</automated>
  </verify>
  <done>PENDING release 재시도가 release 성공 시 markDone, 실패 시 백오프 재적재, 최대 초과 시 exhaust로 정리된다. Redis 분산락 스케줄러가 30s 주기로 구동된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payment-service → product-service | 보상 release HTTP + 재시도(내부 서비스 간) |
| scheduler → stock_release_retry | 배치 재시도, Redis 분산락으로 다중 인스턴스 직렬화 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Repudiation/Leak | reserve 성공 후 persist 실패 → 예약 고아(재고 누수) | high | mitigate | best-effort release + stock_release_retry 재시도(Task 1/2). release는 paymentKey+sku 멱등이라 중복 안전 |
| T-02-06 | Denial of Service | 다중 인스턴스 중복 재시도 | medium | mitigate | Redis 분산락(Redisson tryLock) — 기존 스케줄러 3종과 동일 패턴(Task 2) |
| T-02-07 | Tampering | 무한 재시도 폭주 | low | mitigate | MAX_ATTEMPTS=5 + 백오프 후 exhaust(수동 처리 전이) |
| T-02-08 | Tampering | npm/pip/cargo 신규 설치 | high | accept | 신규 패키지 없음 — 기존 Redisson/JPA/Jackson 재사용. SC 위협 해당 없음 |
</threat_model>

<verification>
- `./gradlew :payment-service:test` 전체 그린.
- core-unchanged gate: `git diff --name-only $(git merge-base HEAD origin/main)..HEAD | grep -E 'CancelPaymentService|CancelTxWriter|CancelDomainService|ProcessingRecoveryScheduler|PendingRecoveryScheduler|CompensationRetryScheduler|CancelEventOutboxPublisher|infrastructure/messaging|cancel_event_outbox'` → 매치 없음(exit 1). 신규 StockReleaseRetryScheduler는 취소/outbox 코어 아님(CancelEventOutboxPublisher 미매치, D-P2-7).
- V17이 payment-service flyway에 적용된다.
</verification>

<success_criteria>
- RSV-03: 예약 성공 후 결제 TX 실패 시 release 보상이 호출되고, 실패 시 재시도 스케줄러(compensation-retry 동형)가 정리한다.
- 취소 코어 불변(core-unchanged gate 통과).
</success_criteria>

<output>
Create `.planning/workstreams/product-stock/phases/02-payment-product/02-02-SUMMARY.md` when done
</output>
