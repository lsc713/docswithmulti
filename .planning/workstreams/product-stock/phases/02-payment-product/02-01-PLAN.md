---
phase: 02-payment-product
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - payment-service/src/main/resources/db/migration/V16__add_sku_and_quantity_to_payment_item.sql
  - payment-service/src/main/java/com/example/payment/domain/entity/PaymentItem.java
  - payment-service/src/main/java/com/example/payment/infrastructure/persistence/PaymentItemJpaEntity.java
  - payment-service/src/main/java/com/example/payment/presentation/dto/CreatePaymentItemRequest.java
  - payment-service/src/main/java/com/example/payment/application/service/CreatePaymentCommand.java
  - payment-service/src/main/java/com/example/payment/presentation/controller/PaymentController.java
  - payment-service/src/main/java/com/example/payment/application/interfaces/ProductStockPort.java
  - payment-service/src/main/java/com/example/payment/infrastructure/http/ProductStockHttpClient.java
  - payment-service/src/main/java/com/example/payment/infrastructure/exception/ProductServiceException.java
  - payment-service/src/main/java/com/example/payment/infrastructure/exception/StockInsufficientException.java
  - payment-service/src/main/java/com/example/payment/common/exception/ErrorCode.java
  - payment-service/src/main/java/com/example/payment/infrastructure/config/ResilienceConfig.java
  - payment-service/src/main/resources/application.yml
  - payment-service/src/main/java/com/example/payment/application/service/CreatePaymentService.java
  - payment-service/src/main/java/com/example/payment/application/service/PaymentCreateTxWriter.java
  - payment-service/src/test/java/com/example/payment/infrastructure/http/ProductStockHttpClientTest.java
  - payment-service/src/test/java/com/example/payment/application/service/CreatePaymentServiceTest.java
  - payment-service/src/test/java/com/example/payment/integration/CreatePaymentReserveIntegrationTest.java
autonomous: true
requirements: [RSV-01, RSV-02]

must_haves:
  truths:
    - "결제 생성 시 payment가 product로 SKU 재고를 동기 예약(payment TX 앞)하고, 예약 성공 후에만 payment TX가 커밋된다 (RSV-01, D-P2-1)"
    - "재고 부족(reserve 409) 또는 product 장애(CircuitBreaker OPEN)면 결제 생성이 거부되고 payment 행이 남지 않는다 (RSV-01, D-P2-2 fail-closed)"
    - "결제 요청·payment_item에 sku_id(product_sku.id)와 quantity가 영속되고, 신규 생성은 sku_id/quantity 필수 검증한다 (RSV-02, D-P2-3/4)"
    - "취소 코어(CancelPaymentService/CancelTxWriter/CancelDomainService/scheduler/messaging)는 무변경이다 (D-P2-7)"
  artifacts:
    - "payment-service/src/main/resources/db/migration/V16__add_sku_and_quantity_to_payment_item.sql (sku_id BIGINT NULL, quantity INT NOT NULL DEFAULT 1)"
    - "com/example/payment/application/interfaces/ProductStockPort.java + infrastructure/http/ProductStockHttpClient.java (reserve/release, 전용 CircuitBreaker)"
    - "com/example/payment/application/service/PaymentCreateTxWriter.java (@Transactional persist) + 재구조화된 CreatePaymentService (비-TX 오케스트레이터)"
    - "com/example/payment/integration/CreatePaymentReserveIntegrationTest.java (reserve 200→저장, 409/CB OPEN→거부)"
  key_links:
    - "CreatePaymentService: paymentKey 생성 → ProductStockPort.reserve(TX 밖) → PaymentCreateTxWriter.persist(@Transactional) 순서 (D-P2-1)"
    - "ProductStockHttpClient: reserve 409 → StockInsufficientException, CircuitBreaker OPEN/장애 → ProductServiceException → 결제 거부 (D-P2-2)"
    - "PaymentItem.of/reconstruct 하위호환 오버로드 — 기존 6/8인자 시그니처 유지로 취소 코어 테스트 무변경(core-unchanged gate 보호, D-P2-7)"
---

<objective>
결제 생성 경로에 product 동기 재고 예약을 배선한다: paymentKey 생성 → product.reserve(HTTP, TX 밖)
→ payment/payment_item persist(@Transactional)로 재구조화하고, sku_id/quantity를 요청~엔티티~DDL(V16)까지
관통 배선한다. reserve 성공 시 저장·409/장애 시 결제 거부(fail-closed)를 실제 MySQL(Testcontainers) +
MockRestServiceServer로 그린으로 만든다. 취소 코어는 불변.

Purpose: 생성 경로 아키텍처(TX-밖-HTTP 예약 규율)를 최초 커밋에서 end-to-end로 검증해 데드엔드 조기 차단.
Output: ProductStockPort/HttpClient + 재구조화된 CreatePaymentService + V16 배선 + 통합테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/product-stock/ROADMAP.md
@.planning/workstreams/product-stock/REQUIREMENTS.md
@docs/superpowers/specs/2026-07-30-sku-stock-lifecycle-design.md
@CLAUDE.md

# 레퍼런스 — 그대로 미러링(재발명 금지)
# reserve 클라이언트는 RiskManagementHttpClient의 RestTemplate+Resilience4j CircuitBreaker+Port/Adapter 템플릿 복제
@payment-service/src/main/java/com/example/payment/infrastructure/http/RiskManagementHttpClient.java
@payment-service/src/main/java/com/example/payment/application/interfaces/RiskManagementPort.java
@payment-service/src/main/java/com/example/payment/infrastructure/config/ResilienceConfig.java
@payment-service/src/main/java/com/example/payment/infrastructure/config/HttpClientConfig.java
@payment-service/src/main/java/com/example/payment/common/exception/ErrorCode.java
@payment-service/src/main/java/com/example/payment/presentation/controller/GlobalExceptionHandler.java
# 생성 경로 현재 코드(재구조화 대상)
@payment-service/src/main/java/com/example/payment/application/service/CreatePaymentService.java
@payment-service/src/main/java/com/example/payment/application/usecase/CreatePaymentUseCase.java
@payment-service/src/main/java/com/example/payment/application/service/CreatePaymentCommand.java
@payment-service/src/main/java/com/example/payment/presentation/dto/CreatePaymentRequest.java
@payment-service/src/main/java/com/example/payment/presentation/dto/CreatePaymentItemRequest.java
@payment-service/src/main/java/com/example/payment/presentation/controller/PaymentController.java
@payment-service/src/main/java/com/example/payment/domain/entity/PaymentItem.java
@payment-service/src/main/java/com/example/payment/infrastructure/persistence/PaymentItemJpaEntity.java
@payment-service/src/main/resources/db/migration/V1__create_payment_core.sql
# product 계약(호출 대상, 불변)
@product-service/src/main/java/com/example/product/presentation/controller/StockController.java
@product-service/src/main/java/com/example/product/presentation/dto/ReserveRequest.java
# 테스트 관행: HTTP 스텁 = MockRestServiceServer(Spring 내장, WireMock 미도입), 통합 = Testcontainers
@payment-service/src/test/java/com/example/payment/infrastructure/http/RiskManagementHttpClientTest.java
@payment-service/src/test/java/com/example/payment/infrastructure/http/PgCancelHttpClientCardinalityTest.java
@payment-service/src/test/java/com/example/payment/application/service/CreatePaymentServiceTest.java
@payment-service/src/test/java/com/example/payment/integration/CancelFlowIntegrationTest.java

# 인프라 확정: external.product-service.url → product-service 8084(CLAUDE.md 모듈표).
# WireMock은 미도입 — VALIDATION seed의 "WireMock"은 MockRestServiceServer(내장)로 대체(신규 의존성 금지, ponytail).
# 공유 RestTemplate 단일 빈(HttpClientConfig) — ProductStockHttpClient도 이 빈 주입, 통합테스트는
# MockRestServiceServer.createServer(restTemplate)로 reserve 응답 스텁.
</context>

<tasks>

<task type="auto">
  <name>Task 1: V16 + sku_id/quantity 배선 (요청~커맨드~엔티티~DDL)</name>
  <files>
    payment-service/src/main/resources/db/migration/V16__add_sku_and_quantity_to_payment_item.sql
    payment-service/src/main/java/com/example/payment/domain/entity/PaymentItem.java
    payment-service/src/main/java/com/example/payment/infrastructure/persistence/PaymentItemJpaEntity.java
    payment-service/src/main/java/com/example/payment/presentation/dto/CreatePaymentItemRequest.java
    payment-service/src/main/java/com/example/payment/application/service/CreatePaymentCommand.java
    payment-service/src/main/java/com/example/payment/presentation/controller/PaymentController.java
  </files>
  <reversibility rating="costly">V16(payment_item에 sku_id/quantity 추가)은 Flyway 적용 후 불변(롤포워드 V17로만 변경)이며 Phase 3 취소 payload의 sku 계약이다. 컬럼 정의는 D-P2-4·spec §4에서 이미 잠금 — 게이트 없이 flag만.</reversibility>
  <action>
    sku_id(product_sku.id)·quantity를 생성 요청부터 DDL까지 관통 배선한다(RSV-02, D-P2-3/4).

    V16__add_sku_and_quantity_to_payment_item.sql (spec §4 그대로):
    - ALTER TABLE payment_item ADD COLUMN sku_id BIGINT NULL, ADD COLUMN quantity INT NOT NULL DEFAULT 1.
    - sku_id는 느슨 참조(FK 없음 — 모듈 격리, CLAUDE.md). NULL 허용 = 기존 데이터 하위호환.
    - 적용 마이그레이션 수정 금지 — V16 신규 파일로만.

    PaymentItem.java (domain 순수 POJO — 하위호환 오버로드로 blast radius 최소화, D-P2-7 core-unchanged 보호):
    - private final Long skuId; private final int quantity; 필드 + getSkuId()/getQuantity() 추가.
    - 기존 of(6인자)·reconstruct(8인자) 시그니처는 유지하되 내부에서 skuId=null, quantity=1로 위임(취소 코어 테스트가
      이 시그니처를 호출하므로 변경 시 컴파일 파손 → 반드시 보존).
    - 신규 of(8인자: +skuId,+quantity)·reconstruct(10인자: +skuId,+quantity) 오버로드 추가 — 생성 경로/JpaEntity가 사용.
    - equals/hashCode 무변경(id+paymentId 기준).

    PaymentItemJpaEntity.java:
    - @Column(name="sku_id") Long skuId; @Column(name="quantity", nullable=false) int quantity; 추가.
    - from(item): e.skuId=item.getSkuId(); e.quantity=item.getQuantity().
    - toDomain(): 신규 reconstruct(10인자)로 skuId/quantity 포함해 재구성.
    - getSkuId()/getQuantity() getter 추가.

    CreatePaymentItemRequest.java: @NotNull Long skuId, @Positive int quantity 필드 추가(신규 생성 필수 검증, D-P2-4).
    CreatePaymentCommand.Item: long skuId, int quantity 추가.
    PaymentController: request.items() → command Item 매핑에 skuId/quantity 전달.
  </action>
  <verify>
    <automated>./gradlew :payment-service:test --tests "com.example.payment.domain.entity.PaymentItemTest" --tests "com.example.payment.infrastructure.persistence.PaymentItemRepositoryImplTest"</automated>
  </verify>
  <done>V16이 sku_id/quantity를 추가하고, PaymentItem 하위호환 오버로드로 기존 테스트가 컴파일·통과한다. sku_id/quantity의 실제 DB 왕복 영속은 Task 3 scenario A(jdbc assert)가 백스톱 증명한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProductStockPort + ProductStockHttpClient (reserve/release, fail-closed)</name>
  <files>
    payment-service/src/main/java/com/example/payment/application/interfaces/ProductStockPort.java
    payment-service/src/main/java/com/example/payment/infrastructure/http/ProductStockHttpClient.java
    payment-service/src/main/java/com/example/payment/infrastructure/exception/ProductServiceException.java
    payment-service/src/main/java/com/example/payment/infrastructure/exception/StockInsufficientException.java
    payment-service/src/main/java/com/example/payment/common/exception/ErrorCode.java
    payment-service/src/main/java/com/example/payment/infrastructure/config/ResilienceConfig.java
    payment-service/src/main/resources/application.yml
    payment-service/src/test/java/com/example/payment/infrastructure/http/ProductStockHttpClientTest.java
  </files>
  <behavior>
    - reserve 200 응답 → 정상 반환(예외 없음)
    - reserve 409(STOCK_INSUFFICIENT) 응답 → StockInsufficientException
    - reserve 5xx/타임아웃 → ProductServiceException
    - CircuitBreaker OPEN 상태 호출 → CallNotPermittedException을 ProductServiceException으로 래핑(fail-closed, D-P2-2)
    - release 200 → 정상 반환
  </behavior>
  <action>
    RiskManagementHttpClient를 복제해 product 동기 클라이언트를 만든다(D-P2-5).

    ProductStockPort (application/interfaces): void reserve(String paymentKey, List<Item> items);
    void release(String paymentKey, List<Item> items); record Item(long skuId, int qty).

    ProductStockHttpClient (@Component, ProductStockPort 구현):
    - 생성자: RestTemplate(공유 빈), @Value("${external.product-service.url}") baseUrl, CircuitBreaker productServiceCircuitBreaker.
    - reserve: circuitBreaker.executeCheckedSupplier로 POST {baseUrl}/v1/stock/reserve, body Map paymentKey+items(spec §5).
      RestTemplate 기본 에러 핸들러가 4xx에 HttpClientErrorException를 던지므로 catch (HttpClientErrorException e):
        e.getStatusCode().value()==409 → throw new StockInsufficientException(...) (재고 부족 = 결제 거부).
      catch (Error e) throw e; catch (Throwable t) → ProductServiceException(장애=거부, CB OPEN의 CallNotPermittedException 포함).
      (RiskManagementHttpClient의 catch 계층·명시적 2xx 가드 규율 동일.)
    - release: 동일 구조로 POST {baseUrl}/v1/stock/release. 실패는 ProductServiceException(호출측이 재시도 판단).

    ProductServiceException(infrastructure/exception, BusinessException 확장, ErrorCode.PRODUCT_SERVICE_UNAVAILABLE).
    StockInsufficientException(infrastructure/exception, BusinessException 확장, ErrorCode.STOCK_INSUFFICIENT).

    ErrorCode 추가:
    - STOCK_INSUFFICIENT("STOCK_INSUFFICIENT", 409, "재고가 부족하여 결제를 생성할 수 없습니다.")
    - PRODUCT_SERVICE_UNAVAILABLE("PRODUCT_SERVICE_UNAVAILABLE", 503, "상품 재고 서비스가 일시적으로 이용 불가합니다.")
    (둘 다 결제 거부 = fail-closed. GlobalExceptionHandler가 BusinessException→상태코드 자동 매핑.)

    ResilienceConfig: productServiceCircuitBreaker 빈 추가 — registry.circuitBreaker("product-service"). DEFAULT_CONFIG 재사용.
    application.yml: external 아래 product-service.url 추가(예: http://localhost:8084, 주석에 컨테이너 호스트 대안).

    ProductStockHttpClientTest: RiskManagementHttpClientTest 패턴 복제 — mock RestTemplate + 테스트용 slidingWindow=2 CB.
    <behavior>의 5개 케이스를 각각 검증. 409는 HttpClientErrorException.create(...)로 스텁.
  </action>
  <verify>
    <automated>./gradlew :payment-service:test --tests "com.example.payment.infrastructure.http.ProductStockHttpClientTest"</automated>
  </verify>
  <done>reserve 409→StockInsufficientException, CB OPEN/장애→ProductServiceException, 200→정상. product-service CircuitBreaker 빈과 external.product-service.url이 존재한다.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: CreatePaymentService 재구조화(paymentKey→reserve→persist) + e2e 그린</name>
  <files>
    payment-service/src/main/java/com/example/payment/application/service/CreatePaymentService.java
    payment-service/src/main/java/com/example/payment/application/service/PaymentCreateTxWriter.java
    payment-service/src/test/java/com/example/payment/application/service/CreatePaymentServiceTest.java
    payment-service/src/test/java/com/example/payment/integration/CreatePaymentReserveIntegrationTest.java
  </files>
  <behavior>
    - reserve 성공 → payment + payment_item(sku_id/quantity) 커밋, Result 반환
    - reserve가 StockInsufficientException(409) → 예외 전파, payment 행 미생성(fail-closed)
    - product CircuitBreaker OPEN → ProductServiceException(503) 전파, payment 행 미생성(fail-closed)
    - paymentKey는 reserve 호출 전에 확정되고, reserve의 멱등 키로 전달된다
  </behavior>
  <action>
    ONE 경로(생성→reserve→persist)를 전 레이어 관통 배선한다. 보상(persist 실패 후 release)·재시도 스케줄러는
    Plan 02에서 확장 — 여기서는 다루지 않는다(reserve 실패 거부만).

    CreatePaymentService 재구조화(D-P2-1 — 취소 플로우 TX1/reserve/TX2 오케스트레이션과 동형):
    - 클래스에서 @Transactional 제거 → 비-TX 오케스트레이터로 전환. 의존성에 ProductStockPort, PaymentCreateTxWriter 추가.
    - create(command):
      (1) String paymentKey = "pay_" + UUID... (현재 line 31 로직을 create 시작부로 유지 — 이미 TX 밖 시작부이나
          @Transactional 제거로 확실히 커밋 이전에 확정).
      (2) List<ProductStockPort.Item> = command.items() → (skuId, quantity) 매핑.
          productStockPort.reserve(paymentKey, items) 호출(TX 밖, HTTP). 실패(StockInsufficient/ProductService 예외)는
          catch하지 않고 전파 → GlobalExceptionHandler가 409/503으로 거부(fail-closed, D-P2-2). persist 미도달.
      (3) return paymentCreateTxWriter.persist(command, paymentKey, totalAmount).

    PaymentCreateTxWriter(@Component, @Transactional — 자기호출 방지 위해 별도 빈, CancelTxWriter 동형):
    - persist(command, paymentKey, totalAmount): 현재 CreatePaymentService의 Payment.of + save + PaymentItem 매핑(신규
      of(8인자)로 skuId/quantity 포함) + saveAll → Result 반환. 이 메서드만 트랜잭션 경계(TX).

    CreatePaymentServiceTest 갱신: ProductStockPort mock 추가. 
    - reserve 성공 시 txWriter.persist 결과 반환 검증, reserve 호출 인자(paymentKey, items skuId/qty) 검증.
    - reserve가 StockInsufficientException 던지면 txWriter.persist 미호출 검증(fail-closed).
    - 기존 금액합산/단일아이템 케이스는 command.Item에 skuId/quantity 추가해 유지.

    CreatePaymentReserveIntegrationTest (tracer e2e — @SpringBootTest + @Testcontainers MySQL(payment_db) +
    @DynamicPropertySource datasource 주입, CancelFlowIntegrationTest 패턴; MockMvc는 webAppContextSetup 조립):
    - MockRestServiceServer.createServer(공유 restTemplate 빈)로 product reserve 응답 스텁.
    - 시나리오 A: reserve 200 스텁 → POST /v1/payments {items:[{skuId,quantity,...}]} → 200. jdbc로 payment 1행 +
      payment_item.sku_id/quantity 저장 확인. mockServer.verify()로 reserve가 paymentKey로 호출됐는지 확인.
    - 시나리오 B: reserve 409(STOCK_INSUFFICIENT) 스텁 → 동일 요청 → 409 code=STOCK_INSUFFICIENT. jdbc로 payment 0행(거부).
    - 시나리오 C: 프로덕션 productServiceCircuitBreaker 빈을 테스트에 주입 후 요청 직전
      `circuitBreaker.transitionToOpenState()`로 OPEN 강제(DEFAULT_CONFIG의 minimumNumberOfCalls=100 때문에
      "연속 실패 스텁"으로는 비현실적·flaky — 직접 전이). OPEN 상태에서 POST /v1/payments → 503
      code=PRODUCT_SERVICE_UNAVAILABLE. jdbc로 payment 0행(fail-closed, D-P2-2). 검증 후 transitionToClosedState()로 원복.
      (대안: application-test.yml/@TestConfiguration로 작은 slidingWindow+minimumNumberOfCalls override — transition 강제가 더 간단.)
  </action>
  <verify>
    <automated>./gradlew :payment-service:test --tests "com.example.payment.application.service.CreatePaymentServiceTest" --tests "com.example.payment.integration.CreatePaymentReserveIntegrationTest"</automated>
  </verify>
  <done>생성→reserve→persist 경로가 실제 MySQL로 그린. reserve 200→저장, 409→409 거부·미저장, CB OPEN→503 거부·미저장이 통합테스트로 증명된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → payment-service | 결제 생성 요청(items: skuId/quantity 포함) — 신뢰 불가 입력, Bean Validation 경계 |
| payment-service → product-service | 동기 reserve/release HTTP(내부 서비스 간) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | POST /v1/payments items(skuId/quantity) | medium | mitigate | @NotNull skuId + @Positive quantity(Task 1). 재고 진위는 product reserve의 원자 UPDATE가 최종 판정 |
| T-02-02 | Denial of Service | product-service reserve 장애 전파 | high | mitigate | 전용 CircuitBreaker(product-service) + fail-closed 거부(Task 2/3, D-P2-2) — 장애가 결제 스레드를 무한 대기시키지 않음 |
| T-02-03 | Elevation/Overselling | reserve fail-open 시 오버셀 | high | mitigate | fail-closed: reserve 실패(409/장애) 시 결제 거부, payment 미생성(Task 3 시나리오 B/C) |
| T-02-04 | Tampering | npm/pip/cargo 신규 설치 | high | accept | 신규 패키지 설치 없음 — 기존 의존성(Resilience4j/RestTemplate/Testcontainers)만 재사용. SC 위협 해당 없음 |
</threat_model>

<verification>
- `./gradlew :payment-service:test` 전체 그린.
- core-unchanged gate: `git diff --name-only $(git merge-base HEAD origin/main)..HEAD | grep -E 'CancelPaymentService|CancelTxWriter|CancelDomainService|ProcessingRecoveryScheduler|PendingRecoveryScheduler|CompensationRetryScheduler|CancelEventOutboxPublisher|infrastructure/messaging|cancel_event_outbox'` → 매치 없음(exit 1). 취소/outbox 코어 무변경(D-P2-7). CancelEventOutboxPublisher = OUTBOX 정식 발행+purge 스케줄러(취소 코어 1급).
- V16이 payment-service flyway에 적용되고, 신규 컬럼이 취소 로직에 영향 없음(취소는 기존 컬럼만 사용).
</verification>

<success_criteria>
- RSV-01: 결제 생성이 product reserve(TX 앞) 성공 후에만 커밋되고, reserve 실패(재고 부족·CB OPEN)면 거부된다.
- RSV-02: sku_id/quantity가 요청·payment_item(V16)에 영속되고 신규 생성 시 필수 검증된다.
- 취소 코어 불변(core-unchanged gate 통과).
</success_criteria>

<output>
Create `.planning/workstreams/product-stock/phases/02-payment-product/02-01-SUMMARY.md` when done
</output>
