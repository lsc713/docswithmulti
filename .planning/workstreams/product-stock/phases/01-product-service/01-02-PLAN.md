---
phase: 01-product-service
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - product-service/src/main/java/com/example/product/application/service/StockService.java
  - product-service/src/main/java/com/example/product/application/interfaces/ProductStockRepository.java
  - product-service/src/main/java/com/example/product/application/interfaces/StockReservationRepository.java
  - product-service/src/main/java/com/example/product/infrastructure/persistence/ProductStockJpaRepository.java
  - product-service/src/main/java/com/example/product/infrastructure/persistence/StockReservationJpaRepository.java
  - product-service/src/main/java/com/example/product/presentation/controller/StockController.java
  - product-service/src/main/java/com/example/product/presentation/dto/
  - product-service/src/test/java/com/example/product/integration/StockConcurrencyIntegrationTest.java
  - product-service/src/test/java/com/example/product/integration/StockIdempotencyIntegrationTest.java
  - product-service/src/test/java/com/example/product/integration/StockReleaseIntegrationTest.java
autonomous: true
requirements: [STOCK-03, STOCK-04]

must_haves:
  truths:
    - "재고 1개에 서로 다른 키 동시 reserve N건 → 정확히 1건만 200, 나머지 409, available_qty가 음수로 내려가지 않는다 (STOCK-03, D-P1-3 오버셀 방지)"
    - "같은 paymentKey+sku로 reserve 재요청(순차·동시) 시 기존 예약을 재사용하고 재차감하지 않으며 uk위반 500이 나지 않는다 (STOCK-04, D-P1-4 멱등, INSERT-ON-DUPLICATE 게이트)"
    - "release는 원자 조건부 상태전이(RESERVED→RELEASED)가 성공(affected=1)할 때만 available += qty 복원 — 동시 이중 release에도 복원 1회, 이미 RELEASED/미존재는 no-op 200 (STOCK-04, D-P1-4)"
    - "다중 items 중 하나라도 부족하면 reserve 전체가 롤백된다(전-items 원자, D-P1-3)"
  artifacts:
    - "com/example/product/presentation/controller/StockController.java (POST /v1/stock/release 추가)"
    - "StockReservationJpaRepository (INSERT-ON-DUPLICATE upsertReserved + 원자 조건부 status 전이 UPDATE)"
    - "product-service/src/test/java/com/example/product/integration/StockConcurrencyIntegrationTest.java (서로다른키 오버셀 + 같은키 멱등 burst 회귀)"
  key_links:
    - "uk_reservation_paymentkey_sku UK → INSERT-ON-DUPLICATE 멱등 게이트(차감보다 앞, D-P1-4)"
    - "release 원자 조건부 전이: UPDATE ... WHERE status='RESERVED' affected=1일 때만 available_qty 복원"
---

<objective>
Plan 01의 검증된 reserve/seed 슬라이스에서 확장한다: (1) 동시 reserve 오버셀 부재를 실측 회귀로 고정,
(2) reserve/release paymentKey 멱등, (3) release(RESERVED만 해제·no-op), (4) 다중아이템 원자 롤백.
STOCK-03의 동시성 증명과 STOCK-04(멱등·release)를 완결한다.

Purpose: 오버셀 방지의 핵심 주장(원자 조건부 UPDATE)을 동시성 부하로 증명하고, 멱등·해제 계약을 잠근다.
Output: release 엔드포인트 + 동시성/멱등/해제 통합테스트 3종.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/product-stock/ROADMAP.md
@docs/superpowers/specs/2026-07-30-sku-stock-lifecycle-design.md
@CLAUDE.md
# Plan 01이 만든 슬라이스 위에서 확장 — StockService/StockController/ProductStockJpaRepository 재사용.
@.planning/workstreams/product-stock/phases/01-product-service/01-01-PLAN.md
# 원자 복원 UPDATE 미러 대상(risk tryRestore)
@risk-management-service/src/main/java/com/example/riskmanagement/infrastructure/persistence/MerchantCancelUsageJpaRepository.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: reserve 멱등 재사용 + 다중아이템 원자 롤백</name>
  <files>
    product-service/src/main/java/com/example/product/application/service/StockService.java
    product-service/src/main/java/com/example/product/application/interfaces/StockReservationRepository.java
    product-service/src/main/java/com/example/product/infrastructure/persistence/StockReservationJpaRepository.java
    product-service/src/test/java/com/example/product/integration/StockIdempotencyIntegrationTest.java
  </files>
  <action>
    StockService.reserve를 멱등·원자로 강화한다(D-P1-4 멱등, D-P1-3 전-items 원자).
    핵심(W1): SELECT-then-INSERT는 동시 같은 paymentKey+sku에서 loser INSERT가 uk 위반 500을 낸다. 이를 근본 차단하려면
    **차감보다 예약 INSERT를 먼저** 두어 UK가 멱등 게이트가 되게 한다(risk `ensureRow`의 INSERT-ON-DUPLICATE 미러 —
    예외·TX poisoning·보상 없음). item당:
    - StockReservationJpaRepository에 native `INSERT INTO stock_reservation(payment_key,sku_id,qty,status,created_at,updated_at)
      VALUES(:pk,:sku,:qty,'RESERVED',now6,now6) ON DUPLICATE KEY UPDATE payment_key=payment_key` → affected int(1=신규 삽입, 0=기존 존재).
    - affected=1 (이 요청이 예약 소유) → tryReserve 차감; 0이면 StockInsufficientException 던짐 → 같은 TX라 방금 INSERT한 예약행까지 롤백.
    - affected=0 (이미 예약됨 — winner가 이미 소유) → 차감 생략, 기존 예약 재사용 200. **loser는 절대 차감 안 함**(중복차감·오버셀 불가).
    - 동시 같은 키: DB가 uk 행락으로 직렬화 → 정확히 1건만 affected=1(차감), 나머지 affected=0(재사용). 취소 경로가 이미 쓰는 패턴.
    - 전체 reserve는 단일 @Transactional — 다중 items 중 하나라도 부족하면 예외 전파로 앞선 item INSERT/차감까지 전부 롤백.
    StockReservationJpaRepository.findByPaymentKeyAndSkuId(재사용 응답 조회용) + upsertReserved(위 native) 추가.
    StockIdempotencyIntegrationTest (@SpringBootTest+@Testcontainers, MockMvcBuilders.webAppContextSetup — Plan 01 패턴):
    - seed available=10. 같은 {paymentKey="pk-1", items:[{sku,qty:3}]}로 reserve 2회 → 둘 다 200, jdbc로 available=7(한 번만 차감),
      stock_reservation 행 1개 확인(재차감/중복행 없음).
    - 다중아이템 원자: skuA available=5, skuB available=1. reserve {pk-2, items:[{A,qty:2},{B,qty:5}]} → 409,
      A의 available 여전히 5(B 부족으로 A 차감 롤백), stock_reservation에 pk-2 행 0개 확인.
  </action>
  <verify>
    <automated>./gradlew :product-service:test --tests "com.example.product.integration.StockIdempotencyIntegrationTest"</automated>
  </verify>
  <done>같은 paymentKey+sku reserve 재요청이 INSERT-ON-DUPLICATE 게이트로 재차감 안 함(멱등, loser 차감 없음), 다중아이템 중 하나 부족 시 전체 롤백.</done>
</task>

<task type="auto">
  <name>Task 2: release 엔드포인트 (RESERVED만 해제, 멱등 no-op)</name>
  <files>
    product-service/src/main/java/com/example/product/application/service/StockService.java
    product-service/src/main/java/com/example/product/application/interfaces/ProductStockRepository.java
    product-service/src/main/java/com/example/product/infrastructure/persistence/ProductStockJpaRepository.java
    product-service/src/main/java/com/example/product/presentation/controller/StockController.java
    product-service/src/main/java/com/example/product/presentation/dto/
    product-service/src/test/java/com/example/product/integration/StockReleaseIntegrationTest.java
  </files>
  <action>
    POST /v1/stock/release 추가(spec §5, D-P1-4·D-P1-5).
    핵심(W2 — 원자성): SELECT status → 무조건 available+=qty → status=RELEASED의 3단 분리는 동시 이중 release에서
    둘 다 RESERVED를 읽고 둘 다 복원(재복원 오버-release)한다. **조건부 상태전이가 게이트가 되도록 순서를 뒤집는다**(reserve 원자 패턴과 동형):
    - StockReservationJpaRepository에 원자 조건부 전이:
      "UPDATE stock_reservation SET status='RELEASED', updated_at=CURRENT_TIMESTAMP(6) WHERE payment_key=:pk AND sku_id=:sku AND status='RESERVED'" → int affected.
    - ProductStockJpaRepository에 원자 복원(risk tryRestore 미러):
      "UPDATE product_stock SET available_qty = available_qty + :qty, updated_at=CURRENT_TIMESTAMP(6) WHERE sku_id=:skuId" → int.
    - StockService.release(paymentKey, items): @Transactional. item당 먼저 조건부 전이 UPDATE 실행.
      **affected=1일 때만** 재고 복원(available += qty). affected=0(이미 RELEASED 또는 미존재) → 복원 생략 no-op.
      상태전이가 재고복원의 유일 트리거이므로 동시 이중 release라도 정확히 1건만 전이 성공 → 복원 1회. 항상 200 {released:true}.
    - StockController.release: ReleaseRequest {paymentKey, items[{skuId,qty}]} → 200 {released:true}.
    StockReleaseIntegrationTest:
    - seed available=10, reserve qty:4(available=6) → release {sku,qty:4} → 200, available=10 복원, 예약 status=RELEASED 확인.
    - 같은 release 재호출 → 200 no-op, available 여전히 10(재복원 없음 — 멱등).
    - 예약 없는 paymentKey로 release → 200 no-op, available 불변.
    - **동시 이중 release(W2 회귀)**: reserve qty:4 후 같은 {pk,sku,qty:4} release를 2스레드 동시 발사 → 둘 다 200,
      available 정확히 10(4만 1회 복원, 14 아님), status=RELEASED. RANDOM_PORT+JDK HttpClient(Task 3 하네스 재사용) 또는
      동일 서비스 동시호출로 검증. Phase 3(cancel consumer+orphan 스케줄러)이 동시 release를 실현하므로 지금 회귀로 고정.
  </action>
  <verify>
    <automated>./gradlew :product-service:test --tests "com.example.product.integration.StockReleaseIntegrationTest"</automated>
  </verify>
  <done>release는 조건부 상태전이(RESERVED→RELEASED) affected=1일 때만 복원 — 동시 이중 release에도 정확히 1회 복원(오버-release 불가), 이미 RELEASED/미존재는 no-op 200.</done>
</task>

<task type="auto">
  <name>Task 3: 동시 reserve 오버셀 부재 회귀 (핵심)</name>
  <files>
    product-service/src/test/java/com/example/product/integration/StockConcurrencyIntegrationTest.java
  </files>
  <action>
    D-P1-3 원자 조건부 UPDATE가 실제 동시성에서 오버셀을 막는지 실측 회귀로 고정한다.
    Boot 4.0.5 제약(D-P1-6)상 MockMvc는 동시성 실측에 부적합 → @SpringBootTest(webEnvironment=RANDOM_PORT) + JDK HttpClient로 실제 HTTP 동시 호출.
    - @Testcontainers MySQLContainer, @DynamicPropertySource로 datasource 주입, @LocalServerPort로 포트 획득.
    - seed: 단일 sku, initialStock=1.
    - ExecutorService(N=20 스레드) + CountDownLatch로 서로 다른 paymentKey(pk-0..pk-19) 20건을 동시에 POST /v1/stock/reserve {qty:1} 발사.
      (paymentKey를 다르게 해야 멱등 재사용이 아닌 순수 재고 경합을 검증 — 같은 키면 UK 멱등으로 흐려짐.)
    - 단언(서로 다른 키 — 재고 경합): 200 응답 정확히 1건, 409(STOCK_001) 19건. jdbc로 product_stock.available_qty=0(음수 아님),
      stock_reservation RESERVED 행 정확히 1개.
    - **same-key burst(W1 회귀)**: 별도 시나리오 — seed available=10, 같은 paymentKey "pk-same"+같은 sku {qty:1}로 20건 동시 발사.
      단언: 20건 모두 200(500/uk위반 없음 — INSERT-ON-DUPLICATE 멱등 게이트), available 정확히 9(단 1회만 차감),
      stock_reservation 행 정확히 1개. 멱등이 동시성에서도 재차감 없이 성립함을 고정.
  </action>
  <verify>
    <automated>./gradlew :product-service:test --tests "com.example.product.integration.StockConcurrencyIntegrationTest"</automated>
  </verify>
  <done>서로 다른 키 동시 20건 → 200 1건·409 19건·available=0(오버셀 없음); 같은 키 동시 20건 → 200 20건·available 1회만 차감·예약행 1개(멱등, uk위반 500 없음).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 동시 client → reserve (product_stock) | 다중 스레드가 같은 재고 행을 경합 — lost update/오버셀 위험 지점 |
| client → release (product_stock) | 재요청/미존재 예약으로 재고 부풀리기(over-release) 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | 동시 reserve 오버셀 | critical | mitigate | 원자 조건부 UPDATE(D-P1-3) + Task 3 서로 다른 키 동시 20건 회귀로 200=1·available≥0 실측 고정. |
| T-02-02 | Tampering | 동시 이중 release로 재고 부풀리기(over-release) | high | mitigate | **원자 조건부 상태전이**(W2): `UPDATE stock_reservation SET status=RELEASED WHERE ... AND status=RESERVED`의 affected=1일 때만 재고 복원. DB가 전이를 직렬화해 동시 이중 release라도 복원 1회. Task 2 동시-이중-release 회귀로 고정(SELECT-then-UPDATE 3단 분리의 재복원 결함 제거). |
| T-02-03 | Tampering | reserve 멱등 우회(같은 키 재차감) | high | mitigate | **INSERT-ON-DUPLICATE 멱등 게이트**(W1): 예약 INSERT를 차감보다 앞세워 uk가 직렬화. affected=1일 때만 차감, loser affected=0은 재사용(차감 없음). 동시 same-key도 500 없이 200. Task 3 same-key burst 회귀로 고정. |
</threat_model>

<verification>
- `./gradlew :product-service:test` — 전체 통합테스트 그린(Plan 01 + Plan 02, Docker 필요).
- 동시성 테스트(Task 3)가 오버셀 부재를 실측 — STOCK-03의 결정적 증거.
</verification>

<success_criteria>
- 동시 reserve에서 오버셀 없음, available_qty 음수 불가 (STOCK-03).
- reserve/release paymentKey+sku 멱등 — 재차감/재복원 없음 (STOCK-04).
- release는 RESERVED만 해제, 이미 RELEASED/미존재는 no-op (STOCK-04).
- 다중아이템 reserve 전-items 원자 롤백 (D-P1-3).
</success_criteria>

<output>
Create `.planning/workstreams/product-stock/phases/01-product-service/01-02-SUMMARY.md` when done
</output>
