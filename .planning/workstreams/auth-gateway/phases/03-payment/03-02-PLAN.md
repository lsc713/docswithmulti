---
phase: 03-payment
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - payment-service/src/test/java/com/example/payment/domain/service/CancelAuthorizerTest.java
  - payment-service/src/test/java/com/example/payment/application/service/CancelAuthorizationServiceTest.java
  - payment-service/src/test/java/com/example/payment/presentation/controller/CancelControllerTest.java
  - docs/domain-rules.md
  - docs/api-spec.md
autonomous: true
requirements: [AUTHZ-01]

must_haves:
  truths:
    - "인가 매트릭스 6종(ADMIN 허용 / MERCHANT match 허용 / MERCHANT mismatch 403 / MERCHANT merchantId 누락 403 / USER 403 / role 누락 403)이 domain 단위로 exhaustive 하게 검증된다 (D-P3-2)"
    - "권한 role(ADMIN) 취소가 인가 통과 후 기존 취소 플로우로 정상 처리됨(cancel 호출 도달)이 컨트롤러 레벨에서 증명된다 (SC#1)"
    - "MERCHANT 경로만 payment 를 read-only 로드하고 ADMIN 은 로드를 생략한다 (D-P3-5)"
  artifacts:
    - payment-service/src/test/java/com/example/payment/domain/service/CancelAuthorizerTest.java
    - payment-service/src/test/java/com/example/payment/application/service/CancelAuthorizationServiceTest.java
    - docs/domain-rules.md
  key_links:
    - "CancelAuthorizerTest → domain CancelAuthorizer (순수 매트릭스)"
    - "CancelAuthorizationServiceTest → PaymentRepository mock (ADMIN 로드 생략 / MERCHANT 로드 검증)"
    - "domain-rules.md 인가 정책 append → 취소 인가 규칙 원본"
---

<objective>
Plan 01 tracer 슬라이스에서 관통시킨 인가 경로를 exhaustive 하게 확장한다: 인가 매트릭스 전 케이스 domain 단위 + 오케스트레이션(로드/404/생략) 단위 + ADMIN pass-through 컨트롤러 증명 + 인가 정책 문서화.

Purpose: Success Criteria 3종을 모두 자동 검증으로 못박고(권한 role 정상 처리 / 무권한 403 / role-only 인가), 신규 인가 정책을 domain-rules.md 원본에 명문화한다.
Output: CancelAuthorizerTest(6종), CancelAuthorizationServiceTest(로드·404·위임·생략), CancelControllerTest ADMIN pass-through 확장, docs/domain-rules.md 인가 정책 append + NetworkPolicy 신뢰 경계 note, api-spec 403 note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/auth-gateway/phases/03-payment/03-RESEARCH.md
@.planning/workstreams/auth-gateway/phases/03-payment/03-01-PLAN.md
@payment-service/src/test/java/com/example/payment/presentation/controller/CancelControllerTest.java
@payment-service/src/main/java/com/example/payment/domain/entity/Payment.java
@payment-service/src/main/java/com/example/payment/application/exception/PaymentNotFoundException.java
@docs/domain-rules.md
@docs/api-spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CancelAuthorizer 인가 매트릭스 6종 (domain 단위)</name>
  <files>payment-service/src/test/java/com/example/payment/domain/service/CancelAuthorizerTest.java</files>
  <action>
    CancelAuthorizer 를 직접 인스턴스화하는 순수 단위 테스트(@ExtendWith 불필요, Spring/Mockito 없이 primitive 인자)로 인가 매트릭스를 exhaustive 하게 커버한다 (D-P3-2): (1) role "ADMIN" → 예외 없이 통과(대상 merchantId 무관), (2) role "MERCHANT" + headerMerchantId==targetMerchantId → 통과, (3) role "MERCHANT" + headerMerchantId!=targetMerchantId → CancelNotAuthorizedException, (4) role "MERCHANT" + headerMerchantId==null(헤더 누락) → CancelNotAuthorizedException, (5) role "USER"(소유 여부 무관) → CancelNotAuthorizedException, (6) role null(누락) → CancelNotAuthorizedException. 던져진 예외의 errorCode 가 FORBIDDEN_PAYMENT(403) 임을 assert 한다.
  </action>
  <verify>
    <automated>./gradlew :payment-service:test --tests '*CancelAuthorizerTest'</automated>
  </verify>
  <done>6종 매트릭스 초록. USER 는 소유자여도 403 임이 명시적으로 검증됨.</done>
</task>

<task type="auto">
  <name>Task 2: CancelAuthorizationService 오케스트레이션 단위 (로드/404/위임/생략)</name>
  <files>payment-service/src/test/java/com/example/payment/application/service/CancelAuthorizationServiceTest.java</files>
  <action>
    @ExtendWith(MockitoExtension.class) + PaymentRepository mock 으로 오케스트레이션을 검증한다: (1) ADMIN AuthenticatedUser → authorize 통과 + verify(paymentRepository, never()).findByPaymentKey(any()) 로 로드 생략 증명 (D-P3-5), (2) MERCHANT + 헤더 merchantId 가 로드한 payment.merchantId 와 일치 → 통과 + findByPaymentKey 1회 호출, (3) MERCHANT + 불일치 → CancelNotAuthorizedException(403), (4) MERCHANT 인데 findByPaymentKey 가 empty → PaymentNotFoundException(404), (5) USER → 로드 없이 403, (6) X-Merchant-Id 가 숫자가 아닌 비정상 값 → NumberFormatException 이 500 이 아니라 403(CancelNotAuthorizedException)으로 흡수됨. Payment 는 Payment.reconstruct/of 픽스처로 원하는 merchantId 를 세팅한다.
  </action>
  <verify>
    <automated>./gradlew :payment-service:test --tests '*CancelAuthorizationServiceTest'</automated>
  </verify>
  <done>ADMIN 로드 생략, MERCHANT match/mismatch, 404, 비정상 헤더 403 흡수가 모두 검증됨.</done>
</task>

<task type="auto">
  <name>Task 3: CancelController ADMIN pass-through 확장 + 인가 정책 문서화</name>
  <files>
    payment-service/src/test/java/com/example/payment/presentation/controller/CancelControllerTest.java,
    docs/domain-rules.md,
    docs/api-spec.md
  </files>
  <action>
    CancelControllerTest 에 ADMIN pass-through 테스트를 추가한다: 실제 CancelAuthorizationService(실제 CancelAuthorizer + mock PaymentRepository) 를 배선하고 cancelPaymentUseCase 는 mock. "X-User-Role: ADMIN" 헤더로 /cancel POST → 200 + verify(cancelPaymentUseCase).cancel(...) 이 1회 호출됨을 검증 → 권한 role 취소가 인가 통과 후 기존 취소 플로우(멱등성·TX 불변)로 진입함을 컨트롤러 레벨에서 증명 (SC#1). 비고: 서비스 레이어 CancelFlowIntegrationTest 는 cancelPaymentService.cancel() 을 직접 호출(컨트롤러 미경유)하므로 인가 pre-check 영향 밖 — 무변경으로 초록 유지되며 그 자체가 취소 코어 불변 증거다(수정하지 않는다).

    docs/domain-rules.md 에 "취소 인가(AUTHZ-01)" 섹션을 append 한다(기존 취소 규칙 항목은 불변): ADMIN=전체 허용, MERCHANT=X-Merchant-Id==payment.merchantId 일치 시 허용, USER·role 누락=403 (D-P3-2). payment 는 게이트웨이 신뢰 헤더 role 만 신뢰·JWT 재검증 없음 (D-P3-3). X-User-Id 는 인가 미사용, 감사 로깅 전용 (D-P3-7). 신뢰 경계: payment(8080) 직접 도달 스푸핑 방어는 k3s NetworkPolicy 로 배포 시점 이관하며 코드는 게이트웨이 경유를 가정한다 — NetworkPolicy 부재 시 X-User-Role 위조로 전량 취소 가능하므로 배포 전 필수 게이트 (D-P3-6). docs/api-spec.md 취소 API 응답에 무권한 시 403 FORBIDDEN_PAYMENT note 를 append 한다(에러코드는 기존 error-catalog 재사용, 신규 추가 없음).
  </action>
  <verify>
    <automated>./gradlew :payment-service:test --tests '*CancelControllerTest'</automated>
    <automated>BASE=$(git merge-base HEAD origin/main); ! git diff --name-only "$BASE" -- payment-service/src/main | grep -E '(CancelPaymentService|CancelTxWriter|CancelDomainService|CancelHistoryRecorder)\.java|/scheduler/|/messaging/'</automated>
  </verify>
  <done>ADMIN 취소가 200 + cancel 1회 호출로 기존 플로우 진입 증명(SC#1). domain-rules.md 에 인가 정책·신뢰 경계 명문화. 취소 코어 파일 diff 0 유지.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway → payment | 신뢰 헤더 role 이 인가 판정으로 유입 |
| MERCHANT → 타 가맹점 payment | 소유권 경계 (X-Merchant-Id vs payment.merchantId) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Elevation of Privilege | MERCHANT 가 타 가맹점 결제 취소 | high | mitigate | X-Merchant-Id==payment.merchantId 소유권 검증, 불일치/누락 403. CancelAuthorizerTest(mismatch/누락) + CancelAuthorizationServiceTest(로드 후 비교)로 검증 |
| T-03-01 | Spoofing/Elevation | payment 직접 도달 헤더 위조 (배포 이관) | high | transfer | domain-rules.md 에 NetworkPolicy 필수 게이트·미이관 리스크 명문화(Task 3). 코드 phase 스코프 밖(D-P3-6) |
</threat_model>

<verification>
- `./gradlew :payment-service:test` 풀 스위트 초록 (신규 authz 테스트 + 기존 취소 스위트 회귀 없음)
- 취소 코어 불변 게이트: `BASE=$(git merge-base HEAD origin/main); git diff --name-only "$BASE" -- payment-service/src/main` 에 취소 실행 코어 파일(서비스/TX writer/도메인 서비스/이력/스케줄러/messaging) 부재
- docs/domain-rules.md 에 취소 인가 정책 섹션 존재
</verification>

<success_criteria>
- SC#1: ADMIN 취소가 인가 통과 후 기존 취소 플로우로 정상 처리(cancel 호출 도달) — 컨트롤러 테스트로 증명
- SC#2: 무권한(MERCHANT mismatch/USER/누락) 403 — 매트릭스 + 서비스 테스트로 exhaustive 증명
- SC#3: role-only 인가, JWT 재검증 없음 — spring-security 의존 부재 + 신뢰 헤더 소비만
</success_criteria>

<output>
Create `.planning/workstreams/auth-gateway/phases/03-payment/03-02-SUMMARY.md` when done
</output>
