---
phase: 04-deploy-manifests
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - infra/k8s/apps/user-service.yaml
  - infra/k8s/apps/config.yaml
autonomous: true
requirements: [DEPLOY-01, DEPLOY-03]
must_haves:
  truths:
    - "user-service.yaml(Deployment+Service)가 kubectl --dry-run=client 통과 — DEPLOY-01"
    - "user-service가 04-01의 jwt-secret Secret과 동일 JWT_SECRET을 secretKeyRef로 주입받아 gateway와 대칭키 공유 — DEPLOY-03"
    - "user_db가 기존 외부 MySQL 스냅샷 컨벤션(*_DB_URL + db-cred 계정)으로 주입되고 매니페스트에 평문 시크릿 없음"
  artifacts:
    - infra/k8s/apps/user-service.yaml
  key_links:
    - "user-service env JWT_SECRET → secretKeyRef(jwt-secret.JWT_SECRET) — 04-01 gateway와 동일 Secret(D-P4-4)"
    - "user-service env SPRING_DATASOURCE_URL → configMapKeyRef(app-config.USER_DB_URL); USER/PASS → secretKeyRef(db-cred)(D-P4-3)"
    - "SPRING_PROFILES_ACTIVE=prod → application.yml 기본 profiles.active:local 무력화 → localhost:3315 대신 USER_DB_URL, ${JWT_SECRET} fail-fast"
---

<objective>
확장: user-service를 기존 배포 컨벤션으로 배포 가능하게 하고(D-P4-1/3), 04-01의 jwt-secret Secret을 동일 값으로 공유 주입해 gateway↔user-service 대칭키 일치를 보장한다(DEPLOY-03). user_db는 외부 MySQL 스키마 추가 컨벤션(USER_DB_URL + db-cred user 계정)으로 연결한다.

Purpose: 신규 2서비스 중 상태 서비스(user-service)의 배포 계약(DB env·JWT 공유)을 완성한다.
Output: infra/k8s/apps/user-service.yaml, config.yaml에 USER_DB_URL + db-cred USER 계정 추가.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/auth-gateway/phases/04-deploy-manifests/04-RESEARCH.md
@user-service/src/main/resources/application.yml
@infra/k8s/apps/order.yaml
@infra/k8s/apps/config.yaml
@.planning/workstreams/auth-gateway/phases/04-deploy-manifests/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: config.yaml에 USER_DB_URL + db-cred USER 계정 추가</name>
  <files>infra/k8s/apps/config.yaml</files>
  <action>ConfigMap app-config data에 USER_DB_URL 추가(D-P4-3). 값은 기존 *_DB_URL 컨벤션을 따른 외부 MySQL user_db: "jdbc:mysql://10.0.1.30:3306/user_db?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true"(호스트 10.0.1.30 선택은 Claude 재량 — 기존 payment 호스트에 user_db 스키마 추가; 다른 노드로 배치 시 이 값만 조정하는 calibration knob). Secret db-cred stringData에 USER_DB_USER "user", USER_DB_PASS "user" 추가(db-cred는 서비스별 개별 계정을 쓰므로 재사용이 아닌 user 전용 계정 추가 — user-service local 기본값 user/user와 일치). 04-01이 추가한 GATEWAY_DOWNSTREAM_* 키 및 기존 취소 코어 4서비스 키·Secret 로직은 불변(D-P4-8).</action>
  <verify>
    <automated>{ if command -v kubeconform >/dev/null 2>&1; then kubeconform -strict -ignore-missing-schemas infra/k8s/apps/config.yaml; else python3 -c 'import yaml,sys;[list(yaml.safe_load_all(open(f))) for f in sys.argv[1:]]' infra/k8s/apps/config.yaml; fi; } && grep -q "USER_DB_URL" infra/k8s/apps/config.yaml && grep -q "USER_DB_USER" infra/k8s/apps/config.yaml && grep -q "GATEWAY_DOWNSTREAM_PAYMENT_URI" infra/k8s/apps/config.yaml && echo CONFIG_OK</automated>
  </verify>
  <done>config.yaml이 dry-run 통과하고 USER_DB_URL·USER_DB_USER·USER_DB_PASS가 존재하며 04-01의 downstream 키가 보존된다.</done>
</task>

<task type="auto">
  <name>Task 2: infra/k8s/apps/user-service.yaml (Deployment+Service, JWT/DB env 주입)</name>
  <files>infra/k8s/apps/user-service.yaml</files>
  <action>order.yaml 컨벤션으로 user-service.yaml 작성(D-P4-1). Deployment name=user-service, ns default, label app: user-service, replicas 2, image camelia9999/cancel-loadtest:user-service-latest, imagePullPolicy Always, containerPort 8085. env: SPRING_PROFILES_ACTIVE 값 prod(중요 — application.yml 기본 profiles.active:local을 무력화; 미설정 시 localhost:3315 + dev 기본 시크릿으로 떠 배포 실패/보안 결함), JWT_SECRET valueFrom secretKeyRef name jwt-secret key JWT_SECRET(04-01과 동일 Secret 공유, D-P4-4), SPRING_DATASOURCE_URL valueFrom configMapKeyRef name app-config key USER_DB_URL, SPRING_DATASOURCE_USERNAME valueFrom secretKeyRef name db-cred key USER_DB_USER, SPRING_DATASOURCE_PASSWORD valueFrom secretKeyRef name db-cred key USER_DB_PASS(D-P4-3, payment/order.yaml의 DataSource 주입 패턴 동일). envFrom app-config는 redis/kafka 등 user-service가 안 쓰는 키뿐이라 생략 가능(넣어도 무해, 최소로 생략). readinessProbe /actuator/health/readiness port 8085, livenessProbe /actuator/health/liveness port 8085. resources requests cpu 500m memory 1Gi. Service name=user-service selector app: user-service port 8085 targetPort 8085. 매니페스트에 평문 시크릿·비밀번호 리터럴을 두지 않는다(모두 secretKeyRef, DEPLOY-03).</action>
  <verify>
    <automated>{ if command -v kubeconform >/dev/null 2>&1; then kubeconform -strict -ignore-missing-schemas infra/k8s/apps/user-service.yaml; else python3 -c 'import yaml,sys;[list(yaml.safe_load_all(open(f))) for f in sys.argv[1:]]' infra/k8s/apps/user-service.yaml; fi; } && grep -q "name: jwt-secret" infra/k8s/apps/user-service.yaml && grep -q "USER_DB_URL" infra/k8s/apps/user-service.yaml && grep -q "SPRING_PROFILES_ACTIVE" infra/k8s/apps/user-service.yaml && grep -A1 "SPRING_DATASOURCE_PASSWORD" infra/k8s/apps/user-service.yaml | grep -q "secretKeyRef" && echo USERSVC_OK</automated>
  </verify>
  <done>user-service.yaml이 dry-run 통과, JWT_SECRET을 04-01과 동일 jwt-secret Secret에서 받고, DB 자격은 db-cred/app-config 참조로만 주입되며 평문 비밀번호 리터럴이 없다(DEPLOY-01, DEPLOY-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-service → 외부 MySQL(user_db) | DB 자격이 주입되는 경계 |
| jwt-secret Secret → user-service·api-gateway | 대칭키 공유 경계(값 불일치 시 전면 401) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Information Disclosure | user-service.yaml DB 자격 | high | mitigate | USER/PASS를 db-cred secretKeyRef로만 주입, 매니페스트에 평문 비밀번호 리터럴 부재 grep 게이트(DEPLOY-03) |
| T-04-05 | Spoofing | gateway↔user 대칭키 불일치/dev키 | high | mitigate | 동일 jwt-secret Secret 공유 + SPRING_PROFILES_ACTIVE=prod fail-fast |
| T-04-06 | Tampering | 미인증 트래픽의 user-service 직접 접근 | low | accept | user-service는 공개 경로(signup/login/refresh) 제공 서비스 — 게이트웨이 경유 원칙이나 이 phase는 payment 경계만 강제(Open Question 2, default-deny는 범위 밖) |
</threat_model>

<verification>
- user-service.yaml + config.yaml kubectl --dry-run=client 통과
- JWT_SECRET secretKeyRef name jwt-secret(04-01과 동일 Secret)
- SPRING_DATASOURCE_URL ← USER_DB_URL(app-config), USER/PASS ← db-cred
- SPRING_PROFILES_ACTIVE 비-local, 매니페스트 평문 비밀번호 리터럴 없음
</verification>

<success_criteria>
user-service가 기존 컨벤션 매니페스트로 배포 가능하고 정적 검증을 통과하며, gateway와 동일 JWT_SECRET을 Secret 공유로 주입받고 DB 자격에 평문이 없다(DEPLOY-01, DEPLOY-03 완성).
</success_criteria>

<output>
Create `.planning/workstreams/auth-gateway/phases/04-deploy-manifests/04-02-SUMMARY.md` when done
</output>
