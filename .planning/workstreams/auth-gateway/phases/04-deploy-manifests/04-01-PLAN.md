---
phase: 04-deploy-manifests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api-gateway/Dockerfile
  - infra/k8s/apps/api-gateway.yaml
  - infra/k8s/apps/jwt-secret.yaml
  - infra/k8s/apps/config.yaml
autonomous: true
requirements: [DEPLOY-01, DEPLOY-03]
must_haves:
  truths:
    - "api-gateway 이미지가 신규 Dockerfile로 실제 빌드된다(docker build exit 0) — DEPLOY-01"
    - "api-gateway.yaml(Deployment+Service+Ingress)가 kubectl --dry-run=client 통과, gateway가 단일 진입점(/) Ingress 보유 — DEPLOY-01"
    - "gateway에 JWT_SECRET이 secretKeyRef(jwt-secret)로 주입되고 매니페스트에 평문 시크릿 없음 — DEPLOY-03"
    - "gateway가 비-local 프로파일로 기동해 ${JWT_SECRET} fail-fast + downstream URI env override가 적용된다"
  artifacts:
    - api-gateway/Dockerfile
    - infra/k8s/apps/api-gateway.yaml
    - infra/k8s/apps/jwt-secret.yaml
  key_links:
    - "Deployment env JWT_SECRET → secretKeyRef(jwt-secret.JWT_SECRET) — user-service와 동일 Secret 공유(D-P4-4)"
    - "Deployment env GATEWAY_DOWNSTREAM_PAYMENT_URI=http://payment:8080 → gateway.downstream.payment-uri 릴랙스드 바인딩 override(D-P4-6)"
    - "SPRING_PROFILES_ACTIVE=prod → application.yml 기본 profiles.active:local 무력화 → jwt.secret ${JWT_SECRET} fail-fast 경로 활성"
---

<objective>
tracer 슬라이스: api-gateway를 빌드→이미지→Deployment→Service→Ingress→Secret/Config까지 한 경로로 관통해 배포 가능함을 정적으로 증명한다(D-P4-2/4/6). 신규 Dockerfile을 실제 빌드하고, api-gateway 매니페스트가 dry-run 통과하며, JWT_SECRET이 Secret 참조로만 주입(평문 없음, DEPLOY-03)됨을 확인한다.

Purpose: 이후 확장(user-service·NetworkPolicy)이 딛고 갈 배포 계약(이미지 태그·Secret 이름·downstream env·프로파일)을 가장 강한 게이트(docker build)로 먼저 못박는다.
Output: api-gateway/Dockerfile, infra/k8s/apps/api-gateway.yaml, infra/k8s/apps/jwt-secret.yaml, config.yaml에 gateway downstream URI 추가.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/auth-gateway/phases/04-deploy-manifests/04-RESEARCH.md
@user-service/Dockerfile
@api-gateway/src/main/resources/application.yml
@infra/k8s/apps/order.yaml
@infra/k8s/apps/config.yaml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: api-gateway/Dockerfile 신규 (user-service 패턴 복사, EXPOSE 8000)</name>
  <files>api-gateway/Dockerfile</files>
  <action>user-service/Dockerfile를 그대로 복사해 api-gateway용으로 조정한다(D-P4-2). 멀티스테이지 유지: builder는 gradle:8-jdk21, 루트 build.gradle·settings.gradle·gradle/ + 서브모듈 build.gradle 복사(캐시 레이어) 후 `gradle :api-gateway:dependencies --no-daemon -q` → `COPY api-gateway/src api-gateway/src` → `gradle :api-gateway:bootJar --no-daemon -x test`. runtime은 eclipse-temurin:21-jre-alpine, `COPY --from=builder /app/api-gateway/build/libs/*.jar app.jar`, EXPOSE 8000, ENTRYPOINT java -jar app.jar. 중요: user-service Dockerfile의 서브모듈 build.gradle COPY 목록(payment/order/merchant-limit/risk/product/user)에 **`COPY api-gateway/build.gradle api-gateway/`를 반드시 추가**한다 — settings.gradle이 api-gateway를 include하므로 builder 컨텍스트에 api-gateway/build.gradle이 없으면 `:api-gateway:bootJar`가 task-not-found로 첫 빌드부터 실패. dependencies/bootJar 타깃만 :api-gateway로 교체.
  또한 executor는 이 phase 첫 task 실행 전 `PHASE4_BASE=$(git rev-parse HEAD)`를 캡처해 이후 Java 무변경 게이트(04-03 T2)의 diff base로 쓴다(execute-phase가 phase 시작 SHA 제공).</action>
  <verify>
    <automated>docker build -t api-gateway:local -f api-gateway/Dockerfile . && echo BUILD_OK</automated>
  </verify>
  <done>docker build가 exit 0으로 완료되고 api-gateway:local 이미지가 생성된다(DEPLOY-01).</done>
</task>

<task type="auto">
  <name>Task 2: infra/k8s/apps/api-gateway.yaml (Deployment+Service+Ingress) + jwt-secret.yaml Secret</name>
  <files>infra/k8s/apps/api-gateway.yaml, infra/k8s/apps/jwt-secret.yaml</files>
  <action>order.yaml 컨벤션을 따라 api-gateway.yaml을 작성한다(D-P4-1). Deployment name=api-gateway, ns default, label app: api-gateway, replicas 2, image camelia9999/cancel-loadtest:api-gateway-latest, imagePullPolicy Always, containerPort 8000. api-gateway는 무상태이므로 DataSource env 없음(D-P4-6). env로: SPRING_PROFILES_ACTIVE 값 prod(중요 — application.yml 기본 profiles.active:local을 무력화해야 비-local 경로의 jwt.secret ${JWT_SECRET} fail-fast + downstream override가 활성; 값 미설정 시 local 프로파일로 떠 dev 기본 시크릿 사용됨), JWT_SECRET을 valueFrom secretKeyRef name jwt-secret key JWT_SECRET(D-P4-4), GATEWAY_DOWNSTREAM_PAYMENT_URI 값 http://payment:8080, GATEWAY_DOWNSTREAM_USER_URI 값 http://user-service:8085(D-P4-6, 릴랙스드 바인딩으로 gateway.downstream.*-uri override). envFrom app-config는 gateway가 소비하는 키가 없으므로 생략 가능(넣어도 무해하나 최소로 생략). readinessProbe httpGet /actuator/health/readiness port 8000, livenessProbe /actuator/health/liveness port 8000(order.yaml delay 값 재사용). resources requests cpu 500m memory 1Gi. Service name=api-gateway selector app: api-gateway port 8000 targetPort 8000. Ingress name=api-gateway(networking.k8s.io/v1) rule path / pathType Prefix backend service api-gateway port 8000 — gateway가 단일 외부 진입점(D-P4-5, payment Ingress는 04-03에서 제거·이관). jwt-secret.yaml: Secret name jwt-secret ns default type Opaque, stringData JWT_SECRET 값은 배포 시 교체할 placeholder 토큰 REPLACE_ME_AT_DEPLOY, 위에 배포 시 실제 256-bit 대칭키를 주입하라는 주석 1줄(DEPLOY-03 — 실 값 하드코딩 금지). user-service와 이 동일 Secret을 공유해 HS256 대칭키 값 일치를 보장(불일치 시 전면 401).</action>
  <verify>
    <automated>{ if command -v kubeconform >/dev/null 2>&1; then kubeconform -strict -ignore-missing-schemas infra/k8s/apps/jwt-secret.yaml infra/k8s/apps/api-gateway.yaml; else python3 -c 'import yaml,sys;[list(yaml.safe_load_all(open(f))) for f in sys.argv[1:]]' infra/k8s/apps/jwt-secret.yaml infra/k8s/apps/api-gateway.yaml; fi; } && grep -q "secretKeyRef" infra/k8s/apps/api-gateway.yaml && grep -q "name: jwt-secret" infra/k8s/apps/api-gateway.yaml && grep -q "REPLACE_ME_AT_DEPLOY" infra/k8s/apps/jwt-secret.yaml && ! grep -Eiq "JWT_SECRET: \"?[A-Za-z0-9+/]{32,}" infra/k8s/apps/jwt-secret.yaml && echo GATE_OK</automated>
  </verify>
  <done>두 매니페스트가 dry-run 통과, gateway가 JWT_SECRET을 jwt-secret Secret 참조로만 받고(평문 없음), Ingress가 api-gateway:8000 단일 진입점을 노출한다(DEPLOY-01, DEPLOY-03).</done>
</task>

<task type="auto">
  <name>Task 3: config.yaml app-config에 gateway downstream URI 추가</name>
  <files>infra/k8s/apps/config.yaml</files>
  <action>ConfigMap app-config data에 GATEWAY_DOWNSTREAM_PAYMENT_URI "http://payment:8080", GATEWAY_DOWNSTREAM_USER_URI "http://user-service:8085" 키를 추가한다(D-P4-6). 참고: Task 2에서 이 값들을 Deployment env value로 이미 직접 주입했으므로 gateway 기동에는 ConfigMap 경유가 필수는 아니지만, 다른 서비스 URL(EXTERNAL_*_URL)과 같은 위치에 downstream URI를 함께 두어 배포 설정 단일 출처를 유지한다. db-cred/기타 키는 이 task에서 건드리지 않는다(USER_DB는 04-02). 기존 취소 코어 4서비스 관련 키·Secret 로직 불변(D-P4-8).</action>
  <verify>
    <automated>{ if command -v kubeconform >/dev/null 2>&1; then kubeconform -strict -ignore-missing-schemas infra/k8s/apps/config.yaml; else python3 -c 'import yaml,sys;[list(yaml.safe_load_all(open(f))) for f in sys.argv[1:]]' infra/k8s/apps/config.yaml; fi; } && grep -q "GATEWAY_DOWNSTREAM_PAYMENT_URI" infra/k8s/apps/config.yaml && grep -q "GATEWAY_DOWNSTREAM_USER_URI" infra/k8s/apps/config.yaml && echo CONFIG_OK</automated>
  </verify>
  <done>config.yaml이 dry-run 통과하고 gateway downstream URI 2개 키가 app-config에 존재한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 클라이언트 → Ingress → api-gateway | 유일 외부 진입점. 미인증 트래픽이 여기서 처음 신뢰 경계에 진입 |
| 매니페스트(git) → 클러스터 Secret | JWT 대칭키가 저장/주입되는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | jwt-secret.yaml stringData | high | mitigate | 매니페스트에 placeholder(REPLACE_ME_AT_DEPLOY)만, 실 값은 배포 시 주입 — grep 게이트로 평문 대칭키 부재 강제(DEPLOY-03) |
| T-04-02 | Spoofing | JWT 검증 우회(dev 기본 시크릿 기동) | high | mitigate | SPRING_PROFILES_ACTIVE=prod로 local 프로파일 무력화 → ${JWT_SECRET} fail-fast, dev 기본키 사용 불가 |
| T-04-03 | Tampering | gateway 우회한 downstream 직접 접근 | medium | transfer | payment 직접 도달 차단은 04-03 NetworkPolicy(DEPLOY-02)로 이관 |
</threat_model>

<verification>
- docker build -f api-gateway/Dockerfile exit 0
- api-gateway.yaml + jwt-secret.yaml + config.yaml 전부 kubectl --dry-run=client 통과
- gateway가 JWT_SECRET secretKeyRef(name jwt-secret)로만 주입, jwt-secret.yaml에 평문 대칭키 없음(placeholder만)
- SPRING_PROFILES_ACTIVE 비-local + downstream URI env 존재
</verification>

<success_criteria>
api-gateway가 신규 Dockerfile로 빌드되고, 그 배포 매니페스트(Deployment+Service+Ingress)가 정적 검증을 통과하며, JWT_SECRET이 평문 없이 Secret 참조로 주입된다(DEPLOY-01, DEPLOY-03 tracer 커버).
</success_criteria>

<output>
Create `.planning/workstreams/auth-gateway/phases/04-deploy-manifests/04-01-SUMMARY.md` when done
</output>
