---
phase: 04-deploy-manifests
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - infra/k8s/networkpolicy/payment-ingress.yaml
  - infra/k8s/apps/payment.yaml
  - infra/k8s/deploy.sh
autonomous: true
requirements: [DEPLOY-02]
must_haves:
  truths:
    - "payment 8080 ingress가 NetworkPolicy로 app: api-gateway 파드에서만 허용된다 — DEPLOY-02"
    - "payment.yaml에서 기존 Ingress가 제거되어 gateway가 단일 외부 진입점이 된다 — DEPLOY-02"
    - "deploy.sh가 config/secret → 신규 서비스 → networkpolicy 순서로 신규 매니페스트를 배포한다"
    - "취소 코어 Java 소스는 무변경(이 phase는 인프라 YAML만) — D-P4-8"
  artifacts:
    - infra/k8s/networkpolicy/payment-ingress.yaml
  key_links:
    - "NetworkPolicy podSelector app: payment ← ingress from podSelector app: api-gateway port 8080(D-P4-5)"
    - "payment.yaml Ingress 제거 → api-gateway.yaml Ingress(04-01)로 진입점 이관"
    - "deploy.sh apply 순서: config.yaml/jwt-secret.yaml → user-service.yaml/api-gateway.yaml → networkpolicy/payment-ingress.yaml"
---

<objective>
배포 시점 보안 게이트를 매니페스트로 강제한다(D-P4-5). payment의 8080 ingress를 NetworkPolicy로 api-gateway 파드에서만 허용해 헤더 스푸핑용 payment 직접 도달을 차단(DEPLOY-02)하고, 기존 payment `/` Ingress를 제거해 gateway를 단일 진입점으로 이관하며, deploy.sh 배포 순서에 신규 매니페스트를 편입한다.

Purpose: 게이트웨이 단일 검증 원칙(Phase 2/3)을 배포 토폴로지로 물리적으로 봉인한다.
Output: infra/k8s/networkpolicy/payment-ingress.yaml(신규), payment.yaml Ingress 제거, deploy.sh 순서 갱신.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/auth-gateway/phases/04-deploy-manifests/04-RESEARCH.md
@infra/k8s/apps/payment.yaml
@infra/k8s/deploy.sh
@.planning/workstreams/auth-gateway/phases/04-deploy-manifests/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NetworkPolicy 신규 — payment 8080 ingress를 api-gateway에서만 허용</name>
  <files>infra/k8s/networkpolicy/payment-ingress.yaml</files>
  <action>networking.k8s.io/v1 NetworkPolicy를 작성한다(D-P4-5, DEPLOY-02). name=payment-allow-gateway, ns default. spec.podSelector matchLabels app: payment(대상은 payment 파드). policyTypes에 Ingress. spec.ingress 한 항목: from podSelector matchLabels app: api-gateway, ports protocol TCP port 8080 — 즉 app: api-gateway 파드만 payment:8080에 인그레스 허용, 그 외 파드는 거부. k3s 기본 flannel + 내장 NetworkPolicy controller 활성이라 실제 강제됨(정찰 확인). egress 정책은 추가하지 않는다(gateway→payment/user egress는 default-deny를 도입하지 않는 한 이미 허용 — 최소 정책, Open Question 2). NetworkPolicy는 podSelector에 매칭되는 파드에만 적용되므로 다른 서비스 트래픽에 영향 없음.</action>
  <verify>
    <automated>{ if command -v kubeconform >/dev/null 2>&1; then kubeconform -strict -ignore-missing-schemas infra/k8s/networkpolicy/payment-ingress.yaml; else python3 -c 'import yaml,sys;[list(yaml.safe_load_all(open(f))) for f in sys.argv[1:]]' infra/k8s/networkpolicy/payment-ingress.yaml; fi; } && grep -q "kind: NetworkPolicy" infra/k8s/networkpolicy/payment-ingress.yaml && grep -q "app: payment" infra/k8s/networkpolicy/payment-ingress.yaml && grep -q "app: api-gateway" infra/k8s/networkpolicy/payment-ingress.yaml && grep -q "port: 8080" infra/k8s/networkpolicy/payment-ingress.yaml && echo NETPOL_OK</automated>
  </verify>
  <done>NetworkPolicy가 dry-run 통과하고 podSelector app: payment + ingress from app: api-gateway port 8080 구조를 갖는다(DEPLOY-02).</done>
</task>

<task type="auto">
  <name>Task 2: payment.yaml Ingress 이관(제거) + deploy.sh 순서 갱신</name>
  <files>infra/k8s/apps/payment.yaml, infra/k8s/deploy.sh</files>
  <action>payment.yaml에서 맨 아래 Ingress 리소스 블록(networking.k8s.io/v1, name payment, path /)과 그 앞 `---` 구분자를 삭제한다 — 진입점을 04-01 api-gateway Ingress로 이관(D-P4-5). Deployment·Service 및 노드풀/anti-affinity/preStop 등 payment 배포 스펙 로직은 일절 불변(D-P4-8, Ingress 이관에만 한정). deploy.sh: (a) redis/config.yaml apply 라인에 jwt-secret.yaml을 함께 apply(config/secret 단계), (b) 앱 배포 라인에 user-service.yaml·api-gateway.yaml 추가하고 rollout status 대상에 deploy/user-service deploy/api-gateway 추가, (c) 앱 rollout 이후 networkpolicy/payment-ingress.yaml을 apply하는 라인 신설. 순서: strimzi/redis → config+jwt-secret → 6개 앱(기존4 + user-service + api-gateway) → networkpolicy. Strimzi 사전설치 전제 등 기존 주석·set -euo pipefail 유지.</action>
  <verify>
    <automated>{ if command -v kubeconform >/dev/null 2>&1; then kubeconform -strict -ignore-missing-schemas infra/k8s/apps/payment.yaml; else python3 -c 'import yaml,sys;[list(yaml.safe_load_all(open(f))) for f in sys.argv[1:]]' infra/k8s/apps/payment.yaml; fi; } && ! grep -q "kind: Ingress" infra/k8s/apps/payment.yaml && grep -q "user-service.yaml" infra/k8s/deploy.sh && grep -q "api-gateway.yaml" infra/k8s/deploy.sh && grep -q "jwt-secret.yaml" infra/k8s/deploy.sh && grep -q "payment-ingress.yaml" infra/k8s/deploy.sh && test -z "$(git diff --name-only "${PHASE4_BASE:?set PHASE4_BASE=\$(git rev-parse HEAD) at phase start}"..HEAD -- '*.java')" && echo INGRESS_MOVED_OK</automated>
  </verify>
  <done>payment.yaml에서 진입점 리소스가 제거되어 dry-run 통과(Deployment/Service 불변), deploy.sh가 jwt-secret·신규 2서비스·networkpolicy를 순서대로 배포하며, 취소 코어 Java 무변경(DEPLOY-02, D-P4-8).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클러스터 내부 파드 → payment:8080 | NetworkPolicy가 걸리는 경계. api-gateway 외 파드의 직접 도달 = 신뢰 헤더 스푸핑 벡터 |
| 외부 → Ingress | payment `/` 제거 후 api-gateway가 유일 진입점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Spoofing | payment 직접 도달로 X-User-Role 헤더 위조 | high | mitigate | NetworkPolicy로 payment:8080을 app: api-gateway 파드만 허용 — gateway 우회 경로 차단(DEPLOY-02) |
| T-04-08 | Elevation of Privilege | payment `/` Ingress 잔존 시 gateway 우회 외부 진입 | high | mitigate | payment.yaml Ingress 제거, gateway Ingress 단일화 — grep으로 payment 진입점 리소스 부재 강제 |
| T-04-09 | Tampering | 인프라 변경이 취소 코어 로직 변경으로 번짐 | high | mitigate | git diff `*.java` 무변경 게이트, payment 변경은 Ingress 이관에 한정(D-P4-8) |
</threat_model>

<verification>
- payment-ingress.yaml + payment.yaml 오프라인 검증(kubeconform 또는 python yaml 파싱) 통과 — 클러스터 비의존
- NetworkPolicy 구조: podSelector app: payment, ingress from app: api-gateway, port 8080
- payment.yaml에 진입점 리소스 부재(`! grep "kind: Ingress"`)
- deploy.sh가 jwt-secret·user-service·api-gateway·networkpolicy를 순서대로 참조
- 취소 코어 Java 무변경(`git diff "$PHASE4_BASE"..HEAD -- '*.java'` 빈 결과 — PHASE4_BASE=phase 시작 SHA, 미설정 시 게이트가 실패)
</verification>

<success_criteria>
payment 8080 ingress가 NetworkPolicy로 gateway 파드에서만 허용되고 payment `/` 진입점이 gateway로 이관되어 단일 진입점이 성립하며, 배포 스크립트가 신규 매니페스트를 순서대로 편입한다(DEPLOY-02).
</success_criteria>

<output>
Create `.planning/workstreams/auth-gateway/phases/04-deploy-manifests/04-03-SUMMARY.md` when done
</output>
