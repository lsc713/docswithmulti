---
phase: 02-hardening
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - .planning/workstreams/payout/phases/02-hardening/02-03-VERIFICATION.md
autonomous: true
requirements: [INV-01]

must_haves:
  truths:
    - "git merge-base(HEAD, main) diff proves EVERY changed path is confined to settlement-service/ (+ .planning/ · docs/) — no payment/order/product/merchant-limit/risk-management/user/api-gateway path appears; payout hardening is settlement-only [INV-01]"
    - "Settlement Flyway is still exactly V1 + V2 (V2__create_payout.sql); NO V3+ was added (the DEAD terminal status is a new value in the existing status VARCHAR(20), needing no migration) and no payment (or any other module) migration was added [INV-01]"
    - "The full four-module no-regression suite (payment/order/product/settlement) is green — the PAY-02/RETRY-01 hardening touched none of the cancel core, stock path, order sync, or the existing settlement ledger/reconcile/Phase-1 payout convergence code [INV-01]"
  artifacts:
    - .planning/workstreams/payout/phases/02-hardening/02-03-VERIFICATION.md
  key_links:
    - "The DEAD-status path (RESEARCH fork A1, resolved) adds NO migration, so the Phase-1 gate's 'no settlement V-file above V2' assertion holds verbatim — do NOT relax it to allow V3"
    - "Duplicate .sql cleanup (cloud-sync) must run before any gradle build or Flyway sees a duplicate V-file → checksum failure; keep macOS-safe single-quoted globs"
---

<objective>
Re-validate INV-01 for Phase 2: the PAY-02 (409/race-loser) and RETRY-01 (retry/DEAD/alert-once) hardening added ONLY settlement-service code (plus planning/docs) — the payment cancel core and every other module stayed diff 0, settlement Flyway is still exactly V1+V2 (the DEAD terminal status needed no migration), and the full four-module no-regression suite is green.

This clones the Phase-1 gate (`01-03-PLAN.md`) VERBATIM. Per RESEARCH §6: because the resolved no-migration DEAD-status path was taken, the Phase-1 gate passes unchanged — it asserts `no settlement V-file above V2` and `no payment migration`, both still true (zero migrations added in Phase 2). The confinement regex `^(settlement-service/|\.planning/|docs/)` and the seven-module diff-0 denylist are identical.

Purpose: turn the Phase-2 structural claim ("payout hardening is a pure settlement-service change, no new schema") into a scripted, re-runnable merge-base gate, and confirm cancel/stock/order/settlement suites did not regress.
Output: a passing INV-01 gate + `02-03-VERIFICATION.md` recording the confinement result, the exactly-V2 Flyway assertion, and per-module suite results.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/payout/phases/02-hardening/RESEARCH.md
@.planning/workstreams/payout/REQUIREMENTS.md
@.planning/workstreams/payout/phases/01-paid-tracer/01-03-PLAN.md
@.planning/workstreams/payout/phases/02-hardening/02-01-PLAN.md
@.planning/workstreams/payout/phases/02-hardening/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: INV-01 merge-base gate — settlement-only confinement, exactly-V2 Flyway (no V3), no other-module diff</name>
  <files>.planning/workstreams/payout/phases/02-hardening/02-03-VERIFICATION.md</files>
  <precondition>Plans 02-01 and 02-02 are committed on feat/settlement-payout (branched from main), so `git merge-base HEAD main` is the correct base ref.</precondition>
  <action>
Run the INV-01 merge-base diff gate (clone `01-03-PLAN.md` Task 1 verbatim). Compute BASE = git merge-base HEAD main. (1) Confinement: assert every changed path vs BASE matches ^(settlement-service/|.planning/|docs/) — any other path (payment/order/product/merchant-limit/risk-management/user/api-gateway or root config) fails as NON_SETTLEMENT_DIFF. (2) Module denylist (belt-and-suspenders): assert the seven non-settlement module dirs have zero committed diff vs BASE. (3) Flyway: assert settlement added exactly V2 (V2__create_payout.sql exists, no settlement V-file above V2 — DEAD needs no migration) and that no payment (or other module) migration was added. Delete cloud-sync duplicate migration copies under build/ before running; use single-quoted glob pathspecs (BSD/macOS). Record the confinement result, the changed-file list, the exactly-V2 assertion, and gate PASS/FAIL in 02-03-VERIFICATION.md. Note explicitly in the record that the DEAD terminal status added zero migrations (fork A1 resolved to the no-migration path).
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /Users/juho/Documents/docswithmulti-payout; find . -path "*/build/*" -name "* [0-9].sql" -delete; BASE=$(git merge-base HEAD main); BAD=$(git diff $BASE...HEAD --name-only | grep -vE "^(settlement-service/|\.planning/|docs/)" || true); test -z "$BAD" || { echo "NON_SETTLEMENT_DIFF: $BAD"; exit 1; }; DENY=$(git diff $BASE...HEAD --name-only -- payment-service order-service product-service merchant-limit-service risk-management-service user-service api-gateway); test -z "$DENY" || { echo "MODULE_DIFF: $DENY"; exit 1; }; test -f settlement-service/src/main/resources/db/migration/V2__create_payout.sql || { echo "MISSING_V2"; exit 1; }; SV=$(ls settlement-service/src/main/resources/db/migration/ | grep -E "^V([3-9]|[1-9][0-9])__" || true); test -z "$SV" || { echo "UNEXPECTED_SETTLEMENT_MIGRATION: $SV"; exit 1; }; PM=$(git diff $BASE...HEAD --name-only -- "payment-service/src/main/resources/db/migration/*" || true); test -z "$PM" || { echo "PAYMENT_MIGRATION: $PM"; exit 1; }; echo INV01_PASS'</automated>
  </verify>
  <done>Gate prints INV01_PASS: every changed path is under settlement-service/ (+ .planning/ · docs/), the seven other module dirs are diff 0, settlement added exactly V2 (no V3+, no payment migration). Confinement + exactly-V2 scope recorded in 02-03-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite no-regression — payment/order/product/settlement green</name>
  <files>.planning/workstreams/payout/phases/02-hardening/02-03-VERIFICATION.md</files>
  <action>
Run the full no-regression suite after the duplicate-.sql cleanup: payment (cancel + stock reserve/release + convergence/idempotency ITs must stay green — proving the payout hardening perturbed nothing), order, product, and settlement (Phase 1/2/3 suites — config, sale/cancel ledger, reconcile, immutability, query — plus the payout ITs: tracer, poll, query, convergence, and the new Phase-2 PayoutApproveHardening + PayoutRetry). Append the module-by-module pass/fail summary to 02-03-VERIFICATION.md.
  </action>
  <verify>
    <automated>bash -c "cd /Users/juho/Documents/docswithmulti-payout && find . -path '*/build/*' -name '* [0-9].sql' -delete; ./gradlew :payment-service:test :order-service:test :product-service:test :settlement-service:test"</automated>
  </verify>
  <done>All four module suites pass. Existing cancel/stock/order and settlement ledger/reconcile/Phase-1 payout ITs show no regression; the new PayoutApproveHardening + PayoutRetry ITs are green. Summary in 02-03-VERIFICATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payout hardening → cancel core / other modules | risk that PAY-02/RETRY-01 edits perturb the payment cancel TX, schedulers, stock path, or any other module |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | undeclared change slipping into a non-settlement module | high | mitigate | merge-base confinement gate asserts every changed path is settlement-only + seven module dirs diff 0; runs in verify |
| T-02-07 | Availability | stray/duplicate Flyway version (parallel worktrees) or an accidental V3 | medium | mitigate | gate asserts exactly V2 in settlement (no V3 — DEAD needs no migration) and no payment migration; cloud-sync duplicate .sql purge before build |
| T-02-08 | Denial of Service | cloud-sync duplicate V .sql → Flyway checksum failure | medium | mitigate | `find . -path '*/build/*' -name '* [0-9].sql' -delete` before every build/gate |
| T-02-SC | Tampering | supply chain (new deps) | low | accept | zero new packages — Phase 2 reuses pre-pinned Redisson/web/JPA/Flyway/Mockito |
</threat_model>

<verification>
- INV-01 gate prints INV01_PASS (Task 1): settlement-only confinement + seven-module diff 0 + exactly-V2 Flyway (no V3) + no payment migration.
- `./gradlew :payment-service:test :order-service:test :product-service:test :settlement-service:test` all green (Task 2).
- 02-03-VERIFICATION.md records confinement diff scope, exactly-V2 assertion, and per-module suite results.
</verification>

<success_criteria>
- INV-01: Phase-2 payout diff confined to settlement-service/ (+ .planning/ · docs/); payment/order/product/merchant + risk/user/gateway diff 0; Flyway V2 only (V1 unchanged, no V3, no payment migration); full four-module suite green.
</success_criteria>

<output>
Create `.planning/workstreams/payout/phases/02-hardening/02-03-SUMMARY.md` when done.
</output>
