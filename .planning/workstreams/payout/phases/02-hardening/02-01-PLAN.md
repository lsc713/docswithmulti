---
phase: 02-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settlement-service/src/main/java/com/example/settlement/common/exception/ErrorCode.java
  - settlement-service/src/main/java/com/example/settlement/application/exception/PayoutAlreadyExistsException.java
  - settlement-service/src/main/java/com/example/settlement/presentation/GlobalExceptionHandler.java
  - settlement-service/src/main/java/com/example/settlement/application/service/PayoutService.java
  - settlement-service/src/test/java/com/example/settlement/integration/PayoutApproveHardeningIntegrationTest.java
  - docs/error-catalog.md
autonomous: true
requirements: [PAY-02]

must_haves:
  truths:
    - "Re-approving a settlement that already has a payout returns HTTP 409 (not 400/500) with a body carrying the EXISTING payout (id/status/amount) — sequential duplicate path [PAY-02]"
    - "Two concurrent approve() calls on the same FINALIZED settlement produce EXACTLY ONE payout row; the race-loser (uk_payout_settlement violation → DataIntegrityViolationException) returns 409 with the winning payout body, never 500 — no double-pay [PAY-02]"
    - "The three existing approve guards stay 400: not-FINALIZED, net≤0, no active account; missing settlement stays 404 [PAY-02]"
  artifacts:
    - settlement-service/src/main/java/com/example/settlement/application/exception/PayoutAlreadyExistsException.java
    - settlement-service/src/test/java/com/example/settlement/integration/PayoutApproveHardeningIntegrationTest.java
  key_links:
    - "The dedicated @ExceptionHandler(PayoutAlreadyExistsException) MUST out-rank the generic @ExceptionHandler(BusinessException) so the 409 body carries the payout — the generic handler only emits {code,message} and cannot carry it"
    - "approve() MUST stay non-@Transactional: @GeneratedValue(IDENTITY) forces the INSERT at save(), so the uk_payout_settlement violation surfaces as DataIntegrityViolationException INSIDE the try/catch. A @Transactional wrapper relocates the INSERT to commit and the catch would miss it → race-loser reverts to 500"
---

<objective>
Harden approve() so a duplicate/concurrent payout approval is a race-safe 409-return-existing instead of the Phase-1 placeholder 400 (sequential) / 500 (race-loser). This is PAY-02's "이중지급 차단": the settlement_id UK plus a DataIntegrityViolationException→409 catch guarantee exactly one payout per settlement, and both the pre-check path and the race-loser path return the existing payout with HTTP 409.

Purpose: close the verification-flagged gap (`01-VERIFICATION.md`: "Duplicate-approve throws PayoutNotPayableException (400) — NOT the Phase-2 409"; race-loser falls through to 500).
Output: `ErrorCode.PAYOUT_ALREADY_EXISTS` (409), `PayoutAlreadyExistsException` carrying the existing Payout, a dedicated 409-with-body handler, both approve() paths rewired, `docs/error-catalog.md` updated, and a new integration test covering sequential-duplicate + concurrent-race + the retained 400/404 guards.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/payout/phases/02-hardening/RESEARCH.md
@.planning/workstreams/payout/REQUIREMENTS.md
@settlement-service/src/main/java/com/example/settlement/application/service/PayoutService.java
@settlement-service/src/main/java/com/example/settlement/presentation/GlobalExceptionHandler.java
@settlement-service/src/main/java/com/example/settlement/common/exception/ErrorCode.java
@settlement-service/src/main/java/com/example/settlement/common/exception/BusinessException.java
@settlement-service/src/main/java/com/example/settlement/application/exception/PayoutNotPayableException.java
@settlement-service/src/main/java/com/example/settlement/presentation/dto/PayoutResponse.java
@settlement-service/src/main/java/com/example/settlement/domain/entity/Payout.java
@settlement-service/src/test/java/com/example/settlement/integration/PayoutTracerIntegrationTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PAYOUT_ALREADY_EXISTS (409) + PayoutAlreadyExistsException carrying the existing Payout + dedicated 409-with-body handler; document in error-catalog</name>
  <files>settlement-service/src/main/java/com/example/settlement/common/exception/ErrorCode.java, settlement-service/src/main/java/com/example/settlement/application/exception/PayoutAlreadyExistsException.java, settlement-service/src/main/java/com/example/settlement/presentation/GlobalExceptionHandler.java, docs/error-catalog.md</files>
  <action>
Add a 409 entry `PAYOUT_ALREADY_EXISTS("PAYOUT_ALREADY_EXISTS", 409, "이미 지급 건이 존재합니다.")` to `ErrorCode` (the enum currently has no 409 entry; place it in its own `// 409` section between the 401 and 404 groups to match the existing ordering-by-status convention).

Create `PayoutAlreadyExistsException extends BusinessException` in the `application.exception` package (mirror `PayoutNotPayableException`'s ctor style). Its constructor takes the existing `Payout` and calls `super(ErrorCode.PAYOUT_ALREADY_EXISTS, ...)`; it stores the `Payout` in a final field and exposes a `getExisting()` getter so the handler can build the body. Do NOT make it carry only a message — the existing payout MUST be retrievable.

Add a dedicated `@ExceptionHandler(PayoutAlreadyExistsException.class)` method to `GlobalExceptionHandler` that returns `ResponseEntity.status(409).body(new PayoutResponse(existing.getId(), existing.getStatus(), existing.getAmount()))`, reading `existing` from `e.getExisting()` (reuse the existing `PayoutResponse` record). A more-specific @ExceptionHandler wins over the generic `@ExceptionHandler(BusinessException)` in Spring, so this method carries the payout body while all other BusinessExceptions keep the generic {code,message} shape.

Update `docs/error-catalog.md`: add a `PAYOUT_ALREADY_EXISTS | 409 | 이미 지급 건이 존재합니다. | 승인 시 정산에 이미 payout 존재(uk_payout_settlement) — 순차 재승인 또는 경합 패자` row in the payout error table (place it after the 401 row, mirroring the enum ordering).
  </action>
  <verify>
    <automated>bash -c "cd /Users/juho/Documents/docswithmulti-payout && ./gradlew :settlement-service:compileJava -q && grep -v '^\s*//' settlement-service/src/main/java/com/example/settlement/common/exception/ErrorCode.java | grep -c 'PAYOUT_ALREADY_EXISTS' | grep -qx 1 && grep -q 'getExisting' settlement-service/src/main/java/com/example/settlement/application/exception/PayoutAlreadyExistsException.java && grep -q 'PayoutAlreadyExistsException.class' settlement-service/src/main/java/com/example/settlement/presentation/GlobalExceptionHandler.java && grep -q 'PAYOUT_ALREADY_EXISTS' docs/error-catalog.md && echo PLUMBING_OK"</automated>
  </verify>
  <done>settlement-service compiles; ErrorCode has exactly one PAYOUT_ALREADY_EXISTS (409) entry; PayoutAlreadyExistsException exposes getExisting(); GlobalExceptionHandler has a dedicated handler for it; error-catalog documents the 409 row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire both approve() paths to 409-return-existing (pre-check + DataIntegrityViolationException race-loser catch); keep approve() non-@Transactional</name>
  <files>settlement-service/src/main/java/com/example/settlement/application/service/PayoutService.java, settlement-service/src/test/java/com/example/settlement/integration/PayoutApproveHardeningIntegrationTest.java</files>
  <behavior>
    - Sequential duplicate: approve a FINALIZED settlement twice → 1st is 200 PROCESSING, 2nd is 409 whose body contains the EXISTING payout id/status/amount (not a fresh row, not {code,message} only).
    - Concurrent race: two threads call POST /v1/settlements/{id}/payout on the same FINALIZED settlement at once → exactly one payout row exists afterward; responses are exactly one 200 and one 409 (never a 500); the 409 body carries the winning payout.
    - Guard 1 (not FINALIZED): approve an OPEN settlement → 400 PAYOUT_NOT_PAYABLE.
    - Guard 2 (net≤0): approve a FINALIZED settlement with net_amount 0 → 400 PAYOUT_NOT_PAYABLE.
    - Guard 3 (no active account): approve a FINALIZED settlement whose merchant has no active payout account → 400 PAYOUT_ACCOUNT_INACTIVE.
    - Missing settlement: approve a non-existent id → 404 SETTLEMENT_NOT_FOUND.
  </behavior>
  <action>
In `PayoutService.approve`, change the existing pre-check (`payoutRepo.findBySettlementId(settlementId).isPresent()`) to `throw new PayoutAlreadyExistsException(existing)` (using the fetched Optional's value) instead of `PayoutNotPayableException`. Then wrap the `payoutRepo.insertProcessing(...)` call in a `try { ... } catch (DataIntegrityViolationException dup)` block: in the catch, re-read `payoutRepo.findBySettlementId(settlementId)` (the winner) and `throw new PayoutAlreadyExistsException(won)`; if the re-read is somehow empty, rethrow `dup`. Import `org.springframework.dao.DataIntegrityViolationException` (already used in settlement main). Leave `bankTransferPort.submit(...)` strictly after the successful insert.

Do NOT add `@Transactional` to `approve()`. Add a code comment at the catch noting the invariant: approve() is intentionally non-transactional so the IDENTITY INSERT flushes at save() and the UK violation surfaces here as DataIntegrityViolationException; a surrounding TX would relocate the INSERT to commit and this catch would miss it (race-loser would regress to 500). Keep the three existing 400 guards and the 404 settlement-not-found unchanged.

Create `PayoutApproveHardeningIntegrationTest` by cloning the `PayoutTracerIntegrationTest` harness (Testcontainers MySQL + RANDOM_PORT + kafka listener off, `@MockitoBean BankTransferPort` stubbed `submit`→`TransferAck(true)`, the `putAccount`/`seedFinalized`/`post`/`statusOf` helpers). Cover every case in <behavior>. For the concurrent-race test, submit two approve POSTs from two threads (e.g. an ExecutorService with a CountDownLatch or CompletableFuture.allOf) against one seeded FINALIZED settlement, then assert `SELECT COUNT(*) FROM payout WHERE settlement_id=?` == 1 and that the two status codes are the multiset {200, 409}. For net≤0, seed a FINALIZED settlement with net_amount 0. For no-active-account, seed FINALIZED but skip putAccount.
  </action>
  <verify>
    <automated>bash -c "cd /Users/juho/Documents/docswithmulti-payout && find . -path '*/build/*' -name '* [0-9].sql' -delete; ./gradlew :settlement-service:test --tests '*PayoutApproveHardening*'"</automated>
  </verify>
  <done>PayoutApproveHardeningIntegrationTest passes: sequential duplicate → 409 with existing payout body; concurrent race → exactly one payout row + {200,409} (no 500); the three 400 guards and the 404 hold. approve() remains non-@Transactional with the DIVE-boundary comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /v1/settlements/{id}/payout | untrusted retries / concurrent approvals cross here; double-pay must be impossible |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | concurrent approve() double-pay | high | mitigate | uk_payout_settlement UK is the race-safe guarantee; DataIntegrityViolationException→409 catch turns the race-loser into a 409-return-existing; exactly one payout row (asserted by 2-thread test) |
| T-02-02 | Information Disclosure | 409 body leaks payout of another tenant | low | accept | body is the caller's own settlement's payout (same settlementId path var); no cross-tenant data — access control is the deploy-time gateway NetworkPolicy (unchanged) |
| T-02-SC | Tampering | supply chain (new deps) | low | accept | zero new packages — reuses pre-pinned JPA/web/Mockito/Testcontainers |
</threat_model>

<verification>
- `./gradlew :settlement-service:compileJava` green; ErrorCode/exception/handler/error-catalog plumbing present (Task 1).
- `./gradlew :settlement-service:test --tests '*PayoutApproveHardening*'` green (Task 2): sequential 409 + concurrent exactly-one + retained 400/404 guards.
- approve() is non-@Transactional with the DIVE-boundary comment.
</verification>

<success_criteria>
- PAY-02: duplicate approve → 409 + existing payout; concurrent approve → exactly one payout (no double-pay), loser 409 not 500; not-FINALIZED/net≤0/no-account stay 400; missing settlement stays 404.
</success_criteria>

<output>
Create `.planning/workstreams/payout/phases/02-hardening/02-01-SUMMARY.md` when done.
</output>
