---
phase: 01-paid-tracer
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - .planning/workstreams/payout/phases/01-paid-tracer/01-03-VERIFICATION.md
autonomous: true
requirements: [INV-01]

must_haves:
  truths:
    - "git merge-base(HEAD, main) diff proves EVERY changed path is confined to settlement-service/ (+ .planning/ · docs/) — no payment/order/product/merchant/risk/user/gateway path appears (payout is settlement-only; net is read from settlement's own row, so — unlike settlement reconcile — no payment allowlist token is needed) [INV-01]"
    - "Exactly one Flyway migration was added to settlement (V2__create_payout.sql); V1 is unchanged and no settlement V-file above V2 exists; no payment (or any other module) migration was added [INV-01]"
    - "The full no-regression suite (payment/order/product/settlement) is green — the payout addition beans-only and touches none of the cancel core, stock path, or existing settlement ledger/reconcile code [INV-01]"
  artifacts:
    - .planning/workstreams/payout/phases/01-paid-tracer/01-03-VERIFICATION.md
  key_links:
    - "The gate drops the PaymentSettlement allowlist token that settlement reconcile's 03-03 gate carried — payout does not call payment at all, so the confinement regex is simply ^(settlement-service/|\\.planning/|docs/)"
    - "Duplicate .sql cleanup (cloud-sync) must run before any gradle build or Flyway sees a duplicate V-file → checksum failure; keep macOS-safe single-quoted globs"
---

<objective>
Prove and guard the Phase-1 INV-01: the payout slice added ONLY settlement-service code (plus planning/docs) — the payment cancel core and every other module stayed diff 0, exactly one settlement Flyway migration (V2) was added, and the full four-module no-regression suite is green.

This mirrors the settlement Phase-3 merge-base gate (03-03-PLAN.md) but with a strictly simpler allowlist: payout is settlement-ONLY, so there is no payment read-surface — the confinement regex has no PaymentSettlement token and the payment denylist is a plain "no payment diff at all" (spec §8: "payment allowlist 확장 불필요 — reconcile와 달리 payout은 payment 조회조차 안 함").

Purpose: turn the structural claim ("payout is a pure settlement-service addition") into a scripted, re-runnable merge-base gate so the invariant is enforced, not assumed — and confirm the cancel/stock/order/settlement suites did not regress.

Output: a passing INV-01 gate + a VERIFICATION.md recording the confinement result, the exactly-V2 Flyway assertion, and per-module suite results.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/payout/phases/01-paid-tracer/RESEARCH.md
@.planning/workstreams/payout/REQUIREMENTS.md
@.planning/workstreams/payout/phases/01-paid-tracer/01-01-PLAN.md
@.planning/workstreams/payout/phases/01-paid-tracer/01-02-PLAN.md
@.planning/workstreams/settlement/phases/03-reconcile-finalize/03-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: INV-01 merge-base gate — settlement-only confinement, exactly-V2 Flyway, no other-module diff</name>
  <files>.planning/workstreams/payout/phases/01-paid-tracer/01-03-VERIFICATION.md</files>
  <precondition>Plans 01-01 and 01-02 are committed on feat/settlement-payout (branched from main), so `git merge-base HEAD main` is the correct base ref.</precondition>
  <action>
Run the INV-01 merge-base diff gate. Compute BASE = git merge-base HEAD main. (1) Confinement: assert every changed path vs BASE matches ^(settlement-service/|.planning/|docs/) — any other path (payment/order/product/merchant-limit/risk-management/user/api-gateway or root config) fails as NON_SETTLEMENT_DIFF. (2) Module denylist (belt-and-suspenders): assert the seven non-settlement module dirs have zero committed diff vs BASE. (3) Flyway: assert settlement added exactly V2 (V2__create_payout.sql exists, no settlement V-file above V2) and that no payment (or other module) migration was added. Delete cloud-sync duplicate migration copies under build/ before running; use single-quoted glob pathspecs (BSD/macOS). Record the confinement result, the changed-file list, the exactly-V2 assertion, and gate PASS/FAIL in 01-03-VERIFICATION.md.
  </action>
  <verify>
    <automated>bash -c 'set -e; find . -path "*/build/*" -name "* [0-9].sql" -delete; BASE=$(git merge-base HEAD main); BAD=$(git diff $BASE...HEAD --name-only | grep -vE "^(settlement-service/|\.planning/|docs/)" || true); test -z "$BAD" || { echo "NON_SETTLEMENT_DIFF: $BAD"; exit 1; }; DENY=$(git diff $BASE...HEAD --name-only -- payment-service order-service product-service merchant-limit-service risk-management-service user-service api-gateway); test -z "$DENY" || { echo "MODULE_DIFF: $DENY"; exit 1; }; test -f settlement-service/src/main/resources/db/migration/V2__create_payout.sql || { echo "MISSING_V2"; exit 1; }; SV=$(ls settlement-service/src/main/resources/db/migration/ | grep -E "^V([3-9]|[1-9][0-9])__" || true); test -z "$SV" || { echo "UNEXPECTED_SETTLEMENT_MIGRATION: $SV"; exit 1; }; PM=$(git diff $BASE...HEAD --name-only -- "payment-service/src/main/resources/db/migration/*" || true); test -z "$PM" || { echo "PAYMENT_MIGRATION: $PM"; exit 1; }; echo INV01_PASS'</automated>
  </verify>
  <done>Gate prints INV01_PASS: every changed path is under settlement-service/ (+ .planning/ · docs/), the seven other module dirs are diff 0, settlement added exactly V2 (no V3+, no payment migration). Confinement + exactly-V2 scope recorded in 01-03-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite no-regression — payment/order/product/settlement green</name>
  <files>.planning/workstreams/payout/phases/01-paid-tracer/01-03-VERIFICATION.md</files>
  <action>
Run the full no-regression suite after the duplicate-.sql cleanup: payment (cancel + stock reserve/release + convergence/idempotency ITs must stay green — proving payout perturbed nothing), order, product, and settlement (Phase 1/2/3 suites — config, sale/cancel ledger, reconcile, immutability, query — plus the new payout ITs: tracer, poll, query, convergence, mock unit). Append the module-by-module pass/fail summary to 01-03-VERIFICATION.md.
  </action>
  <verify>
    <automated>find . -path '*/build/*' -name '* [0-9].sql' -delete; ./gradlew :payment-service:test :order-service:test :product-service:test :settlement-service:test</automated>
  </verify>
  <done>All four module suites pass. Existing cancel/stock/order and settlement ledger/reconcile ITs show no regression; the new payout ITs are green. Summary in 01-03-VERIFICATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payout change → cancel core / other modules | risk that the payout addition perturbs the payment cancel TX, schedulers, stock path, or any other module |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | undeclared change slipping into a non-settlement module | high | mitigate | merge-base confinement gate asserts every changed path is settlement-only + seven module dirs diff 0; runs in verify |
| T-01-10 | Availability | stray/duplicate Flyway version (parallel worktrees) | medium | mitigate | gate asserts exactly V2 in settlement and no payment migration; cloud-sync duplicate .sql purge before build; Flyway fails on duplicate version |
| T-01-11 | Denial of Service | cloud-sync duplicate V .sql → Flyway checksum failure | medium | mitigate | `find . -path '*/build/*' -name '* [0-9].sql' -delete` before every build/gate |
| T-01-SC | Tampering | supply chain (new deps) | low | accept | zero new packages — payout reuses pre-pinned Redisson/web/JPA/Flyway |
</threat_model>

<verification>
- INV-01 gate prints INV01_PASS (Task 1): settlement-only confinement + seven-module diff 0 + exactly-V2 Flyway + no payment migration.
- `./gradlew :payment-service:test :order-service:test :product-service:test :settlement-service:test` all green (Task 2).
- 01-03-VERIFICATION.md records confinement diff scope, exactly-V2 assertion, and per-module suite results.
</verification>

<success_criteria>
- INV-01: payout diff confined to settlement-service/ (+ .planning/ · docs/); payment/order/product/merchant + risk/user/gateway diff 0; Flyway V2 only (V1 unchanged, no payment migration); full four-module suite green.
</success_criteria>

<output>
Create `.planning/workstreams/payout/phases/01-paid-tracer/01-03-SUMMARY.md` when done.
</output>
