---
phase: 01-paid-tracer
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - settlement-service/src/main/java/com/example/settlement/presentation/controller/PayoutAccountController.java
  - settlement-service/src/main/java/com/example/settlement/presentation/controller/PayoutController.java
  - settlement-service/src/main/java/com/example/settlement/application/service/PayoutService.java
  - settlement-service/src/main/java/com/example/settlement/presentation/dto/PayoutAccountResponse.java
  - settlement-service/src/test/java/com/example/settlement/integration/PayoutQueryIntegrationTest.java
  - settlement-service/src/test/java/com/example/settlement/integration/PayoutConvergenceIntegrationTest.java
autonomous: true
requirements: [ACCT-02, PAY-03, ACCT-01, CONFIRM-03]

must_haves:
  truths:
    - "GET /v1/settlements/payout-account/{merchantId} returns the account, or 404 when absent [ACCT-02]"
    - "GET /v1/settlements/{id}/payout returns the payout status, or 404 when absent [PAY-03]"
    - "PUT payout-account with a blank bankCode/accountNumber/holderName returns 400 [ACCT-01 edge]"
    - "When the webhook and the poll both reach the same payout, only the first flips PROCESSING→PAID; the second applyResult updates 0 rows and no-ops — a duplicate callback is likewise idempotent [CONFIRM-03]"
  artifacts:
    - settlement-service/src/test/java/com/example/settlement/integration/PayoutQueryIntegrationTest.java
    - settlement-service/src/test/java/com/example/settlement/integration/PayoutConvergenceIntegrationTest.java
  key_links:
    - "GET endpoints return ResponseEntity and map an empty Optional to ResponseEntity.notFound() (404) directly — no thrown exception, because GlobalExceptionHandler has no 404 mapping and would swallow a thrown not-found to 500"
    - "The convergence IT proves order-independence: webhook-then-poll and poll-then-webhook both terminate exactly once through the shared applyResult chokepoint from 01-01"
---

<objective>
Round out the tracer path into the full Phase-1 read/validation surface and prove order-independent convergence. This expands the controllers created in 01-01 (hence Wave 2 — same files) with the GET queries, the account validation edge, and the CONFIRM-03 idempotency proof that a second arrival (poll after webhook, or a duplicate callback) is a 0-row no-op.

Purpose: complete ACCT-02 / PAY-03 queries and harden the convergence guarantee that both the webhook and the poll funnel through the one applyResult chokepoint with no double effect.

Output: GET account + GET payout endpoints (404 on absent), the blank-field 400 validation, and two ITs (query + convergence idempotency).

## Error convention (encoded)
- Not-found → controller returns ResponseEntity.notFound() (404) on an empty Optional. NO thrown exception and NO shared ErrorCode/GlobalExceptionHandler edit: the existing handler (GlobalExceptionHandler.java:24-35) maps only BusinessException/IllegalArgumentException(→400)/Exception(→500), so a thrown not-found would become 500.
- Blank-field validation → IllegalArgumentException (→400 via the existing handler), mirroring SettlementConfigService.setRate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/payout/phases/01-paid-tracer/RESEARCH.md
@.planning/workstreams/payout/REQUIREMENTS.md
@.planning/workstreams/payout/phases/01-paid-tracer/01-01-PLAN.md
@settlement-service/src/main/java/com/example/settlement/presentation/controller/SettlementQueryController.java
@settlement-service/src/main/java/com/example/settlement/presentation/GlobalExceptionHandler.java
@settlement-service/src/test/java/com/example/settlement/integration/MerchantSettlementConfigIntegrationTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET account + GET payout (404 on absent) + blank-field 400 validation</name>
  <files>settlement-service/src/main/java/com/example/settlement/presentation/controller/PayoutAccountController.java, settlement-service/src/main/java/com/example/settlement/presentation/controller/PayoutController.java, settlement-service/src/main/java/com/example/settlement/application/service/PayoutService.java, settlement-service/src/main/java/com/example/settlement/presentation/dto/PayoutAccountResponse.java, settlement-service/src/test/java/com/example/settlement/integration/PayoutQueryIntegrationTest.java</files>
  <action>
Add GET /{merchantId} to PayoutAccountController: PayoutService looks up the active account (MerchantPayoutAccountRepository.findByMerchantIdAndActiveTrue) and the controller returns ResponseEntity&lt;PayoutAccountResponse&gt; mapping present → 200 body, empty → ResponseEntity.notFound().build() (404). PayoutAccountResponse is a record {merchantId, bankCode, accountNumber, holderName, active}. Add GET /{id}/payout to PayoutController: look up PayoutRepository.findBySettlementId, present → 200 PayoutResponse, empty → 404 (same ResponseEntity pattern). Do NOT throw for not-found (see error convention) and do NOT edit GlobalExceptionHandler or ErrorCode.

Confirm the blank-field guard for PUT (added in 01-01 PayoutService): reject null/blank bankCode, accountNumber, holderName with IllegalArgumentException (→400). If 01-01 already covers it, leave the service as-is and only assert it here.

PayoutQueryIntegrationTest (clone the MerchantSettlementConfigIntegrationTest harness — RANDOM_PORT + kafka auto-startup=false, MySQL Testcontainer, java.net.http.HttpClient, @BeforeEach cleanup): GET missing account → 404; PUT then GET account → 200 with the stored fields; PUT with blank bankCode → 400 (and nothing written); GET missing payout → 404; after seeding a payout row, GET /{id}/payout → 200 with the status.
  </action>
  <verify>
    <automated>find . -path '*/build/*' -name '* [0-9].sql' -delete; ./gradlew :settlement-service:test --tests '*PayoutQueryIntegrationTest'</automated>
  </verify>
  <done>GET account and GET payout return 200 when present and 404 (ResponseEntity.notFound) when absent; PUT with a blank field returns 400 and writes nothing; no ErrorCode/handler edits.</done>
</task>

<task type="auto">
  <name>Task 2: Order-independent convergence + duplicate-callback idempotency (CONFIRM-03)</name>
  <files>settlement-service/src/test/java/com/example/settlement/integration/PayoutConvergenceIntegrationTest.java</files>
  <action>
Prove the shared applyResult chokepoint converges exactly once regardless of arrival order. PayoutConvergenceIntegrationTest (clone the MerchantSettlementConfig harness; @MockitoBean BankTransferPort so getStatus is controllable; drive the webhook via a real POST to /v1/payouts/callback with a valid X-Bank-Signature):

1. Webhook-then-poll: seed a PROCESSING payout (updated_at older than grace). POST a valid PAID callback → status PAID, paid_at set. Then stub bankTransferPort.getStatus → PAID and call payoutResultService.pollStuckProcessing() → the payout is no longer selectable (already PAID) OR applyResult updates 0 rows; assert status stays PAID, paid_at unchanged (no double effect).
2. Poll-then-webhook: seed another PROCESSING payout (stale updated_at); stub getStatus → PAID; pollStuckProcessing() → PAID. Then POST a duplicate PAID callback for the same transferRef → applyResult 0-row no-op; assert status PAID and paid_at unchanged.
3. Duplicate callback: seed a PROCESSING payout; POST the same valid PAID callback twice → first flips to PAID, second is a 0-row no-op; assert exactly one terminal transition (status PAID, paid_at unchanged between the two calls).

Assert paid_at immutability across the second arrival to demonstrate idempotency (the second UPDATE matches 0 rows because status is no longer PROCESSING).
  </action>
  <verify>
    <automated>find . -path '*/build/*' -name '* [0-9].sql' -delete; ./gradlew :settlement-service:test --tests '*PayoutConvergenceIntegrationTest'</automated>
  </verify>
  <done>Webhook-then-poll, poll-then-webhook, and duplicate-callback all terminate exactly once; the second arrival is a 0-row no-op with paid_at unchanged — order-independent, idempotent convergence proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway → GET payout-account / GET payout | financial read; in-service unauthenticated, gated at deploy by NetworkPolicy (same class as SettlementQueryController IDOR note) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information Disclosure | GET account/payout cross-merchant read (IDOR) | medium | accept | deploy NetworkPolicy gateway-only ingress (same class as SettlementQueryController.java:16-18); in-service authz out of scope (spec §10) |
| T-01-07 | Tampering | duplicate/racing terminal callback double-applies | medium | mitigate | applyResult status-guarded UPDATE — second arrival 0-row no-op, asserted by PayoutConvergenceIntegrationTest |
| T-01-08 | Input Validation | blank account fields persisted | low | mitigate | IllegalArgumentException → 400 on blank bankCode/accountNumber/holderName; rejected request writes nothing |
| T-01-SC | Tampering | supply chain (new deps) | low | accept | zero new packages |
</threat_model>

<verification>
- `./gradlew :settlement-service:test --tests '*PayoutQueryIntegrationTest' --tests '*PayoutConvergenceIntegrationTest'` green.
- GET absent → 404; GET present → 200; PUT blank → 400.
- Second arrival (poll after webhook / duplicate callback) is a 0-row no-op with paid_at unchanged.
</verification>

<success_criteria>
- ACCT-02: GET account returns 200 / 404.
- PAY-03: GET payout returns 200 / 404.
- ACCT-01 (edge): blank field → 400, nothing written.
- CONFIRM-03: order-independent, idempotent convergence — exactly one terminal transition.
</success_criteria>

<output>
Create `.planning/workstreams/payout/phases/01-paid-tracer/01-02-SUMMARY.md` when done.
</output>
