---
phase: 03-reconcile-finalize
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - .planning/workstreams/settlement/phases/03-reconcile-finalize/03-03-VERIFICATION.md
autonomous: true
requirements: [INV-01]

must_haves:
  truths:
    - "git merge-base(HEAD, main) diff proves the cancel CORE denylist (CancelPaymentService/CancelTxWriter, 3 recovery + stock-release schedulers, cancel_event_outbox trio, cancel_request idempotency, ProductStockHttpClient, OutboxDataSourceConfig, cancel domain/policy, migrations V1–V18) is diff 0 — unchanged from Phase 2 [INV-01]"
    - "Every changed payment-service path is in the allowlist EXTENDED with the single `PaymentSettlement` token (Phase 2 set + PaymentSettlement) — the only Phase 3 payment change is the new read-only PaymentSettlement* query files [INV-01, RECON-03]"
    - "No new payment Flyway migration exists (RECON-03 queries existing tables read-only) — the highest V-file is still V19 and no version collision surfaces [INV-01]"
    - "The full no-regression suite (payment/order/product/settlement) is green — especially the cancel + stock integration tests (proving the read-only query API and the settlement reconciler did not disturb the payment cancel core) and the settlement reconcile/immutability ITs [INV-01]"
  artifacts:
    - .planning/workstreams/settlement/phases/03-reconcile-finalize/03-03-VERIFICATION.md
  key_links:
    - "Phase 3 keeps the Phase 2 denylist UNCHANGED (cancel core still diff 0) and extends ONLY the allowlist regex with `|PaymentSettlement` — because RECON-03 adds no payment migration and does not touch PaymentController.java, nothing else in payment changes"
    - "All new PaymentSettlement* files (controller, query service, jpa repo, use-case, DTO) match the single `PaymentSettlement` token → one-token allowlist stays clean"
    - "Duplicate .sql cleanup (cloud-sync) must run before any gradle build or Flyway sees a duplicate V-file → checksum failure; keep macOS-safe single-quoted globs + [[:space:]]"
---

<objective>
Prove and guard the Phase 3 INV-01: the reconcile+finalize slice legitimately added ONE read-only payment query surface (the PaymentSettlement* files) and everything else lives in settlement-service's own module — the payment cancel CORE + stock path stayed diff 0, no new payment migration appeared, and the full four-module no-regression suite is green.

This reuses the Phase 2 merge-base denylist/allowlist gate (02-03-PLAN.md:61) verbatim, with the allowlist extended by a single `PaymentSettlement` token (RESEARCH "INV-01 Phase 3 gate"). The denylist is UNCHANGED — the cancel core is still diff 0. The only new permitted payment path is the read-only PaymentSettlement* query files from 03-01; the settlement reconciler (03-02) touches only settlement-service (its own DB/module), which the gate does not police for payment diff.

Purpose: turn the structural claim ("RECON-03 is a disjoint read-only surface; the reconciler is settlement-only") into a scripted, re-runnable merge-base gate so the invariant is enforced, not assumed — and prove the existing cancel/stock/order integration tests plus the new settlement reconcile/immutability ITs did not regress.

Output: a passing INV-01 (Phase 3 variant) gate + a VERIFICATION.md recording the denylist result, the extended allowlist scope, the no-payment-migration assertion, and per-module suite results.

Note (RESEARCH Pitfall 6): if the parallel checkout-buynow branch merged a payment migration or touched PaymentController, the allowlist gate flags it as UNEXPECTED_PAYMENT_DIFF and/or the Flyway build fails — the collision surfaces here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/settlement/phases/03-reconcile-finalize/RESEARCH.md
@.planning/workstreams/settlement/REQUIREMENTS.md
@.planning/workstreams/settlement/phases/02-net/02-03-PLAN.md
@.planning/workstreams/settlement/phases/03-reconcile-finalize/03-01-PLAN.md
@.planning/workstreams/settlement/phases/03-reconcile-finalize/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: INV-01 (Phase 3) merge-base gate — cancel CORE diff 0, allowlist + PaymentSettlement, no payment migration</name>
  <files>.planning/workstreams/settlement/phases/03-reconcile-finalize/03-03-VERIFICATION.md</files>
  <precondition>Plans 03-01 and 03-02 are committed on this branch (feat/settlement-aggregation), branched from main, so `git merge-base HEAD main` is the correct base ref.</precondition>
  <action>
    Run the Phase 3 INV-01 merge-base diff gate — the Phase 2 gate (02-03-PLAN.md:61) with the allowlist regex extended by `|PaymentSettlement`. Compute BASE=$(git merge-base HEAD main). (1) Denylist (UNCHANGED from Phase 2): assert the cancel CORE + stock file set has zero committed diff vs BASE — CancelPaymentService, CancelTxWriter, CancelAuthorizationService, CancelHistoryRecorder, PendingRecoveryService, ProcessingRecoveryService, CompensationRetryService, StockReleaseRetryService, cancel domain/service + domain/policy, the cancel/stock schedulers + CancelEventOutboxPublisher, OutboxCancelEventPublisher + Inline(Async)CancelEventPublisher, CancelEventOutbox*/CancelRequest*/StockReleaseRetry*/CompensationRetry* persistence, ProductStockHttpClient, OutboxDataSourceConfig, and migrations V1–V18. (2) Allowlist (EXTENDED): assert every changed payment-service path matches the permitted set (PaymentCreateTxWriter.java, PaymentEventOutbox*, PaymentCompletedOutbox*, PaymentSettlement, application.yml, V19__create_payment_event_outbox.sql) — any other payment path fails the gate (catches a checkout-buynow migration collision, a PaymentController edit, or an accidental cancel-core touch). (3) No new migration: assert the payment migration dir has no V-file above V19. Use single-quoted glob pathspecs and [[:space:]] (never \s) — BSD/macOS grep. Delete cloud-sync duplicate migration copies under build/ before running. Record the denylist result, the changed-file (allowlist) list, the no-V20 assertion, and gate PASS/FAIL in 03-03-VERIFICATION.md.
  </action>
  <verify>
    <automated>bash -c 'set -e; find . -path "*/build/*" -name "* [0-9].sql" -delete; BASE=$(git merge-base HEAD main); DENY=$(git diff $BASE...HEAD --name-only -- "payment-service/*/application/service/CancelPaymentService.java" "payment-service/*/application/service/CancelTxWriter.java" "payment-service/*/application/service/CancelAuthorizationService.java" "payment-service/*/application/service/CancelHistoryRecorder.java" "payment-service/*/application/service/PendingRecoveryService.java" "payment-service/*/application/service/ProcessingRecoveryService.java" "payment-service/*/application/service/CompensationRetryService.java" "payment-service/*/application/service/StockReleaseRetryService.java" "payment-service/*/domain/service/*.java" "payment-service/*/domain/policy/*.java" "payment-service/*/scheduler/CancelEventOutboxPublisher.java" "payment-service/*/scheduler/PendingRecoveryScheduler.java" "payment-service/*/scheduler/ProcessingRecoveryScheduler.java" "payment-service/*/scheduler/CompensationRetryScheduler.java" "payment-service/*/scheduler/StockReleaseRetryScheduler.java" "payment-service/*/messaging/OutboxCancelEventPublisher.java" "payment-service/*/messaging/InlineCancelEventPublisher.java" "payment-service/*/messaging/InlineAsyncCancelEventPublisher.java" "payment-service/*/persistence/CancelEventOutbox*.java" "payment-service/*/persistence/CancelRequest*.java" "payment-service/*/persistence/StockReleaseRetry*.java" "payment-service/*/persistence/CompensationRetry*.java" "payment-service/*/http/ProductStockHttpClient.java" "payment-service/*/config/OutboxDataSourceConfig.java" "payment-service/src/main/resources/db/migration/V[1-9]__*.sql" "payment-service/src/main/resources/db/migration/V1[0-8]__*.sql"); test -z "$DENY" || { echo "CANCEL_CORE_DIFF: $DENY"; exit 1; }; BAD=$(git diff $BASE...HEAD --name-only -- payment-service | grep -vE "(PaymentCreateTxWriter\.java|PaymentEventOutbox|PaymentCompletedOutbox|PaymentSettlement|application\.yml|V19__create_payment_event_outbox\.sql)" || true); test -z "$BAD" || { echo "UNEXPECTED_PAYMENT_DIFF: $BAD"; exit 1; }; V20=$(ls payment-service/src/main/resources/db/migration/ | grep -E "^V(2[0-9]|[3-9][0-9])__" || true); test -z "$V20" || { echo "UNEXPECTED_PAYMENT_MIGRATION: $V20"; exit 1; }; echo INV01_PASS'</automated>
  </verify>
  <done>Gate prints INV01_PASS: the cancel CORE + stock denylist is diff 0 vs merge-base, every changed payment-service path is in the allowlist (Phase 2 set + PaymentSettlement), and no payment migration above V19 exists. Denylist + extended-allowlist + no-migration scope recorded in 03-03-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite no-regression — payment/order/product/settlement green</name>
  <files>.planning/workstreams/settlement/phases/03-reconcile-finalize/03-03-VERIFICATION.md</files>
  <action>
    Run the full no-regression suite after the duplicate-.sql cleanup: payment (existing cancel + stock reserve/release + convergence/idempotency/loss ITs must stay green — proves the read-only PaymentSettlement query API did not perturb the cancel core or stock path — plus the new PaymentSettlementQueryIntegrationTest), order, product, and settlement (Phase 1/2 suites + the new SettlementReconcileIntegrationTest: back-fill/dedup/finalize + immutability/defer/drift/cross-week). Append the module-by-module pass/fail summary to 03-03-VERIFICATION.md.
  </action>
  <verify>
    <automated>find . -path '*/build/*' -name '* [0-9].sql' -delete; ./gradlew :payment-service:test :order-service:test :product-service:test :settlement-service:test</automated>
  </verify>
  <done>All four module test suites pass. Existing cancel/stock/order integration tests show no regression; the new payment read-only query IT and settlement reconcile/immutability ITs are green. Summary in 03-03-VERIFICATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 3 change → cancel core | risk that the read-only query surface or the settlement reconciler perturbs the payment cancel TX, schedulers, stock path, or existing consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | undeclared cancel-core change slipping into payment | high | mitigate | merge-base denylist gate asserts cancel CORE + stock diff 0; the allowlist (Phase 2 set + PaymentSettlement) rejects any non-permitted payment path; runs in verify. |
| T-03-09 | Availability | Flyway V-version collision / stray payment migration (parallel checkout-buynow) | medium | mitigate | RECON-03 adds no payment migration; the gate asserts no V-file above V19 and the allowlist flags a stray payment path as UNEXPECTED_PAYMENT_DIFF; Flyway build fails on a duplicate version. |
| T-03-10 | Denial of Service | cloud-sync duplicate V .sql → Flyway checksum failure | medium | mitigate | `find . -path '*/build/*' -name '* [0-9].sql' -delete` before every build/gate. |
| T-03-SC | Tampering | supply chain (new deps) | low | accept | Phase 3's only new deps (redisson-spring-boot-starter:4.3.1, spring-boot-starter-restclient) are added to settlement-service and are already vendored/version-pinned across sibling modules — no registry-novel package. |
</threat_model>

<verification>
- INV-01 (Phase 3) gate prints INV01_PASS (Task 1 verify): cancel CORE denylist diff 0 + allowlist (Phase 2 set + PaymentSettlement) only + no payment migration above V19.
- `./gradlew :payment-service:test :order-service:test :product-service:test :settlement-service:test` all green (Task 2 verify).
- 03-03-VERIFICATION.md records denylist/allowlist diff scope, no-migration assertion, and per-module suite results.
</verification>

<success_criteria>
- INV-01 (re-verify): cancel CORE + stock diff 0 (merge-base denylist gate); only allowlist payment files changed (Phase 2 set + the PaymentSettlement read-only query files); no payment migration above V19; existing cancel/stock/order integration tests no-regression; full four-module suite green.
</success_criteria>

<output>
Create `.planning/workstreams/settlement/phases/03-reconcile-finalize/03-03-SUMMARY.md` when done.
</output>
