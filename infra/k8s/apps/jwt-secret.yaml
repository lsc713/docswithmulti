apiVersion: v1
kind: Secret
metadata: { name: jwt-secret, namespace: default }
type: Opaque
stringData:
  # 배포 시 실제 256-bit(32+ byte) HS256 대칭키로 교체하라 — 아래는 placeholder(실 값 하드코딩 금지, DEPLOY-03).
  # user-service와 동일 값이어야 서명·검증 일치(불일치 시 전면 401).
  JWT_SECRET: REPLACE_ME_AT_DEPLOY
